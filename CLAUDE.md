# GlowPT — Project Guide (for Claude Code)
*Living doc. Loaded automatically every session when working in this folder. **⏰ IT IS SATURDAY 2026-09-12 AS OF THE LAST EDIT. RUN `date` BEFORE SAYING ANYTHING ABOUT TIME — DAVID'S DAY STARTS AT 3AM, SO 3 TO 6AM IS HIS MORNING.** Updated 2026-09-12 (**📅 A CHECK-IN NOW BELONGS TO THE PATIENT'S OWN DAY, NOT TO A UTC ONE — THE 07:20 BUG REPORT WAS FIXED, SHIPPED AND VERIFIED BY 08:20.** David: *"it says he has a 1 day streak. But he checked in yesterday too."* **It was DATA LOSS, not a wrong number**: the save keyed the day off `created_at` in UTC while every screen bucketed by local day, and in Eastern the UTC day rolls over at 8pm — so an evening check-in was filed under the next UTC day and the next morning's OVERWROTE it. **Confirmed against Charlie's real rows**, whose Friday 20:14 entry was carrying Saturday morning's "I got up at 3am". **Every check-in now stores `local_date`, the calendar day the PATIENT'S OWN DEVICE was in**, and that is the day key everywhere — index, streak, grids, Last Check-In, weekly email. **David chose that over shifting the boundary to Eastern**, which would have been wrong the moment a clinic outside Eastern signed up. **It also fixed a bug nobody had hit: the roster bucketed days by the STAFF member's browser.** **The destructive overwrite is now impossible by construction**, not merely guarded. Four-step deploy (frontend → patch 1 → API → patch 2) with **no window in which a patient could not check in**; the one already-overwritten row repaired by rule, and **Felix's untouched Friday entry correctly stayed put**. **⚠️ TWO TRAPS CAUGHT IN TESTING: `node-postgres` parses a `date` column into a JS Date** (which would have silently rendered every screen as though the patient had never checked in), **and patch 2 was not re-runnable** — a second run would have dropped the index it had just installed. Tests **69 → 71**. **And `run_tests.sh` hides a suite that ERRORS**, so a short run is not a passing one.) Previously updated 2026-09-11, TWO THREADS (the first ran out of context mid-request; its work was already committed) (**⬛ THE WHITE TRIM AROUND EVERY SCREEN IS GONE, AND IT WAS THE VITE SCAFFOLD IN `src/index.css` ALL ALONG.** Three leftovers stacked: `--bg: #fff` painting the html canvas WHITE, a literal `border-inline: 1px solid #e5e4e7` on `#root`, and `color-scheme: light dark`. **That last one is why David saw it only on the day he took his iPhone OUT of dark mode** — in dark mode the browser's own canvas went dark and hid the other two, which is exactly what he described. Fixed at the root, not patched per screen: navy on html AND body, `color-scheme: dark`, no border, no 1126px cap, plus a `theme-color` meta and `viewport-fit=cover` so iOS Safari's toolbars match instead of sampling. Measured navy edge-to-edge at 375 and 1500 in BOTH modes. **📏 The welcome screen's top padding went 20 → 40 → 56 → 72, David nudging it by eye.** **🧹 The three dead scaffold files are deleted** (`src/assets/react.svg`, `src/assets/vite.svg`, `public/icons.svg`), confirmed unreferenced and the bundle byte-identical after. **🔁 THEN THE FIVE PER-SCREEN `<style>` BLOCKS WERE CONSOLIDATED INTO ONE, AND TWO LIVE BUGS FELL OUT OF THE DRIFT: Admin and Dashboard render Fraunces upright-400 but never loaded that face** (measured 297 vs 306px for the same string, so the manager's greeting has always been a step light), **and the patient app downloaded the whole unused Cormorant Garamond family.** Fonts now load from ONE `<link>` in `index.html`. **🎯 THEN THE FOCUS RINGS WERE UNIFIED AT 0.5 IN ONE RULE, AND QUALIFYING THE SELECTOR FIXED A LATENT ACCESSIBILITY BUG: a bare `input:focus` had been stripping the focus outline from the three CONSENT CHECKBOXES**, so a keyboard user tabbing onto the BAA checkbox saw nothing at all. **⚠️ AND ONE OF THOSE SHIPS BROKE HIS PHONE OVERNIGHT AND WAS REVERTED 2026-09-12 04:10: `viewport-fit=cover`, which I added unprompted, extends the page UNDER the status bar and home indicator while nothing in the app uses safe-area insets** — so every screen's top padding was eaten and the dashboard's Sign Out could not be tapped. **`theme-color` was always the half that fixed the white trim; do not put `viewport-fit=cover` back without safe-area padding first.** **⚠️ AND A SECOND ONE THE SAME NIGHT: the global `overflow-x: hidden` from the consolidation TURNED OFF THE MODAL SCROLL LOCK** (on `<html>` it stops body's overflow propagating to the viewport, which is the only reason `body{overflow:hidden}` locks anything); back on Dashboard and Admin only. **Both regressions were unrequested extras that moved a property onto `html`. `-webkit-text-size-adjust` was put back on the Dashboard for the same reason before it could bite.** **✅ BOTH FIXED 2026-09-12 04:00-04:40 AND DAVID SIGNED OFF ("good work. thanks"), AND THE TAP DIMMING HE LIKES IS STAYING.** **✅ AND RIVERSIDE WAS RE-PRISTINED AT 05:00 SO SUNDAY'S HEARTBEAT IS READY** — 1 of 6 patients inside the window before, 5 of 6 after; David must sign in again as `besoulful@gmail.com` because every demo sub is fresh. **✅ AND THE ONE BUG REPORTED 09-12 07:20 IS FIXED AND LIVE AS OF 08:20: a check-in made after 8PM EASTERN was filed under the NEXT UTC day, so the next morning's check-in OVERWROTE it** — Charlie showed a 1-day streak after checking in two days running, and his Friday evening entry was genuinely destroyed. **Confirmed against his real rows, then fixed: every check-in now carries `local_date`, the calendar day the PATIENT'S OWN DEVICE was in, and that is the day key everywhere.** Four-step deploy (frontend → patch 1 → API → patch 2) with no window in which a patient could not check in; the one already-overwritten row was repaired by rule, and Felix's untouched Friday entry correctly stayed put. **Two traps caught in testing: `node-postgres` turns a `date` column into a JS Date (which would have silently emptied every screen), and patch 2 was not re-runnable.** Tests 69 → 71. **Read the block at the top of the NEW THREAD section first.**) Previously updated 2026-09-10, SHORT SESSION, CLOSED BY DAVID ("we're done for today") (**📐 FRONT-DOOR LOGOS ARE 154** via one `FRONT_DOOR_LOGO_SIZE`, 176 having read too big; inner screens stay 132. **"Your Patients" and "Clinic Overview" are Title Case.** **🧑‍⚕️ PT EMILY JOINED RIDGEPT as a second therapist, the staff invite walked end to end again ("went really well"), and Miracle and Tommy are on her caseload. Sunday volume is 16.** Nothing outstanding in code.) Previously updated 2026-09-08, ONE SESSION, FOUR SHIPS, ALL LIVE, DAVID SIGNED OFF ("looks good now buddy") (**🔗 THE WEEKLY PATIENT EMAIL'S BUTTON NOW POINTS AT `/login`, AND `/login` HANDS A SIGNED-IN USER TO `/`.** James tapped "Open GlowPT" and landed on the SALES page, because the button pointed at the site root, which shows Landing when signed out. Now `/login` for the patient button; and `/login` redirects anyone already signed in to `/`, so a patient with live tokens still lands in their check-in. **📐 LOGO SIZES SETTLED: the two FRONT DOORS (public landing page, signed-in welcome screen) are 176; every INNER screen is `LOGO_SIZE = 132` from `AuthShell.jsx`** (they had ranged 116 to 208 across 21 call sites); **the 34px app-bar mark stays still and small.** **📏 THE WELCOME SCREEN'S GAP ABOVE "Good afternoon" IS A FIXED 26px (40 until 2026-09-12)** at every window height; it had been the window's leftover height, 113px on a phone and 191px on a desktop. **Vocabulary, since he asked: LANDING PAGE = public sales screen, signed out; WELCOME SCREEN = signed-in patient's front door.** Lambda deployed 21s, deployed code pulled back, dry run `queued:15`; frontend live. Also 2026-09-07 afternoon: **🌅 EVERY HERO LOGO FLOATS** from one declaration, app-bar mark excepted at David's confirmation. Detail under Status & backlog, newest first.) Previously updated 2026-09-07 afternoon, 13:25 to 14:10 (**📅 THE WEEKLY EMAIL NOW FIRES SUNDAY 6PM EASTERN, YEAR-ROUND, NOT MONDAY 8AM.** David's call: clinics run Monday to Friday and a Monday patient can have a 7am appointment, so Monday 8am reached staff after the week had begun. The fire time IS the week's cutoff (the Lambda counts the 7 days ending then), so Sunday evening makes the window Monday through Sunday. Moved from an EventBridge RULE (UTC only, so the old 8am was really 7am all winter) to EventBridge SCHEDULER with a real time zone. Three copy lines moved with the day ("this week", "A fresh week starts tomorrow"). Deployed, schedule read back live, old rule confirmed gone, dry run `queued:15`. **Next fire: Sunday 2026-09-13 18:00 EDT. Same 15 recipients.** Also that morning's heartbeat was OBSERVED: all 15 landed, Felix's included. Detail under Status & backlog, newest first.) Previously updated 2026-09-07 morning, 04:50 to 07:40, ONE SESSION, NO CODE TOUCHED (**🚫 AWS CLOSED THE BEDROCK CASE WITH A NO.** At 03:21 EDT AWS replied to case `178761116000010` and marked it **Resolved**: the Bedrock team is prioritizing model access for customers **enrolled in the AWS Startup Program (Activate) or holding a dedicated Account Manager**; everyone else gets access "gradually as part of the standard rollout", no date. **This contradicts Omkar's 2026-09-03 promise of an internal review to set standard defaults; the review evidently came back "not for accounts like this one" and nobody said so plainly.** Verified the same morning: `converse` still throttles, `L-6120CF2D` still applied 0. **The email's Free Tier note does NOT apply: the FranklinAI management account is on the PAID plan, ACTIVE, verified via `aws freetier get-account-plan-state`.** **✅ DAVID APPLIED TO AWS ACTIVATE, FOUNDERS TIER, THE SAME MORNING (submitted ~07:30 EDT)**, Builder ID `david@franklinaisolutions.com`, linked to **glowpt-prod `463556655381`**, credits to apply there; AWS says **5 to 7 business days**, so expect the outcome **2026-09-15 to 09-17**. **When it lands, reopen the Bedrock case citing Activate membership; that is the fast lane AWS itself named.** **🏗️ ORG-WIDE ANSWER DECIDED IN PRINCIPLE: whichever account gets Bedrock unlocked becomes the org's Bedrock account; future products call it cross-account via an assumed IAM role**, because the reduced starting quota is per ACCOUNT and every new account would start at zero again. Detail under Status & backlog, newest first.) Previously updated 2026-09-06 evening, 17:00 to 19:15, ONE SESSION, NINE SHIPS, ALL LIVE AND VERIFIED (**🔤 THE ROSTER SORTS BY FIRST NAME** (retiring the July "most concerning first" order on purpose; the pills carry triage now). **✉️ THE PATIENT INVITE EMAIL SAID "how you are getting on"**, the one British idiom in any user-facing string; David found it in a real invite and it is "how you are doing" now, one Lambda deploy, verified by pulling the deployed code back. **🇺🇸 AMERICAN ENGLISH, SPELLING AND IDIOM, IS NOW A MEMORY RULE** after David caught days of centre/colour/behaviour; 12 code comments and 57 doc lines swept, no user-facing string had any. **🧑‍🤝‍🧑 FELIX JOINED RIDGEPT AS "Miracle Peterson" ON HIS OWN EMAIL THROUGH A REAL INVITE AND SAID IT WORKED WELL**; Danny Hughes followed; RidgePT has 5 patients. **📐 EVERY PATIENT NAME IS STACKED, EVERY FLAG PILL IS ONE FIXED SIZE, AND THE PATIENT COLUMN MEASURES ITSELF TO THE WIDEST WORD, 66 floor / 140 ceiling** (David re-opened the afternoon's pin himself after Felix joined as "Miracle Peterson" and wrapped); the pills were secretly 34px tall from the absolute line-height trap and are 23 now; the scroll floor is computed from the columns instead of hand-typed. **🙂 THE MOOD SCALE IS ONE COMPONENT NOW, `src/screens/FeelingScale.jsx`, rendered by BOTH the patient check-in and the manager legend.** David asked a third time why the two did not match; they could not while they were two hand-written copies. **⛔ AND IT TOOK THREE CUTS: the first made both look like the cramped manager legend instead of the airy patient card; the second fixed the sizes (old patient card, face 36 as hero, numeral 26) but STILL missed his actual complaint, which was that the faces and numbers ZIGZAG up and down across the five cards because one-line words made their cells center differently.** Third cut anchors the cells to the top with a fixed two-line word slot; every face and number now sits on one line, measured. Detail under Status & backlog, newest first.) Previously updated 2026-09-06 (**⭐ SUNDAY: FIVE SESSIONS, ALL SHIPPED AND LIVE, ALL VERIFIED. Consolidated here rather than left as five stacked entries; full detail for each is under Status & backlog, newest first.** **(1) 🗂️ DISCHARGE IS NOW "ARCHIVE", AND A MANAGER CAN PERMANENTLY REMOVE A PATIENT WHO HAS CHECK-INS** — three words, three meanings: **Inactive** (the automatic 5-day flag), **Archive** (clear the roster, reversible, Undo not a dialog), **Remove** (permanent, gated on a typed name). **And Remove finally removes the PERSON: nothing in this app had ever deleted a Cognito account, so every previous removal left the login alive forever.** ⚠️ The deploy failed first time because **AWS does not offer the `cognito-idp` VPC endpoint in every AZ** (1b/1c/1d, not 1a); it rolled back cleanly. **✅ David removed Felix through the screen and his account is gone from the pool.** Tests 64 → 69. **(2) 🔗 THE PATIENT INVITE LINK IS PERMANENT.** It lived only in a one-shot panel, so it vanished the moment you dismissed it — and the link a manager most wants is the one for a patient who has NOT signed in yet, exactly the case the panel had stopped showing. Every pending row now carries **Copy Link** beside Resend and Cancel. **(3) 🚨 NOTHING ANYWHERE EVER TOLD A PATIENT HOW TO COME BACK, and their invite link — the only GlowPT address they had ever been given — 404'd once used and told them to ask the clinic for a NEW INVITE. They needed to SIGN IN.** That screen now leads with Sign In, so **ONE LINK SENT ONCE WORKS FOREVER**: it joins them, then it signs them in. The invite email and the reflection screen now name `glowpt.app`. **(4) 🔑 A "Patient Sign-In Link" CARD under the invite form**, one neutral link for every patient, **copying an INSTRUCTION rather than a bare URL**. ⛔ It shipped on the roster row first and David had it removed within the hour: **a per-row button implies a per-row link, and a joined patient's token is spent.** **(5) 🙂 THE MOOD LEGEND WENT THROUGH THREE SHAPES IN ONE MORNING and ended as a REAL COPY of the patient check-in scale** — cards, borders, numbers — after David said *"The legend didn't change!!!!"*. It had, and his screenshot showed it; **but copying the ARRANGEMENT and none of the APPEARANCE reads as no change at all.** **"3-Day Trend" became "Last 3 Check-Ins"** (it renders the last three CHECK-INS, which can span any number of days), the order is **newest first**, and the empty slot is a **visible ring** (contrast **1.41 → 3.80**, which is why he had never noticed it). **(6) 📐 THE ROSTER COLUMNS WERE RE-MEASURED TWICE** against the real Riverside names by giving the grid `max-content` tracks: Therapist and Archive were more than half air. **Phone scroll 427px → 345px.** **(7) ❓ THE REFLECTION QUOTING "you're at a 3" IS NOT NEW** — the score has been in the prompt since 2026-07-12 and nothing ever told the model to avoid the digit. It reads oddly, so the prompt now says to name the feeling in WORDS; the score stays in the data as the model's tone signal. **Prompt-only, so a request not a guarantee.** **(8) ⚠️ THE INFRA SUITE WAS RED AGAIN**, all 3 tests, one day after being fixed, broken by a deploy that had itself succeeded. **`npx jest` takes 8 seconds and belongs beside `cdk diff`.** **📱 On the home-screen question: NOBODY can put an app there. iPhone allows no prompt at all, only instructions; Android needs a manifest we do not have. David chose the smaller option, so it is not built.** **🔴 AND ONE COMMUNICATION LESSON: I typed `●` into chat to describe a trend row and his client rendered it as a RED dot, so he went hunting for a red dot that does not exist. The app's is a CSS circle, not a character. A glyph in chat is not the thing on screen.**) Previously updated 2026-09-05 (**🐞 A HARD DAY ON ONE THREAD, THREE SESSIONS, ALL FIXED. Consolidated; full detail under Status & backlog.** **(1) AN INVITED PATIENT COULD NOT GET IN — SIX SEPARATE FAULTS, FIVE OF THEM PRE-EXISTING.** `SignUp` takes an address immediately and UNCONFIRMED, so abandoning an invite at the code screen leaves an account that exists in Cognito and is invisible to the database. The next invite threw `UsernameExistsException` and `lib/cognito.js` **fell back to sign-in** (the tell was an **8-digit code; sign-up codes are 6**), so `register_user` never ran and the person had **no `public.users` row**; every attach RPC then died on `profiles_id_fkey` and `auth.jsx` swallowed it into a `console.log`. **🔓 AND THE EMAIL GUARD WAS FAILING OPEN** — `lower(v_inv.email) <> lower(v_email)` is **NULL, not true**, when the caller has no users row, so the promise that a forwarded invite link grants nothing was being kept by a foreign key rather than by the guard written to keep it. **Fixed FIRST, because fixing the other without it would have opened a real hole.** New `ensure_self()` takes **NO id**, so `glowpt_app` still cannot mint an arbitrary identity. **✅ Felix is in.** **(2) 🔒 WALK-IN SIGN-UP, THE QR AND THE `/join` LINK WERE REMOVED ENTIRELY** (David's call): every clinic is invite only, and the route that could turn it back on is **deleted** (it 404s; a live route 401s). The `open_signup` column and its DB gate are **kept on purpose** so the feature can return. **(3) 🚨 ONE CHECK-IN STORED WITH `feeling = 0` BLANKED THE STAFF DASHBOARD FOR BOTH ROLES.** `Number(null)` is `0` and `Number.isInteger(0)` is true, so the guard written to REJECT a missing rating passed it through; `FEELINGS[0]` is `undefined`; and with **no error boundary anywhere**, reading `.word` off it unmounted the whole tree. Fixed in three layers, **the mood became the one required answer**, and **the app got its FIRST ERROR BOUNDARY** plus a visible retry when a check-in fails to save. **(4) 🎨 `#root` HAD `text-align: center`**, inherited invisibly by every screen — a flex cell ignores it, which is why roster headers sat up to 240px from their own cells. **I dismissed that bug as David misreading a screenshot, because my harness rendered the table with no page around it.** The global is gone and the roster renders from ONE column declaration. **(5) NEW: cancel a pending invite** (which closed a hole — a wrong-address invite was claimable for 14 days) **and remove a patient enrolled by mistake**, adding the app's ONLY delete rights. **(6) ✂️ The em-dash rule is now ENFORCED IN CODE** (`src/lib/houseVoice.js`), because three rounds of prompt wording did not hold. **(7) 🛡️ TWO BUILD GUARDS run on every deploy**, because two failures shipped through a green build — a scripted edit deleted six API exports and a namespace import hid it. **Tests 47 → 62.** **⛔ THE DAY'S REAL LESSON: I invented a third-party API's error behavior and built a branch on it without testing it. No build, lint, type-check or schema test can catch a false belief about someone else's API. Only calling it can.**) Previously updated 2026-09-04 (**⭐ A BIG DAY, FIVE SESSIONS, ALL OF IT SHIPPED AND PROVEN. Consolidated here rather than left as five stacked entries.** **(1) 🚪 THE INVITE SYSTEM WAS BROKEN AND IS NOW REBUILT PROPERLY.** "Invite Therapist" had worked on Supabase (David used it), but the AWS cutover removed self-creation at `/login` and left staff **no door at all** — and the V4.2 note had warned in bold against exactly that. Now: **every invite carries a random 256-bit token, a 14-day expiry and a real SES email**, `/invite/<token>` serves staff AND patients, and the manager gets a copyable link. **⛔ THE TOKEN IS NOT A CREDENTIAL** — the signed-in user's VERIFIED email must match the invite, so a forwarded link grants nothing. **Both flows walked end to end in production by David: PT Pete (therapist) and Timmy (patient), each from email to a real AI reflection, and PT Pete's caseload view confirmed correct.** **(2) 🔒 THE OPEN-JOIN-LINK HOLE IS CLOSED.** Anyone with, or guessing, `/join/<slug>` could enrol in any switched-on clinic. **New `clinics.open_signup`, a THIRD flag separate from `activated_at` and `baa_signed_at`, FALSE by default so a new clinic is never exposed.** Patient invites record consent in the same transaction as the attach, which the staff door cannot do, so **the two doors now refuse each other's invites.** **LIVE: Riverside = open link + QR (demo + heartbeat), RidgePT = invite only.** The switch is the MANAGER's, on their own dashboard. **(3) 🧪 THE SANDBOX CLINIC EXISTS: `RidgePT`, `/join/ridgept`.** The doc had said since 2026-08-25 that none existed. **Stop suggesting David test inside Riverside.** **(4) ⚖️ THE PRIVACY NOTICE AND BAA SUMMARY NOW NAME BEDROCK, NOT ANTHROPIC** (v2 → v3, summary-v2 → summary-v3). **⚠️ DELIBERATELY AHEAD OF THE CODE: production still calls `api.anthropic.com`.** Safe because a real patient cannot use GlowPT before Bedrock lands. **If Bedrock is ABANDONED rather than merged, revert this copy with it.** **(5) 📱 The QR download did nothing on an iPhone** (iOS ignores `download` on a data: URL); now uses the share sheet. **⏳ STILL UNCONFIRMED ON HIS PHONE.** **(6) ✂️ THE EM-DASH BAN IN THE AI PROMPT WAS ONLY HALF A BAN** — the model substituted a spaced hyphen and an en dash. Fixed. **Lesson: a prohibition a model can satisfy with a near-synonym is not a prohibition.** **(7) TESTS 24 → 47.** ⚠️ **MONDAY EMAIL VOLUME IS NOW 12** (Riverside 8, RidgePT 4). **Bedrock UNCHANGED: AWS holds the case, David does nothing.** Detail for all of it under Status & backlog.) Previously updated 2026-09-03 (**⭐ AWS ANSWERED THE BEDROCK CASE, PLUS ONE LANDING-FOOTER FIX. No behavior touched; the AI path and the whole AWS stack are untouched.** **(1) THE FOOTER:** the byline and the entity line sat 32px apart and read as two statements rather than one block. Cause was **NOT** the margin: `body` sets an **absolute** `line-height: 26.1px`, which **inherits as-is rather than rescaling**, so both 12px lines were getting a 2.17 ratio. Both now state `lineHeight: 1.5` (load-bearing, do not delete) and the margin is `0`, putting them exactly one line-height apart: **32.1 → 18px**. ⚠️ **The same trap applies to any style in this app under 18px that does not state its own `lineHeight`; deliberately NOT swept app-wide.** Detail under Status & backlog. **(2) BEDROCK:** A named AWS engineer replied to case `178761116000010` at 09:06 EDT, moving it **Unassigned → Pending Amazon**. He **confirmed the diagnosis**, gave the **real cause for the first time — new accounts with limited usage history start with reduced limits that rise automatically with usage** (so BOTH candidate explanations this doc carried for ten days were wrong), and has **submitted the account to an internal review team to set the Bedrock inference quotas to standard defaults, 3 to 5 business days**. **David does nothing and must NOT file Service Quotas requests.** Consequences: **the Friday 2026-09-04 decision point is superseded and both options — $29 Developer Support, and shipping with the AI reflection off — are off the table**; the next signal is AWS's follow-up email, expected roughly 2026-09-09 to 09-11. **The attorney gate is now the only one needing a human push.** Detail in item 1 of the NEW THREAD block. Previously updated 2026-09-01 (**LAYOUT STACKING SHIPPED BY A CROSS-REPO SESSION + A NEW WORKING CONVENTION + TWO PROCESS CONFIRMATIONS FROM DAVID.** (1) The three 2026-09-01 entries under Status & backlog (price line stacked as two constants, modal contact line stacked, landing footer names the legal entity) were shipped by **ONE session that also made the FranklinAI comma change and cut that repo's CLAUDE.md V54** — a single cross-repo session, which is established practice; the confusion it caused from the outside produced the convention in (2). (2) **NEW bullet under How to work with David: any session that will touch AWS, the DB, deploys, or legal work must be ROOTED in this project**, because the AWS allowlist, the per-project memory, and this doc's auto-load are all keyed to this folder. (3) **David confirmed the Bedrock plan: wait until Friday 2026-09-04, then "maybe pay if needed, we'll see"** — recorded at the decision point in the NEW THREAD block. (4) **David expects the attorney review DONE this week (week of 2026-09-01)** — recorded under item 2 there. Nothing else changed; production untouched.) Previously updated 2026-08-31 (**MAINTENANCE + HOUSE-RULE CATCH-UP, plus two clinics changed on the live DB.** (1) **⚠️ THE MONDAY HEARTBEAT MOVED. `Weekly Test PT` IS DELETED and Riverside PT is now the heartbeat** — Expected Monday volume is now **8, not 12**. **⚠️ AND THE GMAIL FILTER THIS DOC HAS DESCRIBED SINCE 2026-08-23 DOES NOT EXIST** — David confirmed 2026-08-31 he never set it up, so all 8 land in his inbox and no filter action is needed. Do not tell him to fix one. (2) **The em-dash ban and the palette sweep had BOTH missed `infra/lambda/`**, because the 2026-08-27 and 2026-08-25 passes were scoped to `src/`. Three user-facing Lambda strings carried em dashes, incl. the clinic email SUBJECT line and the ai-response FALLBACK a patient reads on failure. Fixed on `main` AND on `bedrock-ai-response`, since that branch carries its own copy of the file. (3) **The weekly email now carries the REAL logo** (it had none, just a stock 🌅 standing in) and the current amber. **All emoji are gone from both Lambdas.** (4) **The three modals close on Escape and trap focus**; `useScrollLock.js` became `useModal.js` and absorbed the job. (5) **The bastion AMI is PINNED** — every deploy had been trying to REPLACE the bastion and change its instance id. (6) Riverside re-pristined. (7) Decorative emoji swept from the app, **but the streak 🔥 was put BACK at David's request** and the mood faces were never touched. **Bedrock is UNCHANGED and still blocked — case still Unassigned at 7 days.** Detail under Status & backlog. **Original 2026-08-30 entry follows:** Previously updated 2026-08-30 (**COPY + PRESENTATION, no behavior touched. FOUR THINGS HAPPENED TODAY.** (1) **⚠️ THE BEDROCK FRAMING WAS CORRECTED BY DAVID and the old version was WRONG** — there are **TWO gates on real patients, the clinic BAA AND Bedrock, and both are required**, because David has ruled out paying for an Anthropic 1P BAA, which leaves Bedrock as the only route to a HIPAA-covered AI call. Yesterday's entry said in bold that Bedrock was not the gate; **delete that idea on sight.** Full corrected version in the NEW THREAD block. (2) **The AWS case was escalated** 2026-08-29 07:09 EDT and is still unanswered. (3) **Copy:** the pitch's second sentence, the two bullet lists reconciled to 8 and 8, and the one-subscription bullet rewritten twice. (4) **Presentation:** the More Info modal footer centered, the BAA summary line moved above the submit button, and **`ui.fine` went upright DM Sans app-wide** across five screens. Companion FranklinAI work shipped at **CLAUDE.md V53**. Detail for (3) and (4) under Status & backlog. **Original (3) entry follows:** the pitch's second sentence became "More completed plans of care and the clinic stays full." on all four public surfaces, lifted from the shared bullet so the two match; and **the two bullet lists are now IDENTICAL at 8 and 8, same words same order, which REVERSES the old "do not reconcile the counts" rule** at David's direction. Both changes span this repo and FranklinAI. Hero re-checked at 375px and 1280px. ⚠️ One redundancy this created on glowpt.app only, flagged and undecided: "Patients join free" now appears in bullet 3 AND the price line beneath it). Previously updated 2026-08-29, 2nd session (**BEDROCK CASE ESCALATED, no code touched** — the AWS support case sat Unassigned five days with no contact, so a follow-up correspondence was posted 07:09 EDT carrying new evidence: Bedrock's own control plane now reports the account `AUTHORIZED` and entitled while every on-demand quota reads 0. Text at `docs/bedrock-escalation-2026-08-29.md`. **Nothing else changed; production is untouched and still on Anthropic.** ⚠️ Standing corrections in the Bedrock block: **do not suggest paying for Developer Support** (the free channel is legitimate and is what resolved SES), and **do not suggest an Anthropic 1P BAA as the quick unblock** — David has ruled it out on cost. **NOTE: this entry originally also claimed Bedrock was not the gate on real patients. David corrected that on 2026-08-30 and the Bedrock block now carries the right version: there are TWO gates, the clinic BAA AND Bedrock, and both are required.**) Previously updated 2026-08-29 (**COPY REWRITE + MODAL CLOSE CONTROLS + TWO LAYOUT PINS, no logic touched** — the shared pitch became TWO sentences and the two sites now deliberately DIFFER on naming the niche; every modal gained an X and a small Close; the `/onboard` title is pinned to two lines; the hero measure moved 24ch to 28ch. Commits `3008119`, `14d512b` here and `3bfb051`, `6b48e87` on FranklinAI). Earlier entries are in `docs/history.md`.*

## ⚖️ WHAT MAY BE ADDED TO THIS FILE (set 2026-09-12, and it is the reason the file is readable)

**This file is loaded IN FULL at the start of every session, so its size is a tax on every thread.** On 2026-09-12 it had reached **478 KB / ~135,000 tokens** and was quadrupling every eleven days — every conversation began ~14% full, and David was having to start a new thread constantly. **The cause was Claude, not David:** each session wrote a long narrative entry and the doc told the next session to do the same. A loop that only grows.

**⛔ SO: THIS FILE IS A GUIDE, NOT A JOURNAL.**

**A new session may add:**
- **A durable rule** — something a future session must not get wrong. One or two lines. Put it under STANDING RULES.
- **A change in current state** — what is blocked, what is live, what a date is now. Edit the existing line; do not append a new one beside it.
- **A backlog entry for work shipped in the last two weeks**, and keep it short: what broke, the rule it leaves behind, and what was observed. **Not the investigation.**

**It does NOT go here:**
- The reasoning and narrative of a fix — **that belongs in the commit message**, which is permanent, searchable with `git log`, and costs nothing until someone looks.
- Anything already true of the code — the code and its comments say it better.
- A second copy of a rule that is already stated somewhere above.

**🧹 TRIM IT WHEN IT PASSES ~150 KB.** Move backlog entries older than about two weeks into `docs/history.md`, and lift any durable rule out of them into STANDING RULES first. **Losing that second half is how a trim goes wrong** — the rules are the part that earns its place. The method is written up in `docs/claude-md-trim-2026-09-12.md`.

**📚 WHERE THE OLD DETAIL LIVES:** `docs/history.md` — the full pre-2026-08-29 backlog, the completed AWS migration log, the superseded Bedrock investigation, the SES and Netlify setup records. Nothing was deleted. Grep it.

## What it is
A daily wellness check-in app for physical therapy patients. Patient does a 30-second check-in (feeling 1–5, movement, a note) and gets a warm, AI-written reflection. Their clinic gets dashboards + a weekly summary. **Clinics subscribe; patients use it free** as a value-add.

- **Tagline:** One good day at a time.
- **Live app:** https://glowpt-app.netlify.app
- **Sits under the FranklinAI umbrella** (David Peterson's company; sibling product: ArmCare). GlowPT keeps its own amber/navy sunrise brand in patient-facing surfaces.

## How to work with David (IMPORTANT)
- David is newer to Claude Code and prefers to **go slow and have each step explained in plain, non-technical language before it happens.** Confirm before big/irreversible actions.
- **🧭 SESSION ROOTING (set 2026-09-01): any session that will touch AWS, the database, deploys, or legal work must be STARTED in THIS project folder.** The AWS permission allowlist (`.claude/settings.local.json`), the per-project memory, and this doc's auto-load are all keyed to `~/Downloads/glowpt`; a session rooted in `franklinai-v2` has none of them and hits permission walls the moment it reaches for the bastion or `cdk deploy`. Cross-repo COPY work from either root is fine and established (read the other repo's `CLAUDE.md` first — its "Working across the two repos" section carries the same conditions). **When working cross-repo, say plainly which repo each change lands in, with hashes** — on 2026-09-01 a franklinai-rooted thread legitimately doing glowpt layout work read from the outside as a stray thread running by itself, and untangling that cost a real session. David starts threads from the correct project consistently; this convention is for the sessions, so the root of a thread always matches the risk of its work. The FranklinAI doc records the same rule at V55.
- **Edit files directly, then run `git push` yourself** (confirmed preference — don't hand him the command). Show a clear plain-language summary of what changed.
- **Supabase/SQL steps:** lead with 1–2 plain-English sentences (what it does + reassurance like "nothing gets deleted"), THEN give the SQL block for David to paste & **Run** in the Supabase **SQL Editor** himself. Do NOT just dump a migration file with a terse "paste and run." (He briefly chose "Claude runs SQL via the Chrome extension," but that isn't connected — Chrome + the Claude-in-Chrome extension would be needed; revisit if set up.)
- **Scripts needing the service-role/secret key** (`seed-demo.mjs` / `reset-demo.mjs`) are David's to run locally — he has the Supabase **secret key** (`sb_secret_…`, the new service_role replacement). Never ask him to paste that key into chat/screenshots.
- **Keep the code split** into multiple files (router + `screens/` + `lib/` + `auth.jsx`) — do NOT collapse back into one giant `App.jsx`.
- **Read the current source files before editing them.** David deploys via `git push`; the files on disk are the source of truth.
- Never commit secrets. All `.env*` files are gitignored.
- **Redact secrets in anything David pastes or screenshots back (his explicit ask, 2026-07-17).** Two rules, both on you (Claude):
  1. **Commands you hand him to copy/paste must never print a secret.** Use an obvious placeholder in place of any key — write `Authorization: Bearer [REDACTED]` (use `[REDACTED]` as the standard word, matching claude.ai; `<YOUR-KEY>` only where he genuinely fills it in), or route it through an env var like `PGPASSWORD=… node …` — never a literal key. Nothing sensitive should ever be echoed to his terminal.
  2. **Queries whose output would contain a secret must be written to hide it, so a screenshot he sends back is safe by construction.** The live example: `cron.job.command` holds a plaintext service key, so ask for `select jobid, schedule, jobname from cron.job` (dropping `command`) or a redacted expression, never `select command…`. Read status codes / values back in *words* instead of dumping the raw secret.
  - **Why it matters:** David rotates any key that lands in a screenshot, which is a real, repeated time sink. Design outputs to be screenshot-safe up front. See the Secret-key hygiene note under Status & backlog.

## Tech stack
| Layer | Tool |
|---|---|
| Frontend | React 19 + Vite |
| Routing | react-router-dom |
| Database + Auth | Supabase (passwordless — 6-digit email OTP code) |
| Hosting / deploy | Netlify (auto-deploys on push to `main`) |
| AI reflections | Anthropic Claude (Haiku) via a **Supabase Edge Function** |
| Email | Resend (weekly summaries; scheduled via Supabase Cron/pg_cron) |
| Version control | GitHub — `besoulful-design/glowpt` |

## Key locations
| Thing | Path |
|---|---|
| Router | `src/App.jsx` |
| Entry / providers | `src/main.jsx` |
| Auth + profile context | `src/auth.jsx` |
| Patient app | `src/screens/PatientApp.jsx` |
| Clinic dashboard (manager/therapist) | `src/screens/Dashboard.jsx` |
| Patient join | `src/screens/Join.jsx` · Login `src/screens/Login.jsx` · Onboard `src/screens/Onboard.jsx` |
| Dashboard data logic | `src/lib/clinicData.js` |
| DB migrations | `supabase/migrations/0001_multitenant.sql` · `0002_therapists.sql` (staff invites + caseload RLS) · `0003_discharge.sql` (patient soft-delete + `checkins_update_own`) |
| Edge functions | `supabase/functions/ai-response/` · `supabase/functions/weekly-summary/` |
| Demo seed / reset | `scripts/seed-demo.mjs` · `scripts/reset-demo.mjs` |
| **Legal drafts (attorney review)** | `legal/BAA-draft-for-attorney-review.md` · `legal/Subscription-Agreement-draft-for-attorney-review.md` |
| In-app legal copy | `src/lib/legal.js` (privacy notice + BAA summary + version constants) |
| Env (local, gitignored) | `.env` |

## Architecture (V2)
- **Multi-tenant:** tables `clinics`, `profiles` (role: `patient` | `therapist` | `manager`; patients carry `therapist_id` = assigned PT), `checkins` (keyed on existing `user_id` + `clinic_id`), `consents`, `access_log`, `staff_invites`. Row-Level Security keeps each clinic private.
- **Staff invites (V2.1):** a manager invites a therapist by email (`invite_staff` RPC → `staff_invites` row); the therapist becomes staff on first sign-in (`accept_staff_invite` RPC, called from `loadProfile`) — role is set server-side so a patient can't self-promote. Manager assigns patients to therapists (`assign_therapist` RPC). Migration `supabase/migrations/0002_therapists.sql`.
- **Auth:** Supabase passwordless **6-digit email code** (OTP). Flow = `signInWithOtp` → user types the code into the same tab → `verifyOtp` (shared `CodeVerify` screen used by Login/Join/Onboard). Chosen over magic links because links open in whatever email app, breaking session persistence on phones; the code keeps everything in one tab. **Requires the Supabase "Magic Link" + "Confirm sign up" email templates to include `{{ .Token }}`.** Name/clinic/consent ride in signup **user_metadata** AND a localStorage backup saved by the form. **Gotcha (fixed 2026-07-13):** `signInWithOtp` `data`/user_metadata is only written for a BRAND-NEW email — ignored for an already-existing account. So `loadProfile` also falls back to the localStorage backup (incl. the name) — rely on that, not metadata alone, or a returning email's name/clinic goes blank. "Confirm email" is OFF. CodeVerify checks `getSession()` after `verifyOtp` and forwards into the app on success (an error only shows if truly not signed in).
- **AI:** patient text goes to the `ai-response` Edge Function (keeps PHI inside Supabase — HIPAA-*ready*).
- **Patient app (`PatientApp.jsx`):** welcome → daily check-in → AI reflection → **Progress screen** ("View my progress", built 2026-07-13). Progress is a stack of cards: consecutive-day **streak** (hero) → This-week + all-time counts → **"Your month"** card (avg-mood summary + 4 weekly-average bars showing the arc) → tappable **"This week"** card to read past entries. One 30-day `checkins` query powers week + trend + streak; all-time total via a count query. (Earlier it reused the post-checkin screen + showed 30 anonymous dots — both replaced.)
- **Dashboards:** manager = clinic-wide engagement + care-team management (invite therapists, assign patients); therapist = ONLY their assigned caseload (enforced by RLS, not just UI). Both show streaks, 7-day trend, and flags (Inactive 5+ days / Low-mood 2+ low days). Staff views write an `access_log` entry. **7-day trend shows the same emoji faces the patient taps at check-in** (not colored dots, as of 2026-07-14) — patient/manager/therapist share one language; triage is carried by the Status pills, not color. The 1–5 face + word scale (😔 Really tough → 😄 Feeling great) lives in ONE shared file `src/lib/feelings.js`, imported by both `PatientApp.jsx` and `Dashboard.jsx` so they can't drift. (Patient Progress-screen month bars still color by mood via a local `FEELING_COLOR` in `PatientApp.jsx`.)
- **Weekly emails (PHI-free nudges):** `weekly-summary` Edge Function computes numbers in Supabase and sends via Resend. Patient email = own name + check-in count + link; clinic email = aggregates only, no names. Scheduled: Supabase Cron `weekly_summary`, `0 12 * * 1` (Mon 8am ET).

## Environment variables
- **Netlify (build):** `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY` (= Supabase **publishable** key `sb_publishable_…`). Anthropic keys were removed (AI moved to the Edge Function).
- **Supabase Edge Function secrets:** `ANTHROPIC_API_KEY` (GlowPT workspace key), `RESEND_API_KEY`, `FROM_EMAIL`, `APP_URL`. (`SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` are auto-provided to functions.)
- Supabase project URL: `https://iuefzbsgzsgsvybiurzd.supabase.co`

## Business model
- Clinic is the customer; patients free. **Price = $350/mo. DECIDED 2026-08-24 (David) — one price, no range, no blended figure.** It matches the live marketing site and is written into both legal drafts. The old "~$300 blended, planning range $250–350, reconcile" wording is retired; do not reintroduce it. Year-1 target: **29–40 clinics → $100–140K ARR** ($120K goal).
- Why clinics pay: patients drift off before finishing their plan of care (65–70% industry dropout); GlowPT keeps them engaged between visits → completed care → clinic stays full.
- **Positioning:** GlowPT is NOT an HEP/exercise-program tool (clinics resist those). Its wedge is the daily emotional/adherence check-in no other clinic tool owns. Target buyers: clinic owners + office/practice managers (often on Instagram, not LinkedIn).

## HIPAA guardrail (do not violate)
Patient check-ins are PHI. **Build and demo with DEMO DATA ONLY until a paying/committed clinic + signed BAAs.** Go-live flips (gated on first paying clinic): Supabase Team plan (~$599/mo) + Supabase BAA + Anthropic BAA + **Resend BAA (flagged 2026-07-15 — was missing from this list)**. Why Resend counts: the weekly *patient* email carries their name + email + check-in count, which identifies a person AND reveals they're a PT patient → individually identifiable health info that Resend transmits → business associate. (The *clinic* email is aggregates-only and genuinely PHI-free.) **Verify Resend can sign a BAA and on which plan BEFORE go-live** — if it can't, the email vendor changes. **ANSWERED 2026-07-17: Resend has no HIPAA/BAA option → the whole stack is moving to AWS (email becomes SES). The go-live infra path is now the AWS migration (RDS + Cognito + Lambda + SES under one AWS BAA, plus the Anthropic BAA), NOT Supabase Team + Resend. See ⭐⭐ CURRENT INITIATIVE above.** The demo-data-only rule in this guardrail is unchanged and still governs until a paying clinic + signed BAAs. Netlify only serves the static frontend (PHI goes browser→Supabase directly, never through Netlify), so it's not treated as a business associate. Also needs the `access_log` (done) before a real clinic dashboard. David + friends testing on the sandbox = demo data, not real PHI — that's fine.

## Demo vs sandbox convention
- **Riverside PT** = the clean **sales demo** clinic. **✅ REBUILT ON AWS 2026-08-22** via `scripts/aws-seed-demo.mjs` (the AWS replacement for the Supabase `seed-demo.mjs`/`reset-demo.mjs`, which are now obsolete). David = manager via **`besoulful@gmail.com`**; therapist Sam Torres = `besoulful+samtorres`; **6 patients** all on `besoulful+` aliases: showcase **Grace Bennett** = `besoulful+grace@gmail.com` (~25 check-ins/30 days, 14-day streak, trending up — log in AS her for the patient **Progress screen**), plus Chris Alvarez (engaged), Maria Chen (mixed), **James Okafor** (inactive 8d → flag), **Linda Park** (low-mood → flag), Robert Ellis (sporadic). Each has a REAL Cognito account, so David can sign in as any of them (OTP lands in his inbox). Never share its `/join/riverside-pt` link. **Re-pristine before a demo:** start the bastion + SSM tunnel (localhost:5433), `export PGPASSWORD=<admin secret>`, then `node scripts/aws-seed-demo.mjs` (self-resetting; needs `AWS_PROFILE=glowpt-prod`). Requires `pg` (in root devDependencies).
- **Sandbox = "Ridge PT"** (created via `/onboard`, manager `dwpeterson15@gmail.com`) = for David + friends to dogfood. Test logins (Gmail `+aliases` → all land in his inbox): therapist `dwpeterson15+therapist1@gmail.com` ("Dr. Sam"), patients `dwpeterson15+patient1@gmail.com` (Tim Long, assigned to Dr. Sam), "Charlie" (`+charlie@gmail.com`), "Davey" (`+test@gmail.com`, re-joined properly via `/join/ridge-pt` 2026-07-15, assigned to Dr. Sam). Mess is fine — but **make new test patients via the clinic's `/join/<slug>` link, NOT by typing an email into `/login`**, or they land with no clinic (see V2.4). Deleted 2026-07-15 as orphans: `+timmy@gmail.com` ("Timmy"), `+test2@gmail.com`.
- **Login-code emails use Supabase's built-in rate-limited mailer (~few/hr)** — heavy same-day testing can exhaust it; codes then don't arrive (check spam, wait, or do the custom-SMTP work).

## ⭐⭐ CURRENT INITIATIVE (2026-07-17): migrating off Supabase + Resend to AWS

**📍 STATUS AT A GLANCE (2026-08-22):**
- **✅✅✅ CUTOVER DONE 2026-08-22 — glowpt.app NOW RUNS ON AWS.** The whole stack (Cognito auth + API Gateway/Lambda data layer + RLS + SES email) is live in production; `@supabase/supabase-js` is out of the running app. Verified live: glowpt.app resolved `pc-test-clinic-2` (an AWS-only clinic) with zero console errors. All 6 phases complete. **The migration off Supabase + Resend onto AWS is DONE.**
- **Supabase stays live as the ROLLBACK** ($25/mo) — do NOT tear it down until the new stack is fully exercised. Reverting the `main` merge (commit `2b04f91`) + push switches glowpt.app back to Supabase.
- **✅✅ WEEKLY-SUMMARY ON AWS — DONE + PROVEN 2026-08-22.** The last real feature is live: a VPC Lambda `glowpt-weekly-summary` reads a cross-clinic SECURITY DEFINER function (`weekly_summary_rows()`, callable ONLY by the dedicated `glowpt_weekly` role), sends PHI-minimised nudge emails via SES through an interface VPC endpoint (SES API, `InterfaceVpcEndpointAwsService.EMAIL`, NOT SMTP), and fires from an **EventBridge rule `cron(0 12 ? * MON *)` = Monday 8am EDT** (already live). **Batch/throttled + FAIL-LOUD (`sent === queued`, throws on any drop).** PROVEN end-to-end 2026-08-22: seeded a clean `Weekly Test PT` (manager + 3 patients, backdated check-ins), dryRun `{queued:4,sent:0}`, real `{queued:4,sent:4}`, and **all 4 emails LANDED in David's inbox** (inbox not spam, correct per-recipient copy + 67% clinic summary). Files: `infra/lib/weekly-summary.ts`, `infra/lambda/weekly-summary/index.ts`, `infra/lib/database.ts` (weeklySecret `glowpt/db/weekly`), `db/schema.sql` + `db/patches/2026-08-22_weekly_summary.sql`. Old Supabase `weekly_summary` pg_cron confirmed OFF (cron.job empty). **⚠️ Open items — ALL BUT THE LEGAL GATE CLOSED 2026-08-23:** Riverside demo rebuilt ✅ (2026-08-22); Cognito `deletionProtection` re-enabled ✅; leftover test users + the orphaned pool deleted ✅; `Weekly Test PT` **deliberately kept as a Monday heartbeat** ✅ (⚠️ **HISTORICAL — that clinic was DELETED 2026-08-31 and Riverside PT is the heartbeat now; see the heartbeat entry in the NEXT SESSION block**); patient privacy notice shipped ✅. Still open: the `+p5test` bare row (trivial, blocked on the permissions note), and **the Anthropic BAA + an attorney-reviewed clinic BAA, which gate REAL patients** (demo-data-only until both are done). (pc-test-clinic-2 + Cognito `+pc2`/`+patient1` DB rows cleaned up 2026-08-22 during the weekly seed; the Cognito *users* `+pc2`/`+patient1` may still exist in the pool.)
- **DONE + PROVEN ON AWS:** Phase 0 (foundation: VPC, encrypted Multi-AZ RDS Postgres 17.6, RDS Proxy, CloudTrail, SES TLS config set) and Phase 1 (schema: applied to the real `glowpt` DB, all 7 tables FORCE RLS, **14/14 security tests pass on RDS 17.6**, BYPASSRLS caveat resolved). These were the two hardest, highest-risk phases.
- **✅✅ PHASE 2 DONE + PROVEN END-TO-END 2026-08-18.** Deployed to GlowptFoundation (UPDATE_COMPLETE) AND the full acceptance test PASSED: a real CLI `onboard` sign-up (`besoulful+pc2@gmail.com`) → Cognito → **SES email from `no-reply@glowpt.app` landed in inbox** (first real AWS email) → ConfirmSignUp → post-confirmation Lambda logged `post-confirm ok` → **created `pc-test-clinic-2` + manager profile in RDS** (verified by query). Whole chain works: Cognito passwordless (no password needed) → SES → Lambda → IAM auth to proxy → tested RPCs. **Live IDs:** UserPool `us-east-1_q2W6uXTZE`, App client `6upb217er13tibbke4qbalhbji`, Lambda `glowpt-post-confirmation`, postconfirm secret `...secret:glowpt/db/postconfirm-vKUNnC`. **DB side applied to RDS:** `glowpt_postconfirm` role created (patch `db/patches/2026-08-18_postconfirm_role.sql`) + password set to match secret. **3 bugs caught + fixed live:** (1) `disableOAuth` (CFN rejected empty-flows client), (2) pool `deletionProtection:false` for bring-up (rollback orphaned a protected pool), (3) Lambda `statement_timeout` → `query_timeout` (RDS Proxy rejects the server-side option). **Small Phase-2 follow-ups (not blocking):** re-enable pool `deletionProtection:true` once stable (TODO in `auth.ts`); clean up test data (Cognito users `+pc1`/`+pc2`, clinic `pc-test-clinic-2`, and `+pc1` = confirmed-but-no-clinic from the pre-fix failure); delete the orphaned user pool from the 1st failed deploy's DELETE_SKIP.
- **✅✅ PHASE 3 DONE + PROVEN END-TO-END 2026-08-22.** The data API is LIVE at **`https://byaepos5vl.execute-api.us-east-1.amazonaws.com`**. Shape: ONE Lambda **`glowpt-api`** behind an **HTTP API (API Gateway v2)** with a built-in **Cognito JWT authorizer** (`HttpUserPoolAuthorizer`, validates against pool `us-east-1_q2W6uXTZE` + client `6upb217er13tibbke4qbalhbji`). 17 routes = 16 JWT-protected + **1 public** (`GET /clinics/by-slug/{slug}`, the only unauth read, → `get_clinic_by_slug`). Every authed handler opens a txn, `set_config('app.user_id', <verified sub>, true)`, runs the query, commits — **RLS is the boundary, the authorizer only authenticates**. `glowpt_app` now has a real credential (secret **`glowpt/db/app`**, registered with the proxy) + IAM auth on the proxy like postconfirm; its RDS password was set out-of-band via the bastion `ALTER ROLE` (was a throwaway before). **CORS scoped to `glowpt.app`/`www`/`netlify` — never `*`.** Deploy: one shot, 347s, no retries. **PROVEN:** public route → real clinic (200); unknown slug (404); no token (401); tampered token (401, signature verified — no forged sub); real signed-in `GET /me` → RLS-scoped to the token's sub (manager of pc-test-clinic-2); `GET /clinic` own-clinic; `GET /clinic/roster` manager view + `access_log` `view_roster` row written **in the same txn** (confirmed in DB). **Files:** handler `infra/lambda/api/index.ts` (route→SQL map), CDK `infra/lib/api.ts`, + `database.ts` (appSecret), `infra-stack.ts` (wire Api), `test/infra.test.ts` (Phase-3 assertions, 2/2 pass). **⚠️ Phase 5 frontend MUST send the Cognito ID token** (aud = client id), NOT the access token — the JWT authorizer validates `aud`. **Parked Phase-3 edge (unchanged):** a total post-confirm miss (no `users` row) can't self-heal via `glowpt_app` (no `register_user` grant) — `GET /me` returns 404 `profile_missing` so the frontend NoClinic gate drives re-attach; a full fix (API can ensure the identity row) is a small follow-up, not blocking.
- **✅✅ PHASE 4 COMPLETE 2026-08-22 — ai-response AND weekly-summary BOTH DONE + PROVEN.** (weekly-summary was the sanctioned slip; it is now built, deployed, and proven with real emails landing in David's inbox — see the top mission bullet + the "▶ PICK UP HERE" block, both marked DONE. The "weekly-summary NOT built" text later in THIS bullet is now historical.) The AI reflection now runs on AWS: a SEPARATE Lambda **`glowpt-ai-response`** (deliberately **NOT in the VPC** — it needs the internet for Anthropic and touches no DB, so it gets free egress, no NAT/VPC-endpoint) attached to the SAME HTTP API at **`POST /ai-response`** behind the SAME Cognito authorizer. That authorizer IS the fix for the old function (Supabase's gateway `verify_jwt` gave it auth for free; on AWS it had none, so it was open to anyone with the Anthropic key). Anthropic API key lives in **Secrets Manager `glowpt/anthropic/api-key`** (David created a fresh key `glowpt-aws` in the Default Anthropic workspace, Expires Never, and set the value in the console — never in repo/env/chat; Lambda fetches + caches it). **PROVEN:** no-token → 401; real signed-in call → a genuine warm reflection from Anthropic (200, referenced the check-in details, not the fallback). Files: handler `infra/lambda/ai-response/index.ts`, CDK `infra/lib/ai-response.ts` (+ `api.ts` exposes the authorizer, `infra-stack.ts` wires it). **⚠️ Anthropic BAA is a go-live gate** (PHI in the prompt; demo-data-only until signed). **weekly-summary NOT built** — the migration plan calls it the deferrable phase (runs Mondays); do it in a focused follow-up: rewrite for **SES + EventBridge** `cron(0 12 ? * MON *)`, **scope queries PER CLINIC** (today one JS filter separates all clinics under the service role — a leak risk), batch-send + fail-loud (`sent === queued`), drop the pg_cron row + plaintext key, emails now come from `public.users` (no `auth.admin.listUsers`). weekly-summary needs DB (in-VPC) + SES egress (SES interface VPC endpoint, ~$7/mo, or NAT) — decide then. At cutover, switch OFF the old Supabase/Resend weekly cron so no patient PHI keeps flowing through Resend.
- **✅✅ PHASE 5 (frontend) DONE + PROVEN 2026-08-22 (on branch `phase5-frontend-aws`, NOT merged — merging to main IS the cutover).** `@supabase/supabase-js` is OUT of the code; the whole React app runs on AWS. Auth = Cognito passwordless email OTP from the browser (`src/lib/cognito.js`, `@aws-sdk/client-cognito-identity-provider`): returning user = InitiateAuth(USER_AUTH,EMAIL_OTP)→RespondToAuthChallenge; new user = SignUp(no password)→ConfirmSignUp(code + clientMetadata the post-confirm Lambda reads to attach the clinic)→the returned session **auto-signs-in, no second code**. Tokens in localStorage, ID token sent to the API. Data = fetch against the API (`src/lib/api.js`); `auth.jsx` loads the profile from `GET /me` + runs the idempotent re-attach net; the roster's access_log write is now server-side; the check-in save is one `POST /me/checkins` (server derives ids + one-per-day upsert). `src/config.js` holds the public API URL + Cognito client id + region. Deleted `src/supabase.js`. **PROVEN live in the running app (both roles):** manager sign-in→dashboard renders clinic+roster from AWS; new patient join→auto-login→post-confirm attaches them→check-in→real Anthropic reflection→save→week-count updates. Build clean, zero console errors. **Deploy note for cutover:** `config.js` has the live AWS values hardcoded as defaults, so Netlify needs no new env vars; the old `VITE_SUPABASE_*` are simply unused. API CORS allows glowpt.app/www/netlify (+ localhost:5173 for dev).
- **✅✅ PHASE 6 CUTOVER DONE 2026-08-22.** Merged `phase5-frontend-aws`→main (ff to `2b04f91`) + pushed → Netlify built & deployed → glowpt.app serves the AWS bundle (`index-BU5NcLEV.js`) and its public API call resolves an AWS-only clinic. Also removed the empty `functions` dir from `netlify.toml` (keeps Netlify a non-BA). SPA deep links safe (`public/_redirects`). Follow-ups are the IMMEDIATE list up top.
- **Email on AWS = SES** (Amazon Simple Email Service; replaces Resend). Set up + out of sandbox; config set `glowpt-transactional` (TLS Require). Sends NOTHING until Phase 2 (login codes) and Phase 4 (weekly summaries) wire it.

*(Full detail in the NEXT SESSION block and Progress section below.)*

**Decided, not under discussion.** Resend has **no** HIPAA/BAA option (that's what kicked this off), and Supabase Team + its HIPAA add-on runs ~$950/mo, which doesn't clear GlowPT's own cost until the 4th clinic. AWS is ~$60–120/mo with **one free, self-serve BAA** covering RDS + Cognito + Lambda + SES. The cheap window is open **only because no real PHI exists yet** (demo clinics only) — it closes the moment a real patient logs in. Target: real patients in ~2 weeks (David's own date, promised to nobody, movable).

**Who does what:** **claude.ai = architect** (wrote the planning docs, lives in the web chat, no repo access). **Claude Code (me) = builder** (executes here). **Planning is done; from here it's David + Claude Code.** Only re-engage claude.ai if David wants a second architect opinion on a big call.

> ## 🧭 NEW THREAD? READ THIS FIRST (state as of Saturday 2026-09-12, 08:30; detail for every session is under Status & backlog, newest first)
>
> ## ✅ THE CHECK-IN DAY BUG IS FIXED, SHIPPED AND VERIFIED ON PRODUCTION (2026-09-12, 07:20 reported → 08:20 live). NOTHING IS OUTSTANDING FROM IT.
>
> **David: *"I just checked in Charlie. it says he has a 1 day streak. But he checked in yesterday too."*** He was right, it was **data loss rather than a wrong number**, and it is now fixed end to end.
>
> **THE CAUSE.** The save keyed the day off `created_at` in **UTC**; every screen bucketed by **local** day. In Eastern the UTC day rolls over at **8pm**, so a check-in made after 8pm was filed under the NEXT UTC day, and the next morning's check-in found that slot taken and **overwrote it** — one row, not two, with the evening's feeling, note and reflection replaced. `created_at` is not in the upsert's update list, so the surviving row kept the OLD timestamp with the NEW content, which is why the streak read short.
>
> **CONFIRMED AGAINST CHARLIE'S REAL ROWS, not inferred.** He checked in Friday 14:12 AND Friday 20:14. The 20:14 row now carried **"I got up at 3am"** plus a reflection about a 3am wake-up — unmistakably **Saturday morning's** check-in wearing Friday's timestamp. **Friday evening's content is gone; that happened before any fix and is not recoverable.**
>
> **THE FIX (David chose it: "let's go with A"). `checkins.local_date` — the calendar day the patient's OWN DEVICE was in when they saved.** That is now the day key everywhere: the unique index, the streak, the week and 30-day grids, the Last Check-In column, and the weekly email's count. **Midnight to midnight, the patient's time zone.**
> - **⛔ DO NOT DERIVE A DAY FROM `created_at` AGAIN, anywhere.** `src/lib/localDay.js` is the ONE frontend definition; `local_date` is the one database one. Comments in `db/schema.sql`, `clinicData.js`, `PatientApp.jsx` and the Lambda all say so.
> - **It fixed a second bug nobody had hit yet:** the roster bucketed days by the **STAFF member's** browser, so a therapist in another time zone would have seen their patient's days shifted.
> - **The destructive half of the overwrite is gone BY CONSTRUCTION.** A check-in can now only ever replace one from the same local day, which is the deliberate 2026-07-15 same-day-re-entry behavior. Overwriting *yesterday's* is impossible, not merely guarded.
>
> **⚠️ TWO TRAPS CAUGHT IN TESTING THAT WOULD HAVE SHIPPED, AND BOTH ARE REUSABLE:**
> 1. **`node-postgres` parses a `date` column into a JS `Date`**, which JSON-serialises to `"2026-09-11T00:00:00.000Z"`. Every day comparison would have failed and **every screen would have rendered as though the patient had never checked in** — silent and total. Both reads therefore use **`to_char(local_date,'YYYY-MM-DD')`**, and `to_char` rather than `::text` because **`::text` renders through the session's `DateStyle`** (proved: under `SQL, MDY` it returns `09/11/2026`). Production happens to be `ISO, MDY`, so `::text` would have worked — which is exactly the kind of thing that is true until it isn't. **Do not simplify either half.**
> 2. **Patch 2 was not re-runnable:** it renamed the new index INTO the old one's name, so a second `drop index if exists <old name>` would have **deleted the index it had just installed**. It now drops by index DEFINITION (matching `utc_date`), not by name.
>
> **▶ THE DEPLOY WAS FOUR STEPS AND THE ORDER IS THE POINT — frontend → patch 1 → API → patch 2.** Patch 1 adds the column and the new index but **deliberately leaves the old one**, so the still-deployed API keeps working; patch 2 cuts over after the new API lands. **There was no window in which a patient could not check in.** Both patches are in `db/patches/2026-09-12_local_date*.sql`.
>
> **🔧 PATCH 1 ALSO REPAIRED THE ALREADY-OVERWRITTEN ROW, by a principled rule rather than a hand-edit:** where the Eastern backfill collides, the LATER row takes its UTC date, because that is the day its surviving content actually came from. **Exactly one row qualified (Charlie's), and Felix's 22:09 Friday entry correctly stayed on Friday** because it was never overwritten. Verified row by row on production.
>
> **✅ WHAT WAS OBSERVED, not assumed:** schema suite **69 → 71** (T13b/T13c are the regression test, and T13c asserts the two rows share one UTC day so the old key really did collapse them) · the old schema rebuilt and the collision reproduced on it · both patches rehearsed against a **pre-patch** database seeded with production's shape, both idempotent, and the result **diffed byte-identical against a fresh build of `schema.sql`** including function owner and ACL · `cdk diff` exactly one Lambda, **no replacements, bastion absent so the AMI pin held** · deploy `UPDATE_COMPLETE` 68s, `BastionInstanceId` unchanged · **the deployed Lambda's code pulled back from AWS** carrying the new conflict target and both casts · the live bundle **byte-identical to the build that was tested** · **the bug replayed on production inside a rolled-back transaction**: two check-ins both falling on UTC day 2026-10-02 survive as two rows · the **real `buildRoster` against live production data: Charlie streak 2, "Today"**, which is the number David expected · weekly dry run `queued:16`.
>
> **📅 SUNDAY'S EMAIL IS HEALTHY.** `weekly_summary_rows()` now counts distinct `local_date`; it had counted UTC days, the same axis as the bug, which **over**-counted anyone whose evening check-in spilled into the next UTC date (Charlie read 3 days for 2). **Note the doc previously predicted UNDER-counting; the real effect was over-counting.** Volume 16.
>
> **🪤 AND ONE TEST-HARNESS FINDING WORTH KEEPING: `db/tests/run_tests.sh` PIPES EACH SUITE THROUGH `grep -E "PASS:|FAIL:" || true`, so a suite that ERRORS prints NOTHING AT ALL and the run looks merely short.** `rls_tests.sql` died on a not-null violation and the output simply had no T1-T23 in it. **If a test count looks low, the suite errored — do not read a short run as a passing one.** Worth fixing properly some time.
>
> **⏰ CHECK THE CLOCK BEFORE YOU SAY ANYTHING ABOUT TIME. `date` — do not infer it from this doc or from a commit you half-remember.** **DAVID'S DAY STARTS AT 3AM: 3 to 6am is his MORNING, not a late night, and telling him to "get some rest" at 6am is getting it exactly backwards.** This doc has already recorded one session mis-stating elapsed time by reasoning from its own "end of session" wording; do not add a second.
>
> **▶ NOTHING IS BROKEN AND NOTHING IS OUTSTANDING IN CODE.** The two regressions I shipped on the night of 09-11 were both found by David on his phone the next morning and both are fixed, deployed and verified live (detail under Status & backlog). **He signed off: *"Leave the tap dimming, I like it consistent. i'll let you know if i find anymore surprises. good work. thanks."*** **Open with what he wants to do next, not with a list of what he owes.**
>
> **✅ RIVERSIDE WAS RE-PRISTINED 2026-09-12 05:00 AND SUNDAY'S HEARTBEAT IS READY. DO NOT RUN THE SEED AGAIN FOR THIS FIRE.**
> **Measured against the real window** (the 7 days ending Sun 09-13 22:00 UTC): **before, 1 of 6 Riverside patients had checked in; after, 5 of 6 with 21 check-ins and the newest today.** So Sunday reports roughly **83% with one needing attention** — James Okafor, who is the seeded inactive patient, which is the demo story working as designed.
> - **⚠️ DAVID MUST SIGN IN AGAIN AS `besoulful@gmail.com`.** Every Riverside account was destroyed and recreated with a fresh Cognito sub, so his stored tokens are dead. **Not a bug.** RidgePT was untouched and its real people keep their logins.
> - **❌ MY "IT WILL REPORT 0%" PREDICTION WAS WRONG, and the correction is worth keeping.** It was **1 of 6, not 0 of 6** — James Okafor had a check-in on 09-08, which is **David testing the weekly-email link as James during the 09-08 session.** The reasoning (seed timestamps sit before the window opens) was sound about the SEEDED data and forgot that a human had used the clinic since. **A demo clinic is not inert between seeds.**
> - **🤚 AND THE FIRST VERIFICATION QUERY WAS WRONG IN A WAY THAT LOOKED FINE.** Joining `profiles` AND `checkins` to `clinics` in one query is a **cartesian product**, so every `count(checkins)` came back multiplied by the patient count (Riverside read 8, RidgePT 120). **The `count(distinct ...)` columns were unaffected, which is exactly why it passed a glance.** Use scalar subqueries per clinic, not two sibling joins.
> - **RidgePT read 5 of 5 both before and after** — its five real patients are genuinely using it. Nothing to fix there, and nothing should be.
> - **✅ VERIFIED BY INDEPENDENT QUERY, not by the script's output:** users **16** unchanged · orphan profiles **0** · platform admin **1 and it is `besoulful@gmail.com`** · clinics **2**, both open · feelings within the 1-5 constraint · **RidgePT's `activated_by` resolves to David's NEW user row** (the 09-06 trap, handled — the script logged `preserving activated_by: ridgept <- besoulful@gmail.com` and restored it) · all **16 Cognito accounts CONFIRMED and matching the DB count** · **every Cognito `sub` equals its `users.id`.** Bastion stopped, tunnel closed.
>   - **⚠️ ONE COMPARISON THAT LOOKS LIKE A FAILURE AND IS NOT:** a byte-exact diff of Cognito against `users` flags `besoulful+RidgePTmanager@gmail.com` vs `besoulful+ridgeptmanager@gmail.com`. **The ids are identical; only the email letter-case differs, and `users.email` is `citext`.** Harmless, pre-existing, and it will show up every time someone runs that check.
>
> **🗓️ TWO MORE DATES COMING UP:** the **AWS Activate decision is expected 2026-09-15 to 09-17** by email to `david@franklinaisolutions.com` (check the Credit Application Status page on startups.aws.com before asking him) — **if it lands, that is the moment to reopen the Bedrock case citing Activate membership.** And **after 2026-09-18, delete the `/staff/:token` route alias** in `src/App.jsx`; it exists only because the first staff invite links pointed there and they live 14 days.
>
> **🧪 HE IS STILL TESTING, AND HE IS THE REASON BOTH 09-11 REGRESSIONS WERE CAUGHT WITHIN HOURS. His last words: *"i'll let you know if i find anymore surprises."*** **Take every report seriously and reproduce it before theorising** — on 09-12 he reported "the logo is at the top edge again" and "scroll-lock seems to be turned off", and **both were real, both were mine, and neither was visible to any tool in this harness.** **Expect reports, and expect them out of order** — sometimes about a screen changed twenty minutes earlier, sometimes about one changed yesterday. **Run `git log --date=format-local:'%a %H:%M'` and check the deploy time against the report before theorising**; that has already caught two false alarms. **He works from an iPhone and a desktop Safari, and screenshots both.**
>
> **✅ WHAT DAVID HAS NOW WALKED HIMSELF ON THE LIVE SITE. Do not re-open any of these as "not yet tested".**
> - **🆕 2026-09-12 08:45, THE DAY-BOUNDARY FIX, as Charlie on glowpt.app.** He checked in and screenshotted the result: **"2 day streak 🔥"**, This Week **3** (Mon, Fri, Sat lit), All Time **6**, Your Month 4.0 over 6 check-ins. **That is the bug he reported at 07:20 the same morning, proven fixed by a person rather than by a query** — and the welcome screen, reflection screen, week dots and progress screen all agree on which days he checked in.
> - **Archive / Restore / Undo**, and **Remove** — records AND the Cognito login, **verified gone from the pool**.
> - **Cancel a pending invite**, twice (Tricky Dicky, then Tiny Teflon).
> - **The required mood**: he tried to save a check-in with no face, was refused, and was bounced up to the scale with the prompt. **First human proof of the 2026-09-05 three-layer fix.**
> - Both invite flows end to end (2026-09-04), and the activation gate (2026-09-04).
> - **🆕 2026-09-06 13:20, the whole patient lifecycle in one pass, as "Tommy Olsen" on RidgePT:** invited by email, joined, checked in, **then reached by the Copy Message sign-in link** ("copied the message and sent tommy the link which worked"), **then the spent invite link reopened and correctly offering Sign In.** So the "Patient Sign-In Link" card and the used-invite screen are both proven by a real person, not merely deployed.
> - **The same screenshot also proves this morning's roster changes on live data:** Tommy's row renders **😊 ○ ○** — newest check-in on the LEFT, his two missing ones padded on the RIGHT as visible rings. That is the newest-first reorder, the padding flip that had to move with it, and the 1.41 → 3.80 contrast fix, all correct at once.
>
> **✅ NOTHING IS UNWALKED ANY MORE. The list is empty as of 2026-09-06 14:00**, when David texted a real invite from his iPhone and tapped through it. Every feature in the product has now been driven by a person on the live site.
> - **🆕 2026-09-06 evening: THE FIRST PERSON WHO IS NOT DAVID JOINED ON HIS OWN EMAIL.** Felix (David's husband) accepted a real RidgePT invite as "Miracle Peterson", checked in, and *"said it worked well too."* Then "Danny Hughes" (`besoulful+ridgeptdanny`) was invited and joined. **RidgePT roster: Tommy Olsen, Miracle Peterson, Danny Hughes, Charlie, Timmy, all under PT Pete.**
> - **⚠️ EXPECTED MONDAY EMAIL VOLUME IS NOW 15**: Riverside 8, RidgePT 7 (5 patients + David as manager + PT Pete). **One of those, Miracle Peterson, goes to Felix's real address, not a `besoulful+` alias.** Do not "fix" the count in code.
>
> **✅ RE-DECIDED 2026-09-06 EVENING — EVERY PATIENT NAME IS STACKED (first name over the rest), EVERY FLAG PILL IS ONE FIXED 60×20 SIZE, AND THE COLUMN MEASURES ITSELF: 66px floor, grows to the widest word on the roster, 140 ceiling.** David re-opened it himself after "Miracle Peterson" wrapped at 104 while every short name carried air: *"stack the first and last names, so that you can make the patient column smaller ... let's be consistent and stack them all now."* The width is `patientColumnWidth()`, the widest WORD on the roster measured at render (66 on both clinics today, where "Peterson" is 63); the pills are fixed at 60 so they never set it. **A longer name grows the column; past 140 it wraps; nothing is ever clipped or spills.** **Still true: do not try to make the column auto-size; every row is its own grid, so `fit-content()` resolves PER ROW and the columns stop lining up.** The sideways-scroll floor is now COMPUTED from the columns (`rosterFloor`), so it can no longer go stale.
>
> **🧰 RUN `npx jest` IN `infra/` BEFORE EVERY DEPLOY, BESIDE `cdk diff`.** Eight seconds, and it caught a stale suite on two consecutive days: two weeks stale on 09-05, then broken again on 09-06 by a deploy that had itself succeeded.
>
> **WHAT THE 09-05 / 09-06 SESSIONS WERE, shortest useful version. Detail for each is under Status & backlog, newest first.**
> 1. **09-05 morning** — an invited patient could not get in. **Six separate faults, five pre-existing.** Felix is in. Walk-in sign-up and the QR were removed entirely.
> 2. **09-05 afternoon** — the `#root` `text-align` trap, the roster rebuilt from ONE column declaration, cancel-invite and remove-patient built.
> 3. **09-05 evening** — **one check-in stored with `feeling = 0` blanked the staff dashboard for both roles.** Fixed in three layers; the mood became the one required answer; **the app got its first error boundary**.
> 4. **09-06 early** — **Discharge became Archive**, removal now works for a patient with check-ins and **actually deletes their Cognito login**, and a new patient invite finally appears in the pending list without a reload.
> 5. **09-06 07:00-07:50** — the mood legend, a **permanent Copy Link** on every pending invite, and the dead end that met a patient returning to a used invite link. Infra suite found red again.
> 6. **09-06 08:00-09:05** — the sign-in link became **one card** under the invite form (it shipped on the roster row first and was removed within the hour), and the roster columns were re-measured.
> 7. **09-06 10:15-10:50** — the legend became a **real copy of the check-in scale**, "3-Day Trend" became **"Last 3 Check-Ins"**, the last-3 now reads **newest first**, and the empty slot became a **visible ring**.
> 8. **09-06 17:00-19:15, one long session** — the mood scale became **ONE shared component** (`FeelingScale.jsx`) after three cuts, the last of which finally fixed the thing David had pointed at from the start, **the faces zigzagging across the five cards**; every roster name is **stacked** and the Patient column **measures itself** to the widest word (66 floor, 140 ceiling); the flag pills are **one fixed 60×20 size** and were secretly 34px tall; the scroll floor is **computed**; the roster **sorts by first name**; the invite email lost its one British idiom; and **American English became a memory rule**. Felix and Danny joined RidgePT for real.
>
> **🚨 THE FINDINGS MOST LIKELY TO SAVE THE NEXT SESSION TIME:**
> - **🙂 THE 1–5 SCALE IS ONE COMPONENT, `src/screens/FeelingScale.jsx`, AND EVERY SIZE LIVES THERE.** The patient check-in and the manager legend both render it. **Do not redraw the cells in either screen.** Three times in two days David asked why the two looked different, and the honest answer was that they were two copies. This doc has now recorded FIVE duplicated-declaration drifts (favicon, LogoMark, wordmark, app bar, and this); the rule is one declaration, always.
> - **⛔ "NOT LINED UP" IS A MEASUREMENT, NOT A MOOD. READ THE COORDINATES OFF HIS SCREENSHOT BEFORE TOUCHING A SIZE.** The scale's faces zigzagged high/low/high/low/high for two whole cuts while I adjusted font sizes. The top of face 1 against the top of face 2 would have shown it in ten seconds.
> - **⛔ "MAKE X LOOK AS NICE AS Y" MEANS Y IS THE BASELINE.** The first cut of the shared scale averaged the two screens together and produced the cramped one on both. Any tweak he adds in the same breath ("but the number smaller") is applied INSIDE Y, not used as licence to redesign both.
> - **AN OPTION THAT IS DELIBERATELY WITHHELD MUST SAY WHY.** David reported the remove feature "did not exist" when it was working exactly as designed and merely hidden. **A silent absence is indistinguishable from a missing feature.**
> - **WHEN A REQUEST GROWS RULES AROUND IT, RE-READ THE ORIGINAL SENTENCE.** All he ever wanted was to clear the roster; it grew retention law and deletion semantics and the small thing got buried.
> - **⛔ "MAKE IT LINE UP WITH X" MEANS LOOK LIKE X.** The legend was rebuilt to match the check-in scale's ARRANGEMENT and he said flatly *"The legend didn't change!!!!"* — it had, and his own screenshot showed it, but copying the layout and none of the appearance reads as no change at all. **Copy the visual treatment, not just the geometry.**
> - **🔴 A GLYPH TYPED INTO CHAT IS NOT THE THING ON SCREEN.** I wrote `●` to describe a trend row and his client rendered it as a **red** dot, so he went hunting for a red dot that does not exist (the app's is a CSS circle, no character involved). **This doc records five ways a harness misled me about the product; the CONVERSATION is a rendering surface too.** Name the element or screenshot it; do not reach for a lookalike character.
> - **MEASURE, DO NOT EYEBALL.** Two of today's fixes were only correct because something was measured: the legend's height was unchanged at phone width when it looked halved, and the "no check-in" dot scored a contrast ratio of **1.41**, which is why David had never seen it in three months.
>
> **⛔ DO NOT CHASE HIM ON THE ATTORNEY.** He has it in hand and has said so. Answer it if he raises it; repeating it is noise.
>
> **⛔ DO NOT TREAT THE "~2 WEEKS TO REAL PATIENTS" DATE AS BINDING.** It was always his own, promised to nobody, and he has said he will move it. Never use it to add pressure.
>
> **⏳ WHAT NEEDS A PERSON, NOT CODE. THIS IS THE WHOLE LIST.**
> - **Bedrock. ⚠️ CHANGED 2026-09-07: THE CASE IS CLOSED AND THE ANSWER WAS NO.** AWS resolved `178761116000010` at 03:21 EDT saying the Bedrock team expedites only **Activate members or accounts with an Account Manager**; everyone else waits for a "standard rollout" with no date. **David applied to AWS Activate (Founders tier) the same morning, linked to glowpt-prod; expect the decision 2026-09-15 to 09-17 by email to `david@franklinaisolutions.com`, and check the Credit Application Status page on startups.aws.com.** **When approved: reopen the Bedrock case (or file a new Account case) citing Activate membership and quoting AWS's own email.** Still true: do NOT file Service Quotas requests, do NOT suggest Developer Support, a single free `converse` retry at session start is fine. **Claude cannot read the case** — `aws support` returns `SubscriptionRequiredException` on Basic, so it is console-only. **A second free lever, NOT yet used: click the "If NO" link in AWS's resolution email, or post a correspondence, quoting Omkar's 2026-09-03 commitment and asking what the review team decided.** Hold it until the Activate outcome unless David wants both in flight.
> - **The attorney review.** He has it. See above: do not chase.
>   - **🆕 ONE QUESTION TO ADD TO THAT ENGAGEMENT (cheap, and it settles a whole area): IS A GLOWPT CHECK-IN PART OF THE CLINIC'S DESIGNATED RECORD SET?** If it is a wellness adjunct nobody treats from, deletion on request is easy. But the product's own pitch is that therapists read the roster and act on it, which argues it IS — and then state retention rules bite and a clinic may be unable to delete at all. **Relevant now that managers can permanently remove a patient.**
>
> **💲 COST NOTE: a Cognito interface VPC endpoint was added 2026-09-06, ~$7/mo**, which is what makes "Remove" delete the person's login rather than only their rows. Running cost is now roughly **$95/mo against the $150 budget alarm**.
>
> **🌄 RIVERSIDE WAS RE-PRISTINED 2026-09-06 15:07 for Monday's heartbeat: 23 check-ins this week, last one today, invite-only, RidgePT untouched, 0 orphans, platform admin intact, and every Cognito sub verified against its `users.id`.** ⚠️ **David's stored Riverside-manager tokens are dead (fresh sub); he signs in again as `besoulful@gmail.com`. Not a bug.** **⛔ THE SEED SCRIPT HIT A THIRD TRAP AND, FOR ONE RUN, DELETED ALL EIGHT RIVERSIDE LOGINS WHILE THE DB ROLLED BACK** — `clinics.activated_by` on RidgePT pointed at David's user row and the FK has no on-delete clause. **Fixed in the script (it now preserves `activated_by` like it preserves `platform_admins`, and rehearses the deletes in a rolled-back transaction BEFORE touching Cognito).** Detail under Status & backlog.
>
> **🪤 OPERATIONAL, AND IT WILL BITE AGAIN: `aws ec2 start-instances` ON THE BASTION RETURNED `InsufficientInstanceCapacity` FIVE TIMES ON 2026-09-06.** (It started first time at 14:20 the same afternoon, so it really is a transient.) An AWS-side transient in that AZ, not a configuration fault. **Every database check goes through that bastion**, so the options are to wait it out or change the instance type (a stack change, and the AMI is deliberately pinned). **Do not conclude something is broken on our side.**
>
> **🗓️ ONE DATED CLEANUP: after 2026-09-18, delete the `/staff/:token` route alias** in `src/App.jsx`. It exists only because the first staff invite links pointed there and they live 14 days.
>
> **🔑 HOW A PATIENT GETS BACK IN. ⛔ THE "Patient Sign-In Link" CARD IS DELIBERATELY ONE NEUTRAL LINK FOR EVERY PATIENT, NOT A PER-ROW CONTROL** — a joined patient's invite token is spent, and a permanent per-person link would put "this address is a PT patient" in a URL forever. It shipped on the roster row first and David had it removed the same hour: **a per-row button implies a per-row link.** Do not put it back and do not "fix" it to be unique. **It copies an INSTRUCTION, not a bare URL**, and the exact text is rendered on the card.
> **glowpt.app** is the address to SAY to a patient. **Their invite link is the thing to SEND**: long, never typed, tapped once, and it works forever now — before joining it joins them, after joining it offers Sign In. **Day to day neither is usually needed, because tokens auto-refresh straight into the check-in.** The hole only opened when a session lapsed or they changed phone or browser.
>
> **🔎 WHEN THE NEXT REPORT COMES IN, START HERE. Every diagnosis in this doc came from a detail, not from reasoning about the code:**
> - **A BLANK PAGE MEANS SOMETHING DIFFERENT SINCE 2026-09-05.** The app has an error boundary, so a render crash shows "Something went wrong." **Ask him to tap "Show Details" and send that line** — it is the fastest route to a diagnosis. **A genuinely blank page now points at something the boundary cannot catch** (a failed bundle load, an async throw outside render, a route rendering nothing), which is a different search.
> - **The CODE LENGTH names the branch.** Sign-in codes are **8 digits**, sign-up confirmation codes are **6**.
> - **The EMAIL TEMPLATE names it too.** Sign-in says *"Your authentication code is …"*; sign-up says *"The verification code to your new account is …"*.
> - **WHICH failure screen appeared is evidence.** The plain "You're not connected to a clinic yet" means **no attach was attempted**; "We couldn't finish connecting you" means one was attempted and failed.
> - **Ask for the browser console.** `sign-in failed:` carries the real Cognito error name and `Profile re-attach failed:` the attach one.
> - **EVERY NEW CODE KILLS THE PREVIOUS ONE.** A second tab or an earlier attempt invalidates the code being typed. A private window works because it has no stale session.
> - **For a LAYOUT report, reproduce it with the page shell around it**, or you will conclude wrongly that it is not real.
>
> **🔑 TWO STANDING OPERATIONAL FACTS.**
> 1. **A new clinic is CLOSED until David opens `/admin` and presses Switch On.** The only human decision left in the flow, and where the BAA and the first payment get confirmed. `Record BAA Signed` and `Switch On` are separate buttons on purpose.
> 2. **🔒 EVERY clinic is invite only, always. There is no switch.** Walk-in sign-up (the `/join/<slug>` link and the printable QR) was removed on 2026-09-05. `/join/<slug>` still resolves but only says "You'll need an invite." **The `open_signup` column and its database gate are kept so the feature can return; only the route and the UI are gone.**
>
> **The two long-running gates on real patients, in detail. Neither is a coding task. Nothing is broken.**
>
> **1. ⛔ BEDROCK — UPDATE 2026-09-07: THE "FIX IN MOTION" BELOW DID NOT HAPPEN. AWS CLOSED THE CASE.** The 03:21 EDT reply (from `no-reply-aws@amazon.com`, unsigned) says the Bedrock team prioritizes model access for **AWS Startup Program (Activate) members** and **customers with a dedicated Account Manager**, and that all others "receive access gradually as part of the standard rollout." It points at Sales (`aws.amazon.com/contact-us/sales-support/`, "Sales Support" as nature of inquiry) and at enterprise support. Its "Important Notes" say Bedrock access and quota increases are restricted "during the Free Tier period"; **that clause does not apply, the org is on the PAID plan (verified)**. Case status **Resolved**. **Everything from here to the next horizontal rule is the 2026-09-03 state and is kept for the record; read it knowing the review it describes came back negative.**
>
> **▶ WHAT WAS DONE ABOUT IT, SAME MORNING:** David applied to **AWS Activate, Founders tier** ($1,000 credits, self-serve, the lane AWS named). Builder ID on `david@franklinaisolutions.com` (a THIRD AWS credential beside root and SSO; in his password manager), company `FranklinAI Solutions LLC`, website `franklinaisolutions.com`, founded **2026-07-27**, product launch 2026-07-12, bootstrapped, No Valuation, Healthcare, B2B, web browser, "using AI/ML to improve our products", linked and verified against **glowpt-prod `463556655381`**. **⛔ NOT the management account, deliberately**: the Bedrock wall is in glowpt-prod and both the credits and the Activate association sit at account level. Stated turnaround 5 to 7 business days (the guide says 7 to 10). **⚠️ The startups.aws.com form times out in minutes and loses the current step; steps already saved survive. David lost step 4 once.**
>
> **🏗️ THE ORG-WIDE QUESTION, ANSWERED (David asked whether this should unlock Bedrock for the whole org, since more HIPAA products are coming).** HIPAA coverage is already org-wide (the org BAA covers every current and future member account). **The Bedrock quota is NOT: it is per account, per Omkar's own explanation, so every new member account will start at zero again.** Activate cannot fix that either, since it links one Builder ID to one account and Founders is effectively one-shot. **So the plan is architectural: whichever account gets Bedrock unlocked becomes the organization's Bedrock account, and future products in their own accounts call it through a cross-account IAM role.** One quota fight, not one per product. An Account Manager, if one ever materializes via Sales, is customer-level and is when "cover all my accounts" becomes a sentence to say to a person.
>
> **🔧 SESSION HOUSEKEEPING:** a second CLI profile **`franklinai-mgmt`** (management account `456112636877`, same SSO session) now lives in `~/.aws/config`; it was added to read the Free Tier plan state, which a member account reports as "Missing data". Nothing else changed; no code, no deploy, production untouched.
>
> **[HISTORICAL FROM HERE] 1. ✅ BEDROCK — AWS HAS REPLIED, A FIX IS IN MOTION, AND THE FRIDAY DECISION IS OFF.**
> **✅ 2026-09-03 09:06 EDT — a named AWS engineer (Omkar) answered case `178761116000010`. The escalation WORKED and the free channel did NOT dead-end.** The case moved **Unassigned → Pending Amazon**, so it now sits with AWS, not with David. The reply, in substance:
> - **AWS CONFIRMED THE DIAGNOSIS IN ITS OWN WORDS** — all on-demand Bedrock inference quotas applied at 0, while the Service Quotas console believes the defaults already apply and refuses requests at or below them. **The three-system contradiction the escalation carried is what finally got the case read. That is the lesson worth keeping: carry NEW evidence, never a "any update?" nudge.**
> - **🆕 THE CAUSE, STATED BY AWS AT LAST: newer accounts, and accounts with limited usage history, START in a reduced-limit state on some services, and those limits rise automatically as usage and billing history grow.** `glowpt-prod` was created 2026-08-02 and has run almost nothing but RDS, Lambda and SES. **⚠️ NEITHER of the two candidate explanations this doc carried for ten days was right — not the account/region verification hold, not the Marketplace provisioning lag. Delete both on sight.** Nothing about the account, the code, model access or the use-case form was ever wrong.
> - **AWS has submitted the account to an internal review team to set the Bedrock inference quotas to standard defaults.** Stated turnaround **3 to 5 business days**; Omkar will follow up.
> - **⛔ DAVID MUST DO NOTHING, AND SPECIFICALLY MUST NOT FILE SERVICE QUOTAS REQUESTS.** AWS said so explicitly. **The Service Quotas channel stays closed.**
> **▶ THE FRIDAY 2026-09-04 DECISION POINT IS SUPERSEDED. BOTH OPTIONS ARE OFF THE TABLE.** **(a) $29 Developer Support now buys NOTHING** — a human holds the case and has actioned it; raising it would be the "pay to be heard" note he pushed back on. **(b) Shipping with the AI reflection switched off** is likewise unnecessary. **Do not re-open either before the review outcome lands.**
> **Claude cannot read the case.** `aws support` returns `SubscriptionRequiredException` on Basic, so it is console-only and checking is David's.
>
> **⚠️ THE FRAMING, CORRECTED BY DAVID 2026-08-30. AN EARLIER VERSION SAID "BEDROCK IS NOT WHAT BLOCKS REAL PATIENTS." THAT WAS WRONG. DELETE THAT IDEA ON SIGHT.**
> **There are TWO gates on real patients and they are AND, not OR. Both must clear.**
> 1. **The attorney-reviewed clinic BAA** (item 2 below).
> 2. **A HIPAA-covered path for the AI reflection.** The prompt carries PHI, so whoever runs the model is a business associate.
>
> **Gate 2 has exactly two possible routes, and one is closed by David's decision:**
> - **An Anthropic 1P BAA — RULED OUT.** Not self-serve, needs Sales, and **David is not paying for a second BAA to do what the AWS one he already holds covers for free.** A settled commercial call. **Do not re-propose it as the quick unblock.**
> - **Bedrock — the live route, and it is stuck.** Covered by the org-level AWS BAA at no extra cost.
>
> **So Bedrock IS a hard gate.** **What remains true:** the two gates are **independent and can be worked in parallel**, so a session with nothing to do on Bedrock should push other work rather than re-measuring quotas.
> **The only lever that would remove Bedrock from the critical path** is a product decision, David's to make: ship with the AI reflection switched off, or de-identify the prompt so hard nothing PHI-bearing leaves AWS. The second guts the feature. **Neither has been proposed to David; do not assume either is acceptable.**
> **Production is untouched and healthy** — glowpt.app still calls api.anthropic.com.
>
> **2. ⚖️ WAITING ON AN ATTORNEY — two legal drafts are written and ready to be reviewed together. David has it in hand (see the top of this block); do not chase him.**
> - `legal/BAA-draft-for-attorney-review.md` — HIPAA/PHI, legally required.
> - `legal/Subscription-Agreement-draft-for-attorney-review.md` — the commercial deal ($350/mo, term, IP, warranties, **liability cap**).
>
> Both are **committed and pushed**, marked **DRAFT — NOT FOR EXECUTION**, and carry **`[REVIEW]`** tags at every decision point. `BAA_IS_EXECUTED = false` in `src/lib/legal.js` and stays false until an attorney signs off AND a clinic signs. **Demo-data-only still governs.**
>
> **The three questions counsel must answer, in priority order:**
> 1. **Does the liability cap reach PHI claims?** 12 months at $350/mo is **$4,200**, trivial against a real breach — and a cap set too low can be struck out entirely, leaving none. **This and the insurance question are the same decision.**
> 2. **Is the clinical disclaimer enforceable, and must it also appear in the patient UI?** Patients write free-text notes nobody reads in real time; nothing detects or escalates an emergency or self-harm. Subscription §4.4 + §11.3. **More product-specific and more dangerous than the HIPAA boilerplate.**
> 3. **Click-through or signature?** Both drafts present the choice (BAA §A/§B, Subscription §16.1/§16.2). Pick one, delete the other. Likely also drives the PDF-download backlog item.
>
> **⚠️ ONE HAZARD THAT EXPIRES ON ITS OWN:** the BAA's subcontractor table names **AWS alone**, which is only true *after* Bedrock ships. Production still calls Anthropic today. **Do not execute the BAA before `bedrock-ai-response` merges without fixing that table first.**
>
> ---
>
## AWS account facts (Phase 0 — permanent reference, from claude.ai handover 2026-08-02)

**This is the permanent home for AWS account facts. Do NOT copy any of this into the FranklinAI website project instructions — that project explicitly excludes GlowPT/AWS backend and legal-entity matters.** Region is `us-east-1` everywhere unless stated.

**Legal entity (the BAA binds to this):**
- **FranklinAI Solutions LLC**, Pennsylvania, entity #0015737128, EIN in hand.
- The AWS **management account's Company name** field reads exactly `FranklinAI Solutions LLC` (no comma). **The org BAA binds to this field — do not change it.** The account's **Full name** is David Peterson (individual contact) — correct and expected.
- Billing address = David's home, 6210 Ridge Ave #3, Philadelphia PA 19128. Registered-agent (ZenBusiness) and IRS addresses differ on purpose — normal, not a problem. Tax settings (TRN/legal name) are intentionally blank; optional, no dependency.

**Organization structure:** Root → `Workloads` OU → `glowpt-prod`, with the management account at Root.

| Item | Value |
|---|---|
| Organization ID | `o-4js89l459j` |
| Feature set | All features enabled |
| Root ID | `r-i93g` |
| Workloads OU | `ou-i93g-vvz3dnxq` |
| Management account | `FranklinAI` — `456112636877` — root email `besoulful+aws-franklinai@gmail.com` |
| Member account (workloads) | `glowpt-prod` — `463556655381` — root email `besoulful+aws-glowpt@gmail.com` |

`OrganizationAccountAccessRole` left in place on `glowpt-prod` at creation.

**BAA — Active, effective 2026-08-02:**
- **AWS Organizations Business Associate Addendum accepted at the ORG level** from the management account (AWS Artifact → Agreements → Organization agreements). Coverage is **automatic for the management account and every current & future member account** (incl. `glowpt-prod`) — **no per-account BAA needed.** It's a click-accept addendum binding the entity in the Company-name field; it never prompts for a typed entity name.
- The separate **HCLS BAA Addendum was deliberately NOT accepted** (it would let the Amazon Connect Health Team de-identify data for service improvement — not needed). Leave it Inactive.
- **Attorney review was skipped by David's explicit decision** — recorded so it isn't silently re-litigated.
- BAA reminder: PHI accounts must use **only HIPAA-eligible services** and **encrypt PHI in transit and at rest**.

**IAM Identity Center (SSO) — how David signs in:**
| Item | Value |
|---|---|
| Instance type | Organization instance |
| Instance ID / ARN | `ssoins-72237eeae3063609` / `arn:aws:sso:::instance/ssoins-72237eeae3063609` |
| Identity Store ID | `d-906678b3ec` |
| **AWS access portal URL** | `https://d-906678b3ec.awsapps.com/start` (permanent; both accounts reachable from one login) |
| Issuer URL (OIDC) | `https://identitycenter.amazonaws.com/ssoins-72237eeae3063609` |
| Region | `us-east-1` (permanent — moving it means delete + rebuild) |
| Identity source | Identity Center directory (built-in) |
- **User:** `david` / David Peterson / `david@franklinaisolutions.com` (enabled).
- **Permission set:** `AdministratorAccess` (predefined), **8-hour** session, no relay state.
- **Assignments:** `david` + `AdministratorAccess` → **both** `FranklinAI` and `glowpt-prod` (provisioned & confirmed).

**Credentials model — SSO only:**
- **No IAM users, no long-lived access keys — none are to be created.** Console via the portal URL above.
- **CLI/SDK:** `aws configure sso` against the portal URL, profile region `us-east-1`.
- Root creds for both accounts live in the password manager with virtual MFA; **root is retired to billing changes + account closure only.**
- **Default console region** set to `us-east-1` under User settings in both accounts (per-user, per-account — doesn't follow the identity across accounts).

**Cost controls:** budget `franklinai-monthly` in the **management account** (billing is consolidated there), **$150/mo**, all services, alerts to `david@franklinaisolutions.com` at 85% actual / 100% actual / 100% forecasted.

### 🔧 Live infrastructure and the DB runbook (moved up 2026-09-12 from the archived migration log — this is operational, not history)

| Thing | Value |
|---|---|
| **DB admin secret NAME** (this is what `--secret-id` takes) | `GlowptFoundationDatabasePos-3cW3pOZuMQHv` |
| DB instance endpoint | `glowptfoundation-databasepostgres277ef4cb-7l9zmtaid6jt.ci7a20as6ck9.us-east-1.rds.amazonaws.com` |
| RDS Proxy endpoint (the app connects HERE, never the DB directly) | `glowptfoundationdatabaseproxyd1849d21.proxy-ci7a20as6ck9.us-east-1.rds.amazonaws.com` |
| Bastion instance (kept STOPPED between sessions) | `i-04b7a6d483e8b682a`, t4g.nano in **us-east-1a** |
| Cognito user pool / app client | `us-east-1_q2W6uXTZE` / `6upb217er13tibbke4qbalhbji` |
| HTTP API | `https://byaepos5vl.execute-api.us-east-1.amazonaws.com` |
| psql | `/Applications/Postgres.app/Contents/Versions/18/bin/psql` |
| Other DB role secrets | `glowpt/db/app` · `glowpt/db/postconfirm` · `glowpt/db/weekly` |

**⚠️ THE SECRET NAME HAS NO SUFFIX.** The full ARN ends `…-3cW3pOZuMQHv-ZUpaOq`; that trailing `-ZUpaOq` is Secrets Manager's random ARN suffix and is **NOT part of the name**. Passing it returns `ResourceNotFoundException`, psql then silently drops to a `Password:` prompt and hangs. **You cannot look the name up at runtime — `aws secretsmanager list-secrets` is classifier-blocked — so it has to be correct here.** This cost a whole session on 2026-08-23.

**▶ TO REACH THE DATABASE:**
1. `aws ec2 start-instances --instance-ids i-04b7a6d483e8b682a --profile glowpt-prod --region us-east-1` — then wait for SSM `Online` (~1 min).
2. `aws ssm start-session --target i-04b7a6d483e8b682a --document-name AWS-StartPortForwardingSessionToRemoteHost --parameters '{"host":["<DbInstanceEndpoint>"],"portNumber":["5432"],"localPortNumber":["5433"]}' --profile glowpt-prod --region us-east-1` — **RUN THIS BARE.** `session-manager-plugin` is symlinked into `/usr/local/bin`, so it resolves on PATH. **⛔ Do NOT prepend `export PATH=…`** — the permission allowlist matches by PREFIX, so a prefixed command no longer starts with `aws ssm start-session` and gets blocked. Bare = allowed but needs the symlink; prefixed = finds the plugin but is refused. That dead end reads exactly like a permissions failure and is not one.
3. Connect: `PGHOST=localhost PGPORT=5433 PGSSLMODE=require`, creds from the secret above.
4. **`aws ec2 stop-instances` the bastion when done**, and shred any local copy of the password.

**🪤 `InsufficientInstanceCapacity` ON THE BASTION IS A RECURRING AWS TRANSIENT**, not a fault here — five times on 2026-09-06, three on 2026-09-12. **us-east-1a is the constrained AZ for this account** (it is also the one that lacks the `cognito-idp` VPC endpoint). **A retry loop is the answer**; changing the instance type is a stack change and would give the bastion a new instance id, which this runbook hardcodes.

**⚠️ DEPLOY GOTCHAS:** always `cdk diff` first and **STOP if anything unexpected shows `replace`** · run `npx jest` in `infra/` beside it (8 seconds; it has caught a stale suite twice) · a VPC Lambda takes ~20 min to DELETE on rollback (slow, not stuck) · if `cdk.out` is locked add `--output cdk.out.deploy` · RDS Proxy rejects `statement_timeout` (use pg `query_timeout`) · Cognito sign-IN OTP is **8 digits**, sign-UP confirm is **6** · a signed-in API test needs the **ID token**, not the access token · the bastion AMI is **deliberately pinned** in `infra/lib/bastion.ts`, so bump it only in its own change.

**🚧 Claude cannot run a destructive statement against production** — the classifier blocks it, correctly. Hand David the paste-and-run instead: write the `.sql` in its OWN command and verify it exists on disk first (bundling the heredoc and the psql run means the whole thing is refused as a unit and he gets a dead file path), always wrap in `begin; … commit;` with the guards BEFORE the commit, and tell him `PGPASSWORD` is already exported.

### AWS open items (carry into the build session)

1. **✅ SES production access — GRANTED 2026-08-10.** `glowpt-prod` SES (us-east-1) is **out of the sandbox**: sending quota **50,000 emails/24h**, max send rate **14/sec**, account health **Healthy**, can now send to any recipient (no longer verified-addresses-only). Approval email landed at `besoulful@gmail.com`; case `178587773500860` is resolved. (History for context: requested 2026-08-04 on the streamlined form → AWS asked the routine "tell us more" follow-up → David replied 2026-08-07 → granted overnight 8/9–8/10.) **This was the last SES setup item.** The only SES work left is the two go-live TODOs (KMS-encrypted SNS key + in-AWS bounce handler), which are Phase 4/cutover items — see "SES progress" below. Original prepped use-case text (no longer needed, kept for reference): *GlowPT is a patient-engagement app for PT clinics; transactional email only … bounces/complaints monitored via SNS, hard bounces auto-suppressed.*
2. **✅ Second MFA device — DONE 2026-08-04.** `david`'s SSO user now has **two** MFA devices: `david's MFA 1` (WebAuthn passkey, bound to the **current MacBook** via Safari/iCloud-keychain — likely NOT portable to the new laptop) + `david's MFA 2` (**Authenticator app** = his phone's Google Authenticator, **cloud-synced to his Google account** so it rides to a new phone). **AWS 2FA is no longer laptop-dependent.** Root cause of the earlier "failed twice": **no TOTP was ever registered** (the Security page showed zero authenticator apps) — the 3 duplicate `AWS SSO: david` phone rows were dead orphans, now **deleted**, leaving exactly one live row. His iPhone clock was fine; the real culprits were code-expiry-on-retype + picking a dead row. **✅ Both remaining pieces DONE 2026-08-04 (2nd session):** (a) `david's MFA 2` **renamed → "iPhone – Google Authenticator"** (IAM Identity Center → Users → david → MFA devices); (b) **SSO password VERIFIED** — David signed into the access portal in a private window with the password he knew (Touch-ID passkey as 2nd factor). **Laptop-swap login now fully de-risked: password (known) + iPhone Google Authenticator (cloud-synced, portable) both travel; the laptop-bound passkey won't be needed.** AWS 2FA item CLOSED. Full detail in the `feedback-auth-2fa-handling` memory.
3. **Low priority:** SCP to stop member accounts creating their own Identity Center instances; Identity Center instance name (cosmetic); Billing tax TRN/legal name; **CloudTrail** (flagged "Recommended", not yet configured — revisit when PHI is in scope, since audit logging is a HIPAA expectation).

**Session logistics for handing AWS steps to David:** one step per message; **name the account AND region before every step** (SES especially is per-account, per-region); no optional side-quests mid-task; **verify the current console flow before sending him into it** (buttons have moved); direct answers to direct questions; he's new to AWS, not slow — **explain what a thing is *for* before the clicks.** QR-code steps go on the laptop with the phone as scanner, never the reverse.

## 📜 STANDING RULES (distilled 2026-09-12 from entries now in `docs/history.md`)

**These are the rules whose reasoning was archived. Each line is the rule; the story behind it is in `docs/history.md`, searchable by the phrase in bold.**

**✍️ COPY**
- **STATEMENTS** (headlines, prose, empty states) get **sentence case and a terminal period**. **LABELS** (titles, buttons, pills, section heads) get **Title Case and no period**. Questions keep the `?`.
- **⛔ NO ALL-CAPS anywhere.** If you ever remove caps from something, retune the tracking too (~0.01em, not the 0.1–0.2em that suits uppercase) and step the size up.
- **⛔ NO EM DASHES in any user-facing string** — and the rule covers `infra/lambda/` too, not just `src/`. Use a period, comma, colon or parentheses. A dash used as a LABEL SEPARATOR becomes the house middot `·`.
  - **⛔ DO NOT find-and-replace: SIX bare `—` are empty-value placeholders** (the dashboard Streak cell, the PatientApp selected-day stats) and must survive. Code comments and this doc are exempt.
  - **The AI prompt carries the rule, and `src/lib/houseVoice.js` enforces it in code** because three rounds of prompt wording did not hold. Keep both: the prompt line stays even though the backstop exists.
- **Title Case convention (AP, not Chicago):** a preposition of **four or more letters is capitalised** (`With`, `From`, `About`, `Between`); three or fewer stays lowercase (`at`, `by`, `for`, `in`, `of`, `to`, `up`). Articles and coordinating conjunctions lowercase unless first or last. **Phrasal particles ARE capitalised** (Sign In, Sign Out, Sign-Up). Capitalise **both halves of a hyphenate** (Last Check-In).
- **🇺🇸 American English, spelling AND idiom.** A spelling sweep will not catch idiom — "how you are getting on" was perfectly spelled and still wrong. Search for phrases and inflections.
- **FranklinAI forbidden words apply here too** (the voice travels, not the repo): no "software" (say "apps"), no "in plain English", no "upsell", no "treatment room" (say "treatment areas").
- **🔗 An inline link inside prose must be a `<span role="button" tabIndex={0}>`, never a `<button>`.** A button is an atomic inline-block and will not wrap, which stranded a period on its own line at phone width. **Its onClick MUST call `preventDefault()` as well as `stopPropagation()`** — these sit inside a `<label>`, and without it opening the modal silently ticks the consent checkbox.

**🎨 BRAND — ONE DECLARATION, ALWAYS**
- **The logo is `public/favicon.svg` displayed directly**, declared once as `LogoMark` in `src/screens/AuthShell.jsx`. Tab icon, home-screen icon, in-app logo and the weekly email are the same artwork and cannot drift. **If the art changes, re-render `public/apple-touch-icon.png` from it** (headless Chrome at `--force-device-scale-factor=3 --window-size=180,180`, then `sips -z 180 180`) — that PNG is the one derived copy, and email clients cannot render SVG.
- **The wordmark is one `<Brand />` component**; the palette is `#F5A81A` (primary) and `#FBC02D` (accent) on `#0d1825` / `#1a2840`.
- **⛔ `FEELING_COLOR[3]` in `PatientApp.jsx` keeps the OLD amber `#c8861d` on purpose** — there the colour is data, the middle of a red→green mood ramp, not branding. Do not "finish" the palette sweep by changing it.
- **This doc has recorded SIX duplicated-declaration drifts** (favicon, LogoMark, wordmark, app bar, mood scale, per-screen style blocks). The rule is one declaration, always.

**🧪 TESTING AND DEMO**
- **⛔ NEVER fold David's own testing into Riverside PT.** `scripts/aws-seed-demo.mjs` deletes the whole clinic row, check-ins cascade and profiles go `clinic_id = null`, so any extra patient becomes an orphan. **Riverside = the sales demo, reset freely, nobody real is ever invited. RidgePT = the sandbox, never reset, its dashboard is never shown outside.**
- **A re-pristine invalidates David's session** — every demo Cognito account is destroyed and recreated with a fresh sub, so he signs in again. Not a bug.
- **The seed script has needed teaching FOUR times** (`activated_at`, `open_signup`, `platform_admins`, `activated_by`). It inserts the clinic directly rather than through `provision_clinic`, so **any new column with a safe default needs teaching there in the same commit.**

**🖼️ HOW TO ACTUALLY LOOK AT A UI CHANGE**
- **Start the dev server with `preview_start {name:"glowpt-dev"}`, never Bash.**
- **The Browser pane cannot open scratchpad files** (they load as a static snapshot). For a file harness, put it in the project temporarily and delete it after.
- **Headless Chrome clamps its window to a 500px minimum** — a `--window-size=390` screenshot lays out at 500 and CROPS, which reads exactly like overflow. **Measure `innerWidth` before believing a narrow screenshot**; use the browser pane at an emulated width instead.
- **`Dashboard` and `PatientApp` sit behind the OTP login and cannot be driven here.** For those, extract the REAL style values out of the source into a harness, and say plainly that it is not the live screen.
- **Measure, do not eyeball.** Read coordinates off the screenshot before touching a size.

**⚙️ ARCHITECTURE FACTS THAT ARE STILL LIVE**
- **A new clinic is CLOSED until David presses Switch On in `/admin`.** Two separate columns on purpose: `activated_at` (the gate) and `baa_signed_at` (the legal record). The gate is enforced in the DATABASE, in both `join_clinic` and the `checkins` RLS policies — gating only the join would let an already-attached patient keep writing PHI to a switched-off clinic.
- **`platform_admins` is its own table, not a profiles role**, with RLS forced, no policies and no grant to `glowpt_app`.
- **Modals use the shared `src/lib/useModal.js`** (scroll lock, Escape, focus trap, ARIA). **Call it ABOVE any early return**, and put the ref on the PANEL. Do not paste the effect into a fourth screen.
- **Fraunces text must state `fontStyle: 'normal'` explicitly.** `font-style` inherits, and an incomplete font request lands on the italic face in Safari. Load-bearing; do not delete as redundant.
- **`PRICE_LINE` is contractually load-bearing** (Subscription §5.1 "the amount stated at sign-up") and renders on `/onboard` as well as the More Info modal. Change `MONTHLY_PRICE_USD` in `src/lib/marketing.js`, never the strings.
- **Any style under 18px that does not state its own `lineHeight` inherits the body's ABSOLUTE 26.1px** and will read too loose. Deliberately not swept app-wide; it is why the landing footer and the flag pills each had to state their own.

**⏳ STILL-OPEN BACKLOG ITEMS (carried over from archived entries)**
- **Owner/super-admin dashboard across all clinics** — David flagged it 2026-07-14 as a near-term want. Keep it to clinic-level aggregates and billing, never patient PHI across clinics. (`/admin` now does part of this.)
- **A downloadable dated PDF of the accepted agreements** — the clinic is a covered entity and generally needs the executed BAA in its own records. Most of the machinery exists (`legal.js` versions the text, the app records a version per user). Gated on counsel answering the click-through-vs-signature question.
- **No design-token file.** Colours are still inline literals across seven files. If the palette moves again, extract tokens first.

## Status & backlog

- **📅 A CHECK-IN BELONGS TO THE PATIENT'S OWN DAY, NOT TO A UTC ONE. Reported by David 2026-09-12 07:20, live 08:20 (commit `669afb8`).** *"I just checked in Charlie. it says he has a 1 day streak. But he checked in yesterday too. I'm confused."*
  - **IT WAS DATA LOSS, NOT A WRONG NUMBER, AND THE PROOF WAS IN THE ROW ITSELF.** Charlie checked in Friday 14:12 and again Friday 20:14. The 20:14 row carried **"I got up at 3am"** and a reflection about a 3am wake-up — Saturday morning's check-in, on Friday's timestamp. The morning's upsert had found the UTC slot taken and updated it, and **`created_at` is not in the update list**, so the row kept the old time with the new content. **Friday evening's content is unrecoverable; it was gone before anyone looked.**
  - **THE MECHANISM: the save keyed on `utc_date(created_at)`, every display bucketed by local day, and in Eastern the UTC day rolls over at 8pm.** So the two axes disagreed for exactly the check-ins made in the evening.
  - **⛔ THE FIX WAS PUT TO DAVID AS A DESIGN DECISION RATHER THAN PATCHED QUIETLY**, because "which day does a 10pm check-in belong to" is a product question. He chose storing the patient's own date over shifting the boundary to Eastern: *"a day should be a 24 hour day starting at midnight, right? And it will be tied to the patient's correct time zone too, right?"* Yes to both. The cheaper option was wrong the moment a clinic outside Eastern signs up.
  - **`src/lib/localDay.js` IS THE ONE FRONTEND DEFINITION OF "WHICH DAY IS IT".** ⛔ Never `toISOString().slice(0,10)` — that is the UTC date and is the bug itself. The helper's own test prints the trap: a Friday 20:14 local time returns `2026-09-11`, while `toISOString` says `2026-09-12`.
  - **🆕 IT FIXED A SECOND BUG NOBODY HAD HIT: the roster bucketed days by the STAFF member's browser**, so a therapist in another time zone would have read their patient's days shifted. `lastCheckin` now carries `local_date` too, so "Yesterday" means the patient's yesterday.
  - **✅ THE DESTRUCTIVE HALF OF THE OVERWRITE IS GONE BY CONSTRUCTION**, which is the neatest part: once the day is right, a check-in can only ever replace one from the same local day — the deliberate 2026-07-15 behavior. Overwriting a DIFFERENT day is now impossible rather than guarded against.
  - **⚠️ TRAP 1, AND IT WOULD HAVE BEEN A SILENT TOTAL OUTAGE: `node-postgres` PARSES A `date` COLUMN INTO A JS `Date`.** It JSON-serialises to `"2026-09-11T00:00:00.000Z"`, so every string day-comparison would have failed and **every screen would have rendered as though the patient had never checked in.** Both reads use **`to_char(local_date,'YYYY-MM-DD')`** — and `to_char` rather than `::text` because **`::text` renders through the session's `DateStyle`**, proved by setting `DateStyle=SQL, MDY` and watching it return `09/11/2026`. Production is `ISO, MDY`, so `::text` would have worked today. **That is the point: it was correct by luck, not by construction.**
  - **⚠️ TRAP 2: PATCH 2 WAS NOT RE-RUNNABLE.** It renames the new index into the old one's name, so a second run of `drop index if exists <old name>` would have **destroyed the index it had just installed**. Its own guard caught it during rehearsal and rolled back. It now drops by index DEFINITION (matching `utc_date`), never by name. **When a migration renames X into Y's name, any name-based step in the same patch is a landmine on the second run.**
  - **▶ FOUR STEPS, AND THE ORDER IS LOAD-BEARING: frontend → patch 1 → API → patch 2.** Patch 1 adds the column and the new index but **deliberately leaves the old index**, so the still-deployed API keeps working; it also sets a transitional default reproducing the old behavior, since the live API inserts without the column. Patch 2 drops the old index, drops the default (so a future writer that forgets the day **fails loudly** instead of silently filing a UTC day again) and updates `weekly_summary_rows()`. **There was never a window in which a patient could not check in.**
  - **🔧 THE REPAIR RULE, rather than a hand-edit of one row:** where the Eastern backfill collides, the **later** row takes its UTC date, because that is the day its surviving content came from. Exactly one row qualified. **Felix's 22:09 Friday entry correctly stayed on Friday**, because it was never overwritten — verified row by row on production, and that distinction is the whole reason the rule is written this way.
  - **📅 `weekly_summary_rows()` COUNTED DISTINCT UTC DAYS, THE SAME AXIS AS THE BUG**, so it **over**-counted anyone whose evening check-in spilled into the next UTC date (Charlie read 3 days for 2). ⚠️ **The doc had predicted UNDER-counting; the real effect was the opposite.** Spliced verbatim out of `schema.sql` into the patch so the two cannot drift.
  - **✅ OBSERVED, NOT ASSUMED:** suite **69 → 71**, with **T13b/T13c** the regression test — and **T13c asserts the two rows share one UTC day**, so it proves the old key really did collapse them · the old schema rebuilt from git and the collision reproduced on it (`duplicate key ... (user_id, utc_date(created_at))`) · both patches rehearsed against a **pre-patch** database seeded with production's shape, both idempotent, result **diffed byte-identical against a fresh build** including function owner and ACL · the **real handler statement extracted from `index.ts`** and replayed · `cdk diff` exactly one Lambda, **no replacements and the bastion absent**, `BastionInstanceId` unchanged after · **deployed code pulled back from AWS** · live bundle **byte-identical to the build tested** · **the bug replayed on production in a rolled-back transaction**: two check-ins both on UTC day 2026-10-02 survive as two rows · **the real `buildRoster` on live data: Charlie streak 2, "Today"** · weekly dry run `queued:16`.
  - **🪤 T13b FAILED FIRST TIME FOR A REASON THAT HAD NOTHING TO DO WITH WHAT IT TESTED:** written with 2026-09-11/09-12, it collided with the seeded `current_date` row and read 1. **A test dated "today" is a test that behaves differently tomorrow.** It uses fixed past dates in June, which is also unambiguously EDT so the 8pm straddle does not ride on a DST boundary.
  - **🪤 AND THE RUNNER HID AN ERROR COMPLETELY: `db/tests/run_tests.sh` pipes each suite through `grep -E "PASS:|FAIL:" || true`, so a suite that ERRORS prints NOTHING.** `rls_tests.sql` died on a not-null violation and the run simply had no T1-T23 in it, looking merely short. **If the test count looks low, the suite errored.** Not fixed; worth doing.
  - **🪤 OPERATIONAL: `InsufficientInstanceCapacity` ON THE BASTION AGAIN**, three times across the morning, cleared on the third attempt of a 60s retry loop at 08:13. It is a **t4g.nano in us-east-1a** — the same AZ that lacked the `cognito-idp` endpoint on 09-06, so **us-east-1a is the constrained one for this account.** A retry loop is the answer; changing the instance type is a stack change and would give the bastion a new instance id, which this doc's runbook hardcodes.

- **⬛ THE WHITE TRIM AROUND EVERY SCREEN WAS THE VITE SCAFFOLD IN `src/index.css`. Fixed 2026-09-11.** David: *"Look at the white border/trim around the screen. I had always had my iphone in dark mode until now, so I never noticed it. In dark mode, the white trim appears darker and blends in. Can you make it all darker or bleed it off the screen or something."*
  - **THREE LEFTOVERS STACKED, AND HIS OWN SENTENCE NAMED THE THIRD ONE.** (1) `:root { background: var(--bg) }` with **`--bg: #fff`**, so the page canvas (html) was WHITE. (2) **`#root { border-inline: 1px solid var(--border) }`** — a literal 1px `#e5e4e7` line down the left and right edges. **That was the trim.** (3) **`color-scheme: light dark`**, so in DARK mode the browser painted its own canvas and controls dark and hid all of it. **That is why he had never seen it in three months and saw it the day he turned dark mode off. A bug that is invisible in one of the two modes is a bug nobody reports until the mode changes.**
  - **⚠️ THE SCREENS ALL LOOKED CORRECT, WHICH IS WHY THIS HID IN PLAIN SIGHT.** Every screen paints `#0d1825` on its own container at `minHeight: 100vh`. The white was never inside a screen; it was the page **around** them — the overscroll bounce above and below, the 1px edges, and everything past 1126px on a desktop. **Reading a screen file would never have found it.**
  - **🔁 AND IT IS THE SIXTH DUPLICATED-DECLARATION DRIFT THIS DOC HAS RECORDED**, after the favicon, `LogoMark`, the wordmark, the app bar and the mood scale. **Five screens inject their own `<style>` shell reset: Landing, AuthShell and PatientApp set `body` ONLY, while Dashboard and Admin set `html, body`.** So two screens happened to cover the canvas and three did not, and the three that did not are the three David was looking at. **The fix is in `index.css` so it covers all five, including the screens behind the OTP login that cannot be checked by eye.** ✅ **And the five copies were then consolidated the same evening, at David's word — see the entry directly below, which found two live bugs hiding in them.**
  - **WHAT `color-scheme: dark` BUYS BEYOND THE CANVAS, and it is the half worth keeping:** it makes the browser's OWN surfaces dark on a phone that is in light mode — the overscroll area, the scrollbars, **and native form controls.** The sign-in email field and the dashboard's therapist `<select>` were rendering with light chrome inside a navy app. **GlowPT has no light mode; the stylesheet now says so.**
  - **📱 `theme-color` ADDED TO `index.html`, AND IT IS THE HALF THAT ACTUALLY FIXED THIS.** Without it, iOS Safari tints its toolbars by sampling the page, which can land lighter than the app and reads as more of the same frame.
    - **⛔⛔ `viewport-fit=cover` WAS ADDED ALONGSIDE IT AND HAD TO BE REVERTED THE NEXT MORNING (2026-09-12). DO NOT PUT IT BACK.** It extends the PAGE under the status bar and the home indicator, not merely the background — and **nothing in this app uses `env(safe-area-inset-*)` to compensate** (grepped: zero). So on David's iPhone the status bar ate the top padding of every screen and the home indicator ate the bottom: **the welcome and landing logos sat on the very top edge, the dashboard's Sign Out could not be tapped at all, and pages read as though the bottom had pushed up.** His words: *"You screwed up all the top padding above the logo... I don't know what you did buddy."* **It buys only navy behind the notch, which nobody asked for and `theme-color` largely covers. If it is ever genuinely wanted, every top-level screen container needs safe-area padding FIRST.** A comment on the viewport tag says so.
  - **⚠️ TWO THINGS IN THAT FILE WERE PRESERVED ON PURPOSE AND MUST STAY.** (1) **`font: 18px/145%`, the ABSOLUTE 26.1px root line-height** that the landing footer and other small text were tuned against on 2026-09-03 — byte-identical before and after, checked rather than assumed. (2) **The `#root` "NO text-align here" warning block**, which is the 2026-09-05 trap that cost most of a session twice.
  - **🧹 The dead scaffold went with it:** the purple `--accent` family, `#social .button-icon`, the `h1`/`h2`/`code`/`.counter` rules and the whole light/dark variable block, **all verified unreferenced first** (nothing in `src/` uses any `var(--…)`, and there is no `<h1>`, `<h2>`, `<code>`, `.counter` or `#social` anywhere in the app). **CSS bundle 2.4 kB → 0.70 kB.** The three stale files the doc had flagged since 2026-08-25 are deleted too — `src/assets/react.svg`, `src/assets/vite.svg` and **`public/icons.svg`, which was being served to the live site for nothing**; the JS and CSS hashes were byte-identical after the delete, which is the proof they were unused.
  - **✅ MEASURED, NOT EYEBALLED, IN BOTH MODES:** at **375px in LIGHT mode** (the mode that exposed it) html and body both `rgb(13,24,37)`, `#root` border `0px`, root spanning `left 0 → right 375`, no horizontal overflow · same at **375px in DARK** · at **1500px light** the root fills the full width with **zero gap either side**, where the 1126px cap used to leave white · `/login` (the AuthShell shell, one of the three that set `body` only) clean at 375 · zero console errors · build and both prebuild guards clean.

- **⚠️ MY SECOND REGRESSION, SAME NIGHT, SAME SHAPE: A GLOBAL `overflow-x: hidden` TURNED OFF THE MODAL SCROLL LOCK. Shipped 2026-09-11 in `fb5f210`, reverted 2026-09-12 in `269a768`.** David: *"I'm pretty sure scroll-lock was on for open modals, but now it seems to be turned off again. How many things were screwed up buddy."*
  - **THE MECHANISM, AND IT IS WORTH KNOWING BECAUSE IT IS INVISIBLE:** `useModal` locks the page with **`document.body.style.overflow = 'hidden'`**. That only reaches the viewport because **body's overflow PROPAGATES to it, which the spec allows only while `<html>` is `overflow: visible`.** Putting `overflow-x: hidden` on html makes html **its own scroll container** — and note `overflow-x: hidden` forces `overflow-y` to compute as **`auto`**, so html stops being `visible` even though nothing named overflow-y. Propagation stops, the lock clips nothing, and the page scrolls happily behind the modal.
  - **IT HAD ONLY EVER BEEN ON DASHBOARD AND ADMIN, NEITHER OF WHICH HAS A MODAL**, so it was harmless there for months. Globalising it hit **exactly the three screens that do have one** (Landing, Onboard, Join). **It is back on those two screens, in their own `<style>` tags, with a comment on both sides saying why it cannot be global.**
  - **🤚 TWO TESTS GAVE CONFIDENT FALSE READINGS BEFORE A REAL ONE WORKED. This is the reusable part.** (1) **`window.scrollTo()` IGNORES `overflow: hidden`** — programmatic scrolling is not blocked by it, so "the page still moved" proved nothing and made the FIX look broken too. (2) **`getComputedStyle(html).overflowY` DOES NOT REFLECT PROPAGATION** — it reports the cascaded value, so it read `visible` whether or not the lock was reaching the viewport. **Only a dispatched scroll event (`computer{action:"scroll"}`) tests a scroll lock.** With it, the bug reproduced instantly (0 → 98.5 behind an open modal with the lock applied) and the fix verified just as clearly.
  - **✅ VERIFIED ON TWO OF THE THREE MODALS DIRECTLY** (Landing's More Info, Onboard's BAA): page holds at 0 under a real wheel event, the panel still scrolls its own content (500px), the lock releases on close, the page scrolls again after, and **the consent checkbox is still untouched through the cycle** so the 2026-08-26 `preventDefault` behaviour survives. **The third (the privacy notice) is no longer reachable without a live invite token** — `/join/<slug>` lost its modal when walk-in sign-up was removed on 09-05 — so it is covered by construction (same hook, same global CSS) rather than by observation.
  - **🧹 THE AUDIT THAT FOLLOWED, because David was right to ask how many.** Every change from that evening was re-checked against "was this asked for, and can I test it?". **`-webkit-text-size-adjust: 100%` was put back on the Dashboard alone** (`6884d6a`): globalised as a tidy-up, never requested, it changes how iOS sizes text, and **it cannot be verified without his phone — the same shape as both things that broke.** The deleted scaffold was re-confirmed inert (all four `<p>` elements set their own margins; no `<h1>`, `<h2>`, `<code>`, `.counter` or `#social` exists in `src/`). **The tap-feedback rule `a:active, button:active` was KEPT, flagged to him, and ✅ HE CONFIRMED IT: *"Leave the tap dimming, I like it consistent."*** So links and buttons dim alike on tap app-wide, on purpose. A 0.85 opacity flash, no layout effect.
  - **⛔ THE LESSON, WHICH IS NOW TWO FOR TWO IN ONE NIGHT: THE BUG WAS NEVER THE CSS, IT WAS SHIPPING UNREQUESTED GLOBALS.** He asked for a white trim to go away and for five style blocks to become one. **Both regressions came from extras bolted on alongside** — `viewport-fit=cover` and this — each moving a property from one screen onto `<html>`, where it acquired reach nobody reasoned about. **A property that sits on `html` or `body` is load-bearing for the whole app. Moving one there is a change in its own right, not tidying, and it needs its own justification and its own test.**

- **⚠️ MY REGRESSION: `viewport-fit=cover` PUT EVERY SCREEN UNDER THE NOTCH. Shipped 2026-09-11 in `04ff342`, reverted 2026-09-12 04:10 in `5951871`.** David, on his iPhone the next morning: *"You screwed up all the top padding above the logo, and maybe other pages. The welcome screen and the landing page, the logo is at the top edge of the screen again. You can't even access the log-out at the top right on the one screen... I don't know what you did buddy."*
  - **ONE CAUSE, ALL THREE SYMPTOMS.** `viewport-fit=cover` extends the layout viewport **under the status bar and the home indicator**, and **nothing in this app uses `env(safe-area-inset-*)`**. So the status bar ate the top of every screen — the welcome screen's **72px** (the padding David had just spent three nudges tuning), the landing page's **40px**, and the dashboard bar's **18px**, which is why **Sign Out was physically untappable** — and the home indicator ate the bottom, which is the "bottom of the page pushed up" he could not quite name.
  - **⛔ I ADDED IT UNPROMPTED, AND THAT IS THE LESSON.** The ask was "make the white trim darker or bleed it off the screen." `theme-color` did that. **`viewport-fit=cover` was a nice-to-have I bolted on to get navy behind the notch, and it is the one thing in a three-commit evening that touched vertical layout.** A tag that changes where the page BEGINS is not a cosmetic extra, and it cannot be added without the safe-area padding that makes it survivable. **The scope creep, not the CSS, is what broke his phone.**
  - **⚠️ AND I COULD NOT HAVE SEEN IT.** There is no notch in the browser pane or in headless Chrome, so every measurement that evening was correct and none of them could reach this. **The whole class — safe areas, notches, home indicators — is invisible to every tool here and is only ever provable on David's actual phone.** The same shape as the 2026-08-28 Safari italic bug: when a change interacts with real device chrome, shipping it is the test, so do not ship one as a bonus.
  - **NO PADDING VALUE WAS EVER CHANGED, in either direction** — checked all three in source before touching anything, because "you screwed up the padding" sounds like a padding edit and was not one. **The values were fine and the viewport was lying about where the screen started.**
  - **✅ THE REST OF THAT EVENING'S DELETIONS RE-VERIFIED AS INERT, rather than assumed**, since he reasonably suspected more was wrong: all four `<p>` elements in the app set their own margins (so dropping the scaffold's `p { margin: 0 }` changes nothing), and there is no `<h1>`, `<h2>`, `<code>`, `.counter` or `#social` anywhere in `src/`. **`viewport-fit=cover` was the only vertical-layout change in the entire diff**, confirmed by reading the whole three-commit diff with comments stripped.
  - **⚠️ SAFARI CACHES THE PAGE SHELL, so a plain reload may not pick up a changed `<meta>`.** Tell him to hard-refresh when a fix lives in `index.html` rather than in the bundle.

- **🔁 THE FIVE PER-SCREEN `<style>` BLOCKS ARE ONE DECLARATION NOW, AND TWO LIVE BUGS WERE HIDING IN THE DRIFT. 2026-09-11, David: *"Go ahead and consolidate those five copies into one."*** Landing, AuthShell, PatientApp, Admin and Dashboard each injected their own shell reset. **They were never actually identical, which is the whole argument for this doc's one-declaration rule.**
  - **🐞 BUG 1, LIVE AND VISIBLE: THREE SCREENS ASKED FOR A FONT FACE THEY NEVER LOADED.** Landing, Admin and Dashboard requested **only two Fraunces instances** (`0,300` and `1,400`) while AuthShell and PatientApp requested all four. But **`Admin.jsx:30` and `Dashboard.jsx:32` render Fraunces at `fontWeight: 400` upright** — a face those screens never downloaded. With **`font-synthesis: none`** set in `index.css` the browser cannot fake it, so it substituted the nearest face it had, upright-300. **Measured: the dashboard greeting string is `297.23px` at weight 300 and `306.23px` at weight 400, and neither equals the generic serif at `278.03px`.** So the manager's greeting has been rendering ~9px narrower and a step lighter than designed, on every dashboard, for as long as those screens have existed.
    - **⚠️ THIS IS THE SAME ROOT CAUSE AS THE 2026-08-28 SAFARI ITALIC BUG, WHICH WAS ONLY HALF FIXED.** That entry correctly identified Landing's incomplete Fraunces request — and fixed it by stating `fontStyle: 'normal'` explicitly on the headline, which pinned the STYLE axis. **The WEIGHT axis was left broken, and on two other screens.** The explicit `fontStyle: 'normal'` is still there and still load-bearing; **the incomplete request underneath it is what is fixed now.**
  - **🐞 BUG 2: THE PATIENT APP DOWNLOADED AN ENTIRE UNUSED FONT FAMILY.** Its `@import` pulled **Cormorant Garamond, all six instances**, and **nothing in the app has ever referenced it** (grepped, zero hits outside the import). That is dead weight on the one screen patients open every single day. Gone. **DM Sans 300 went too** — loaded by two screens, and all ten `fontWeight: 300` declarations in the app turn out to be Fraunces.
  - **📁 THE FONTS NOW LOAD FROM ONE `<link>` IN `index.html`, NOT FIVE `@import`s.** Beyond being one declaration, it is faster for a real reason: **an `@import` inside a `<style>` that React injects cannot even begin downloading until React has mounted and rendered**, whereas a `<link>` in `<head>` starts with the document. `preconnect` to both Google font hosts added alongside. The union shipped is **Fraunces ×4 + DM Sans 400/500/600**, a superset of every face any screen asks for and a subset of what was being fetched.
  - **WHAT MOVED TO `src/index.css`:** `* { box-sizing: border-box }` (five identical copies) · **`overflow-x: hidden`** (Dashboard and Admin only, and it belongs to the shell — a sideways scrollbar is always a bug here, since the one wide thing, the roster, scrolls inside its own box) · **`-webkit-text-size-adjust: 100%`** (Dashboard alone, and nothing to do with that screen — it stops iOS inflating text on rotate) · and **tap feedback**, which was `button:active` in two screens and `a:active` in Landing, **three copies of one affordance across two selectors**, now `a:active, button:active` so a link and a button feel the same everywhere.
    - **⛔⛔ THE `overflow-x: hidden` GLOBALISATION WAS REVERTED 2026-09-12: IT SILENTLY BROKE THE MODAL SCROLL LOCK.** See the entry above. It is back on Dashboard and Admin only. (The note it replaced was true as far as it went — a hidden overflow does not blind `scrollWidth > innerWidth` — which is exactly why checking the one thing you thought of is not the same as knowing a change is safe.)
  - **✅ WHAT STAYED LOCAL, DELIBERATELY:** PatientApp's three `@keyframes` (`breathe`, `dotPulse`, `pulse`) and its placeholder/focus rules, and AuthShell's placeholder/focus rules. **Those are genuinely screen-specific and are not drift.** Both files now carry a comment saying what left and why. **⚠️ ONE REAL DRIFT WAS FLAGGED RATHER THAN SILENTLY CHANGED — the focus ring at `0.5` in AuthShell and `0.4` in PatientApp — and ✅ DAVID THEN CALLED IT: *"Make the focus rings consistent, use 0.5"*. See the entry below; qualifying that selector turned out to fix an accessibility bug on all three consent checkboxes.**
  - **✅ OBSERVED, not assumed:** all seven faces confirmed **declared and loadable** via `document.fonts.load()` — note `fonts.check()` is the WRONG test here, it reports whether a face has been DOWNLOADED on this page and returns true for fallbacks, which briefly read as "Cormorant is still loading" when it was not · the document now carries **only DM Sans and Fraunces** · upright-300 and upright-400 measured as **genuinely distinct faces** · Landing and `/onboard` re-rendered at 375px in light mode and **identical to before**, navy edge to edge, no horizontal overflow · the global rules confirmed in the cascade (`*` → border-box, `a:active, button:active` → 0.85) and an `<input>` confirmed inheriting border-box · exactly two `<style>` tags left on `/onboard`, one of them Vite's own dev injection · zero console errors · build and both prebuild guards clean · **JS bundle 487.37 → 485.81 kB.**

- **🎯 THE FOCUS RING IS ONE RULE AT ONE VALUE — AND QUALIFYING IT FIXED A REAL ACCESSIBILITY BUG ON ALL THREE CONSENT CHECKBOXES. 2026-09-11, David: *"Make the focus rings consistent, use 0.5"*.**
  - **THERE WERE THREE INCONSISTENCIES, NOT ONE.** (1) The alpha: `rgba(245,168,26,0.5)` in AuthShell, `0.4` in PatientApp. (2) PatientApp's `input:focus` was **missing the `outline: none`** that its own `textarea:focus` rule two lines above it had, so a focused text input there got the amber border AND the browser's default outline. (3) **The Dashboard's invite fields and therapist dropdown had no branded ring at all** and fell back to the platform default. All three are now one rule in `src/index.css` at **0.5**, covering `input`, `textarea` and `select`.
  - **⛔ THE CHECKBOX EXCLUSION IS THE POINT OF THIS ENTRY, AND IT WAS A LATENT BUG NOBODY ASKED ABOUT.** The old selector was a bare **`input:focus`**, which does not distinguish a text field from a checkbox — so it also caught the **three CONSENT CHECKBOXES** on Join, InviteJoin and Onboard. **A native checkbox has no author border for `border-color` to touch, so the only thing those rules actually did to it was `outline: none`.** A keyboard user tabbing onto the **Business Associate Agreement checkbox** — the one that records a clinic's legal agreement — got **no visible focus indicator at all.** The new selector excludes `[type=checkbox]` and `[type=radio]`, and they keep the platform's own ring.
  - **⚠️ `!important` IS LOAD-BEARING AND MUST STAY.** Every one of these controls carries an inline `border` shorthand from React, which beats a stylesheet `border-color` without it. **Proven rather than assumed:** applying the same declaration to a real `/onboard` input moved it from `rgba(245,239,228,0.12)` to `rgba(245,168,26,0.5)`.
  - **🤚 A HARNESS TRAP THAT ALMOST PRODUCED A FALSE FINDING, AND IT IS A NEW ONE FOR THIS DOC: `:focus` DOES NOT MATCH IN THE BROWSER PANE, BECAUSE THE PANE'S DOCUMENT NEVER HAS FOCUS.** `el.focus()` sets `document.activeElement` correctly and **`el.matches(':focus')` still returns false**, with `document.hasFocus()` false — and `tabs_select` does not fix it. **The first "before" measurement therefore read the UNFOCUSED styles of both elements and looked like evidence.** It was worthless. **To test a `:focus` rule, render it in headless Chrome with `autofocus`**, which does hold document focus; that is how the checkbox bug was actually proven, by rendering a focused checkbox under the old selector (no ring) and the new one (a clear ring) and looking at both. **Selector matching (`el.matches(...)`) is deterministic and needs no focus, so use that for "is this element covered".** This is the sixth way a harness has misled a session about this product.
  - **✅ OBSERVED:** the shipped rule read back out of the live cascade with `border-color` carrying `important` · the consent checkbox confirmed **excluded** and the text input **included** by selector match on the real `/onboard` · the `!important` declaration confirmed to beat React's inline border · **zero `:focus` rules left anywhere outside `src/index.css`** · build and both prebuild guards clean.

- **🧑‍⚕️ PT EMILY JOINED RIDGEPT AS A THERAPIST, 2026-09-10, AND DAVID WALKED THE WHOLE STAFF-INVITE FLOW END TO END: *"it went really well."*** Second therapist on the sandbox after PT Pete, and the first staff invite completed since the 2026-09-08 sign-in redirect and logo changes. **RidgePT staff: David (manager), PT Pete, PT Emily. David then assigned Miracle Peterson and Tommy Olsen to Emily ("I believe", his words), so the two therapists now split the roster. Sunday email volume 15 → 16.** Nothing to fix; recorded so nobody re-tests the staff door or "fixes" the count in code.

- **📏 THE WELCOME SCREEN'S GAP ABOVE "Good afternoon" WAS THE WINDOW'S LEFTOVER HEIGHT. Fixed 2026-09-08 (commit `c05e8fb`).** David: *"tighten up the space above 'good afternoon'."*
  - **THE MECHANISM:** `welcomeWrap` was `justifyContent: space-between` over `minHeight: 100vh`, so the three blocks (logo stack, greeting, buttons) were spread across the whole window and **every pixel of leftover height became the gap above the greeting**: measured **112.6px on a 390×844 phone and 190.6px in a 1280×1000 desktop window.** Shrinking the welcome logo from 208 to 132 handed those 76 pixels straight to that gap, which is why he noticed it the same day.
  - **NOW FIXED GAPS:** 26px above the greeting (40 until David tightened it by eye on 2026-09-12; measured identical at 375x812 and 1280x1000, so it still does not vary with window height, which was the whole point), 36px above the buttons, at every window height. **And 40px above the logo (2026-09-11, David: "needs some breathing room above it"): the wrap's top padding went 20 → 40, matching the landing page's shell, then **56 at his "step it up some", then 72 at "nudge it a little more"**, all the same afternoon by eye on the live screen.** The spread and the `100vh` are gone; a comment in `PatientApp.jsx` says why. The screen still fits a phone without scrolling (content ends at 695 of 844).
  - **✅ MEASURED through a harness built from the REAL style lines extracted out of `PatientApp.jsx`** (`styles` lives inside the component, so it cannot be imported; harness deleted afterwards, `git status` clean): 40 / 36 at both widths, no horizontal overflow, and the old layout re-applied in the same page to get the before numbers with every inherited style identical.
  - Also in this pass: the missed `App.jsx` splash size strip from `ddb7a05` committed as `f1895aa`.

- **🔗 THE WEEKLY PATIENT EMAIL SENT A SIGNED-OUT PATIENT TO THE SALES PAGE. Fixed 2026-09-08 (commit `ddb7a05`; Lambda deploy 21s, `UPDATE_COMPLETE`, bastion unchanged).** David, as James, tapped "Open GlowPT" in Monday's email and got "Bring GlowPT to Your Clinic". *"Shouldn't it take him to just a login page?"* Yes.
  - **THE CAUSE:** the patient button was `href="${APP_URL}"`, the site root. `Home` at `/` renders **Landing when signed out** and the app when signed in, so the link was right for a patient whose tokens were still good and wrong for exactly the person the email exists to nudge back.
  - **⛔ POINTING THE EMAIL AT `/login` ALONE WOULD HAVE MADE THE COMMON CASE WORSE.** `/login` had no signed-in redirect: a patient with live tokens would have been shown the sign-in form and asked for a code they did not need. So the two changes are a pair: **the email points at `/login`, and `/login` now sends anyone already signed in to `/`**, where `Home` routes by role. A comment in `Login.jsx` says why. The clinic button already pointed at `/dashboard`, whose `StaffRoute` already bounces a signed-out reader to `/login`; it needed nothing.
  - **✅ OBSERVED:** signed-out `/login` still renders the form with the logo at 132 · a throwaway ID token injected into `localStorage` (unsigned, a made-up sub) made `/login` hand off to `/`, which routed that clinic-less account to NoClinic as it should, then the token was removed and the form came back · the two console 401s were that token hitting `/me`, expected · `cdk diff` exactly one Lambda's code · deployed code pulled back from AWS carries `/login` on the patient button and `/dashboard` on the clinic one · dry run `{"queued":15,"sent":0}`.
  - **⏳ The real proof is Sunday's email**, tapped from a signed-out phone.

- **📐 EVERY HERO LOGO IS ONE SIZE NOW: `LOGO_SIZE = 132`, declared once in `AuthShell.jsx`. Shipped 2026-09-08 (commit `ddb7a05`).** David: *"the logo on the welcome screens is jumbo size compared to the other logos ... the one on the reflection screen and other screens look consistently the same and all good. Make them all consistent."*
  - **THEY RANGED 116 TO 208** across 21 call sites: welcome 208, landing 176, sign-in / join / no-clinic 140, check-in / reflection / splash 132, onboard 128, code screen 120, dashboard / admin / join-error 116. Each was a hand-tuned literal from the 2026-08-25 retune, which is how a family of one thing drifts into seven sizes.
  - **The reflection screen's 132 is the baseline**, because that is the one David named as looking right ("make X like Y" means Y is the baseline). **No screen passes `size` any more**; `LogoMark` defaults to `LOGO_SIZE`. The only explicit size left is `BrandLockup`'s 34px app-bar mark. **⛔ THE LANDING PAGE IS THE ONE EXCEPTION AND STAYS AT 176 (David, same day: "Keep the landing page logo at 176, that one was fine").** It passes `size={176}` with a comment; every other hero is `LOGO_SIZE`. **Two vocabulary points, because he asked:** the **landing page** is the public marketing screen ("Bring GlowPT to Your Clinic", signed out); the **welcome screen** is the signed-in patient's front door ("Good afternoon, Charlie"). The welcome logo was the 208 he called "jumbo"; it went to 132, then to 176 to match the landing page, and **on 2026-09-10 David judged 176 still too big on both front doors, so they share `FRONT_DOOR_LOGO_SIZE = 154`** (the midpoint), declared once in `AuthShell.jsx` beside `LOGO_SIZE`. Every inner screen stays 132. **Same day: the dashboard's page heading went Title Case, "Your Patients" (therapist) and "Clinic Overview" (manager), per the house label rule; David spotted the first, the second sat in the same slot.**
  - **✅ OBSERVED:** `/login`, `/` and `/onboard` on the dev server each measure the logo at exactly 132 with `glowpt-float` running; build clean; live bundle carrying the change. The patient and dashboard screens follow by construction, since they render the same component with no size of their own.

- **🌅 EVERY HERO LOGO FLOATS NOW, FROM ONE DECLARATION. Shipped 2026-09-07 afternoon (commit `d9fea71`).** David, with the welcome screen and the check-in screen side by side: *"The logo only bounces/floats up and down on the start check in page, not the other pages where the logo is. Can you make them all consistent."*
  - **THE CAUSE WAS THE SIXTH DUPLICATED-DECLARATION DRIFT IN THIS DOC.** The float was a private `@keyframes float` inside `PatientApp`'s style block plus a `logoFloat` wrapper div around ONE of that file's four logos. Every other screen rendered a plain `LogoMark`. Same shape as the favicon, `LogoMark` itself, the wordmark, the app bar and the mood scale: one copy gained a feature and the others never heard.
  - **NOW:** `@keyframes glowpt-float` lives ONCE in `src/index.css`; `LogoMark` in `AuthShell.jsx` applies it itself via a `float` prop that defaults to true; the welcome screen's wrapper and keyframes are deleted. **Amplitude 6px, period 4s, the welcome screen's originals.** `prefers-reduced-motion` switches it off.
  - **⛔ THE 34px APP-BAR MARK IN `BrandLockup` PASSES `float={false}` ON PURPOSE.** A bobbing icon beside a row of header text distracts rather than welcomes. **David confirmed this the same afternoon ("Don't make the app bar logo float too, you're right") and said he loves the floating logos.** Do not make the app-bar mark float.
  - **✅ OBSERVED:** `/login`, `/onboard` and `/` on the dev server all report `animationName: glowpt-float`, 4s, infinite, one running animation on the logo, zero console errors. **Motion proven by driving the animation's clock by hand** (`anim.currentTime = 0/1000/2000/3000/4000`): the logo's top edge read **40, 37, 34, 37, 40**. Live CSS on glowpt.app fetched back carrying `glowpt-float`.
  - **🪤 TWO HARNESS TRAPS, BOTH ALREADY IN THIS DOC AND BOTH BIT AGAIN:** the browser pane reports `visibilityState: hidden` and its animation clock stays at 0 even after `tabs_select`, so nine samples over 4s all read the same position; and **headless Chrome's `--virtual-time-budget` does NOT advance a CSS animation for a screenshot either** (three budgets, logo top row 55 in all three). **To prove motion, set `currentTime` on the `Animation` object and read the rect; layout reflects a transform whether or not anything paints.**

- **📅 THE WEEKLY EMAIL FIRES SUNDAY 6PM EASTERN NOW, YEAR-ROUND. Shipped 2026-09-07 afternoon; deploy 77s, `UPDATE_COMPLETE`, bastion unchanged.** David: *"Monday is too late to send a summary for the previous week. Our PT clinic is only open Monday to Friday ... a Monday patient could also have a 7am PT appt on Monday."*
  - **THE FIRE TIME IS THE WEEK'S CUTOFF.** `weekly_summary_rows()` counts each patient's check-ins in the 7 days ending at `now()`, and "needs attention" means zero in that window. So nothing in the database changed: firing Sunday evening makes the window Monday morning through Sunday evening, which is the week a clinic thinks in. **Saturday or Sunday morning would cut the weekend's check-ins off the count** and flag someone who was about to check in.
  - **6pm was David's pick** (7pm was suggested; later counts more of Sunday, earlier is friendlier to receive). A late Sunday check-in rolls into next week's count, which is harmless because the window rolls.
  - **⏰ IT IS AN EVENTBRIDGE *SCHEDULER* SCHEDULE NOW, NOT AN EVENTBRIDGE *RULE*, AND THAT IS WHY IT HOLDS 6PM IN WINTER.** A rule's cron is UTC only, so the old `cron(0 12 ? * MON *)` was 8am from March to November and **7am the rest of the year**, a drift the code comment had accepted. Scheduler takes `ScheduleExpressionTimezone: America/New_York`, so `cron(0 18 ? * SUN *)` is 6pm Eastern all year. It invokes the Lambda through its own IAM role (CDK made it: `SchedulerRoleForTarget-*`), and the old rule plus its invoke permission are destroyed. **The target input is an explicit `{"dryRun":false}`** rather than Scheduler's default event, so the intent is written down.
  - **THREE COPY LINES MOVED WITH THE DAY:** "You checked in N days **this week**" (it was "last week"; on a Sunday the week is still this one), "A fresh week **starts tomorrow**. A good time to check back in." (was "is here"), and "log today" stays, because a patient who has not checked in on Sunday is exactly who that line is for.
  - **NEW TEST** in `infra/test/infra.test.ts` pins `AWS::Scheduler::Schedule` at `cron(0 18 ? * SUN *)` / `America/New_York` / ENABLED **and asserts no `AWS::Events::Rule` remains**, because leaving the old one would fire the job twice a week. Suite 3 → 4, run before the deploy with `cdk diff`.
  - **✅ OBSERVED, not assumed:** `cdk diff` showed the rule and permission destroyed, the schedule, role and policy added, the Lambda code changed, **no replacements, bastion absent** · deploy `UPDATE_COMPLETE` in 77s with `BastionInstanceId` unchanged · `aws scheduler get-schedule` read back live: expression, time zone, ENABLED, correct target ARN, `{"dryRun":false}` input · `aws events describe-rule` on the old name: `ResourceNotFoundException` · **the deployed Lambda's code pulled back from AWS** carries "this week." and "starts tomorrow" and no old line · a real `dryRun:true` invoke against production returned `{"ok":true,"queued":15,"sent":0}`.
  - **⏳ THE REAL PROOF IS SUNDAY 2026-09-13 AT 18:00 EDT (22:00 UTC).** Expected volume is **16** as of 2026-09-10: Riverside 8, RidgePT 8 (5 patients, David as manager, PT Pete, **PT Emily, who joined 2026-09-10**), one of them (Miracle Peterson) to Felix's real address. **Every "Monday 8am" elsewhere in this doc is now historical.** Riverside's re-pristine should happen before Sunday evening now, not before Monday.
  - **The `timeout` command does not exist on macOS**; a backgrounded `aws sso login` piped through `tail` also buffers its URL until exit. Run it bare.

- **🚫 AWS CLOSED THE BEDROCK CASE, AND DAVID APPLIED TO AWS ACTIVATE THE SAME MORNING. 2026-09-07, 04:50 to 07:40, no code touched.**
  - **THE REPLY (03:21 EDT, case `178761116000010`, now Resolved):** the Bedrock team prioritizes model access for customers **enrolled in the AWS Startup Program or assigned a dedicated Account Manager**; all others "will receive Amazon Bedrock model access gradually as part of the standard rollout process." It recommends contacting Sales for Account Manager assignment or Startup Program enrollment, and enterprise support "for enterprise-scale implementations". **It never mentions the internal review Omkar promised on 2026-09-03, and it never says the quotas will be set to defaults.** Read it as that review having come back negative.
  - **❌ "WE'RE A STARTUP PROGRAM, RIGHT?" NO.** AWS Activate is an application, nothing in the record shows one, and the management account carried **$0 in credits**. The org was created 2026-08-02 as a plain Organization. **This is the "all other customers" bucket.**
  - **❌ THE FREE TIER NOTE DOES NOT APPLY.** `aws freetier get-account-plan-state` on the **management account** reports `accountPlanType: PAID`, `ACTIVE`; on the member account it reports "Missing data", which is why the second profile exists. So the instant lever the email dangled ("transition beyond the Free Tier") is not available, because it is already done.
  - **MEASURED THAT MORNING, so nobody re-runs it:** `converse` on `us.anthropic.claude-haiku-4-5-20251001-v1:0` still `ThrottlingException: Too many tokens per day`; `L-6120CF2D` applied **0**. Unchanged since 2026-08-23.
  - **✅ ACTIVATE APPLIED, FOUNDERS TIER, submitted ~07:30 EDT. CONFIRMED ON THE STATUS PAGE (screenshot from David, 07:45): status PENDING, date applied 9/7/2026, $1,000 requested, stage 2 of 3 "Pending verification", decision date blank.** The page is **startups.aws.com → AWS Credits → "AWS Activate Credits applications"** (Pending / Reviewed tabs), behind the Builder ID sign-in; it is the place to check before asking David whether AWS has answered. The flow, as it actually is on 2026-09-07 (verified by reading AWS's own guide, dated 2026-06-04, before sending David in): Builder ID → Activate profile (Company type **Startup**, not Self Employed) → tier → "Complete your profile" (job title **CEO**, there is no Founder option; founder = Yes) → "Describe product and funding" (**the product box is 250 characters**, so the long description was cut to 245) → link + verify the AWS account in the console → review and submit. **Turnaround stated on the confirmation screen: 5 to 7 business days; the guide says 7 to 10. Status at the Credit Application Status page on startups.aws.com. Outcome by email to `david@franklinaisolutions.com` (Outlook, not Gmail).**
    - **⛔ USE `david@franklinaisolutions.com` FOR EVERYTHING ON THAT SITE.** The form validates the email domain against the company website and crawls the website (`franklinaisolutions.com`) as part of review. A Gmail address anywhere on it would have slowed or sunk it. The Builder ID is a **third AWS credential** (root, SSO, Builder ID) and is in his password manager.
    - **⛔ LINKED TO glowpt-prod `463556655381`, NOT THE MANAGEMENT ACCOUNT.** The console confirmed "Account linking successful ... da****@franklinaisolutions.com and your AWS Account ID 463556655381". Credits apply there (and are shared org-wide under consolidated billing anyway); the Activate association, which is what the Bedrock team looks at, is what mattered in the choice.
    - **🪤 THE FORM TIMES OUT IN MINUTES.** David was bounced to the sign-in page from the profile step and got "Failed to save" on step 4 with no field named; both were the session expiring. Saved steps survive, the current one does not. **Fill each step in one pass with answers ready; do not stop to screenshot mid-step.** The prepared answers are in this session's scratchpad only; the substance is recorded above.
  - **▶ WHEN THE OUTCOME LANDS:** if approved, reopen the Bedrock case (or open a new Account case) **citing Activate membership and quoting AWS's own 2026-09-07 email** that names it as the expedited lane; and if the quotas then move, the "REMAINING STEPS" under PICK UP HERE apply unchanged. If refused, the remaining free levers are the "If NO" feedback link on the resolution email with Omkar's contradiction attached, and the Sales contact form. **The product-level levers (ship with the reflection off; de-identify the prompt) are David's and have still not been proposed to him as a decision; they come back on the table only if Activate fails too.**
  - **🏗️ FOR THE NEXT PRODUCT: Bedrock lives in ONE account and everyone else calls it cross-account.** Decided in principle with David 2026-09-07 after he asked whether this should unlock the whole org. The reduced starting quota is per account; a new account starts at zero again; Activate is one account, one time. So the unlocked account becomes the org's Bedrock hub, and a future product's Lambda assumes a role in it. Small CDK change when the time comes, not a new support case.
  - **✅ THE MONDAY HEARTBEAT FIRED AT 08:00 EDT AND ALL 15 LANDED, confirmed by David's inbox screenshots at 13:30.** 14 in his inbox (RidgePT summary to ridgeptmanager + ridgeptptpete; Riverside summary to him + samtorres; 10 patient emails: grace, chris, maria, james, linda, robert, ridgeptcharlie, ridgepttimmy, ridgepttommy, ridgeptdanny) plus Miracle Peterson to Felix's real address = 15, exactly as predicted. Content correct: RidgePT 5 of 5 (100%), Riverside 5 of 6 (83%, 1 needs attention), James got the no-check-ins variant as seeded, real logo and warm amber throughout, "1 day" singular on Tommy and Danny. **First Monday the heartbeat has covered two clinics.**

- **🔤 THE ROSTER SORTS ALPHABETICALLY BY FIRST NAME. Shipped 2026-09-06 evening (commit `21a3b2f`).** David: *"The roster needs to sort on patient name alphabetically by first name."* **It had been "most concerning first" since 2026-07-13**: flagged patients on top, then whoever had gone longest without a check-in. That was deliberate triage, and it is retired on purpose: the Low Mood / Inactive pills now sit under every flagged name, so trouble is visible wherever the row lands, and a manager looking for a specific person finds them faster in a predictable order. `byFirstName` in `lib/clinicData.js`: first word, `localeCompare` with `sensitivity: base` (case- and accent-insensitive), full name breaks ties. **Proven through the real `buildRoster`** (bundled with esbuild, `import.meta.env` stubbed, CJS output because the AWS SDK's requires do not survive ESM bundling): Álvaro · Charlie · Danny · james · Miracle · Sam Park · Sam Torres · Timmy · Tommy. **The manager's stat tiles ("Need attention") are unaffected; they count flags, not order.**

- **✉️ THE PATIENT INVITE EMAIL SAID "how you are getting on". It says "how you are doing" now. Shipped 2026-09-06 18:53 (commit `e8f1cc2`, deploy 21s, one Lambda, bastion unchanged).** David found it in a real invite (to `+ridgeptdanny`) after telling me earlier that "get on" had bothered him for days. **My search for it had missed, because I grepped "get on" and the copy said "getting on".** It was the ONLY British idiom in any user-facing string; the therapist version of the same email already said "how they are doing". **Verified by pulling the deployed Lambda's code back from AWS and grepping it, not by trusting the deploy log.** Resend on any pending patient row sends the new wording.
  - **▶ IDIOM IS NOT CAUGHT BY A SPELLING SWEEP.** The 2026-09-06 spelling pass (centre/colour/grey) found nothing in user-facing strings and was right; this sentence was spelled perfectly. When sweeping for British English, search for phrases too, and search for inflections ("getting on", not only "get on").

- **📐 EVERY PATIENT NAME IS STACKED AND THE PATIENT COLUMN IS 76px, DOWN FROM 104. Shipped 2026-09-06 evening (commit `84ecedc`).** Felix joined RidgePT as "Miracle Peterson" through a real invite on his own email (*"He said it worked well too!"*) and his name wrapped at 104 while Charlie and Timmy sat in a column of air. David's call: *"stack the first and last names ... let's be consistent and stack them all now. And tighten up that column now!"* This reverses the afternoon's "leave it pinned at 104" and is his to reverse.
  - **`PatientName` in `Dashboard.jsx` splits on whitespace: first word on line one, the rest on line two.** A one-word name is one line. A title stays with the first name ("Dr. Sam" / "Torres") so "Dr." never sits alone, reusing the same `TITLES` set as the greeting. **The cell is a flex COLUMN now** (name stack, then any flag pills under it, all centered), not the old wrapping row.
  - **THE WIDTH IS THE WIDEST WORD OR PILL, MEASURED with the real font against every real name on both clinics:** "Peterson" 63px, "Bennett" 56, "Dr. Sam" 53, "Tommy"/"Miracle" 52; pills "Low Mood" 74, "Inactive" 62. **So the pill sets it, at 76.** Re-measure before widening; the header "Patient" is 40.
  - **🪤 THE PILLS WERE 34px TALL AND NOBODY HAD NOTICED.** `s.pill` stated no `lineHeight`, so an 11px pill inherited the body's ABSOLUTE 26.1px, the exact trap the 2026-09-03 landing-footer entry warned would bite any small text that does not state its own. Now `lineHeight: 1.4`, 23px. Flagged rows dropped 108 → 97.
  - **THE SCROLL FLOOR IS COMPUTED NOW: `rosterFloor(cols)` = tracks + 12px gaps + 32 padding + 2 for the row border.** It had been a hand-typed number six times in two days (780, 858, 770, 680, 708, 688), each with a "recompute when a column changes" warning that had to be obeyed by hand. **Manager 688 → 660. ⚠️ Therapist 560 → 448**: the old 560 was never derived from anything and carried 84px of slack on the right of a therapist's roster. **Phone scroll 345 → 315.**
  - **✅ OBSERVED, not assumed:** the REAL `ROSTER_COLUMNS`, `s`, `Trend`, `NameFlags`, `PatientName` and `rosterFloor` driven in a temporary harness (temporary export line added, then removed; `grep` confirms it is gone) with all eleven real names plus both flagged rows and a titled double-flag edge case · tracks exactly 76/64/100/44/82/132/56 and grid exactly 660 · header-vs-cell center drift is a uniform 1px on every column, which is the row's own border · **no cell overflows its track** · therapist view 448 with the same zero drift · at 375px the page does not scroll while the table's box does (345 visible, 660 scrollable) · headless render inspected: names stacked, pills under names, column tight · build guard clean · live bundle byte-identical to the build tested.
  - **🔁 SAME EVENING, ONE MORE PASS (commit below): EVERY PILL IS ONE FIXED SIZE, 60×20, AND THE COLUMN DROPPED AGAIN 76 → 66.** David: *"Why are the pills/flags under the names different sizes? The smaller one is better, maybe a little smaller even. Make them the same."* They were sized to their text, so "Low Mood" ran 12px wider than "Inactive" and two flagged rows read as two different badges. Now `width: 60`, `boxSizing: border-box`, centered, at 10px/600 ("Low Mood" text measures 49 inside 52). **With the pill no longer the widest thing, the widest WORD sets the column: "Peterson" 63 → 66.** Grid 660 → 650. **⚠️ A longer flag word than "Low Mood" will not fit in 60; re-measure before adding one.** Verified the same way: both pills 60×20 with text fitting, tracks 66/64/100/44/82/132/56, no overflow, uniform 1px border drift, no page scroll at 375.
  - **🔁 AND A THIRD PASS THE SAME NIGHT, BECAUSE DAVID ASKED THE RIGHT QUESTION: *"what happens when a name is longer than Peterson? the column width should adjust to fit the longest, right? Or will names start getting cut off?"*** With a fixed 66 track a single word wider than the column would neither wrap (a word cannot break) nor clip; **it would spill sideways into the Avg Mood column.** Nobody on either clinic triggered it, but a Vandenberg would have. **My defect from the same evening, fixed in commit `514d5d1`.**
    - **THE COLUMN NOW MEASURES ITSELF: `patientColumnWidth(names)`** measures every stacked unit of every name on the roster (a word, or a title + first name that `splitName` keeps together) on a canvas in the row's real font, takes the widest, adds 3px, and clamps between a **66 floor** and a **140 ceiling**. **One number, shared by the header and every row, which is why this can auto-size when `fit-content()` could not: fit-content resolves per row.** Re-measured once `document.fonts.ready` resolves, because before that the fallback face is wider. `rosterFloor` reads the substituted width, so the scroll floor follows.
    - **PAST THE CEILING, `nameStack` HAS `overflowWrap: anywhere`**, so a 22-letter surname breaks onto two lines rather than spilling. Nothing is ever clipped.
    - **✅ MEASURED with three rosters through the real helper and components:** short names → 66 (grid 650); "Christopher Vandenberg" → **89** with the text ending inside its own cell and 12px clear of the next column; "Wolfeschlegelsteinhausen" → **140** and two lines, text right edge exactly at the cell edge, next column untouched. Header-vs-cell drift a uniform 1px in every case.
    - **🪤 THE TEMP HARNESS EXPORT SHIPPED IN `514d5d1` because the doc script that also removed it died on a quoting error before running a line.** Harmless (unused exports) and removed in the next commit, but: **do the cleanup in its own command, then the doc, not both in one script whose first syntax error aborts everything.**
  - **Pre-existing and left alone:** "8 days ago" wraps to two lines in the 82px Last Check-In column. It always did.

- **🙂 THE MOOD SCALE IS ONE COMPONENT, AND THE MANAGER LEGEND IS NOW THE PATIENT'S CHECK-IN SCALE, LITERALLY. Shipped 2026-09-06 evening (commit `2259176`).** David, with both screens side by side on his phone: *"Make the manager's legend look as nice as the patient mood legend. I asked you to do this before. Nothing is centered correctly or lined up... Why can't you make both legends look identical."*
  - **⛔ THE HONEST ANSWER TO "WHY CAN'T YOU" IS THAT THEY WERE TWO HAND-WRITTEN COPIES.** `PatientApp` had `feelingScale`/`feelingBtn`/`feelingNum`/`feelingEmoji`/`feelingWord`, `Dashboard` had `legendScale`/`legendCell`/`legendFace`/`legendNum`/`legendWord`, and every size, gap, padding and color was retyped in each. The 10:15 session even wrote "THIS IS A COPY, DELIBERATELY, DOWN TO THE BORDER" above the dashboard version, and it was still a different size (18px face vs 20, 22px number vs 36, 8px gap vs 10, 10px padding vs 14) because a copy made by hand is a copy that drifts. **This is the FIFTH duplicated brand/UI declaration this doc has caught drifting**, after the favicon, `LogoMark`, the wordmark and the app bar. The fix each time was the same and it is the same here.
  - **NEW FILE `src/screens/FeelingScale.jsx`.** One component. The patient passes `selected` and `onSelect` and it is tappable, with the amber fill, the scale-up and the glow; the dashboard passes nothing and it renders static, no cursor, with a `title` per cell. **Every size is a constant at the top of that file and nowhere else.** Both screens deleted their five local styles.
  - **⛔⛔ THE FIRST CUT (commit `2259176`) GOT THE DIRECTION BACKWARDS, AND HOW IS THE LESSON.** David's sentence had two halves: *"make the manager's legend look as nice as the patient mood legend"* and *"make the number smaller ... and the face should be bigger."* I treated the second half as the whole instruction, set face 30 / number 22 / padding 12, and shrank the PATIENT card down to that. The result: both screens now looked like the cramped manager legend he had just complained about, and the legend itself barely moved (its numeral was already 22). His reply: *"Why didn't the legend change?????????? You screwed them both up to the choice that I don't want."* **When a request names a reference ("as nice as X"), X is the baseline and every other adjustment is made INSIDE X. Do not average the two things together.**
  - **⛔⛔ AND THE SECOND CUT STILL MISSED THE ACTUAL COMPLAINT, WHICH WAS IN HIS FIRST SCREENSHOT ALL ALONG.** *"Nothing is centered correctly or lined up on the manager's legend."* Look at the five cards on a desktop: **the faces ZIGZAG, high, low, high, low, high, and so do the numbers.** Cause: "Hard day" and "Good day" fit on one line while the other three words wrap to two, and each cell was `justifyContent: center`, so the one-line cells had less content and pushed their face and number ~7px DOWN. **It hid on his phone screenshot of the patient scale only because at that width every word wrapped**, which is why the patient one looked "so nice" and the legend, with 2px less side padding so "Hard day" fit, did not. Two whole cuts went by adjusting SIZES while the row bounced. **Fixed in `FeelingScale.jsx` (commit below): cells anchor to the TOP (`flex-start`) and the word gets a fixed two-line `minHeight`, so all five faces sit on one line and all five numbers on one line at every width. Measured: identical `top` for every face and every number in both scales at 390 and at 1280.** ⛔ Do not put `center` back; a comment in the file says why.
  - **▶ THE LESSON, which is the third time this doc has recorded it in one form or another: WHEN HE SAYS "NOT LINED UP", FIND THE THING THAT IS NOT LINED UP IN HIS SCREENSHOT BEFORE CHANGING ANYTHING.** Measure the positions. I changed sizes twice and never once read the y-coordinate of face 1 against face 2.
  - **✅ THE SIZE HALF, FIXED IN COMMIT `7926219`, after one confirming question rather than a third guess: the card is the OLD PATIENT CHECK-IN CARD EXACTLY** (padding 14/6/10, 6px gap, 124px tall on a phone against the old 122), **with the face at 36px, the size the numeral used to be, and the numeral one step down at 26.** Word stays 10. Every size is a constant at the top of `FeelingScale.jsx` with this history written above it. **Do not shrink the card to make the legend "fit"; it fits.**
  - **THE LEGEND IS CENTERED AND CAPPED AT THE PATIENT CARD'S OWN WIDTH.** `legendWrap` is `maxWidth: 398` with 12px side padding (content 374 = the 430 patient shell minus 28 each side) and auto margins; the "Daily Feeling · ○ No check-in" head is centered above it. **Measured at 390px: both scales 334px wide, both centered at x=195, cells 59×112 on both. At 1280px: both 374px, centered.** The previous version was left-aligned in a 688 block with a 470 cap, which is what "nothing is centered or lined up" was describing, fairly.
  - **⚠️ THE 12px SIDE PADDING IS NOT COSMETIC.** The dashboard page pads `clamp(14px, 4vw, 28px)` and the patient screen pads 28; without it a phone renders the legend ~25px wider than the check-in scale and the two stop matching exactly where he compares them.
  - **✅ OBSERVED, not assumed (both cuts, same method):** the REAL component driven in a temporary Vite harness (`harness.html` + `src/__harness.jsx`, both deleted afterwards, `git status` showing only the three intended files) mirroring both real call sites, including the patient's two-element shell (outer 430, inner 28px padding), **which the first harness got wrong as one element and over-reported the patient width at 430 rather than 374; caught by checking the real `styles.screen`/`checkinBody` before trusting the number** · widths, centers, cell sizes and computed font sizes read back with JS at 390px · selected state proven (`aria-pressed`, weight 600, dark numeral, amber fill) and the dashboard copy confirmed non-interactive · headless Chrome renders at 500 and 1280 inspected · build guard clean.
  - **🪤 THE BROWSER PANE STOPPED PAINTING WHILE HIDDEN, so CSS transitions never ran and a click read back the pre-transition background.** Not a bug in the app. When the pane is hidden, measure with JS and take pictures with headless Chrome, which is the house technique anyway.

- **🌄 RIVERSIDE RE-PRISTINED 2026-09-06 15:07 FOR MONDAY'S HEARTBEAT — AND THE SEED SCRIPT DESTROYED EIGHT LOGINS ON THE WAY. Fixed in the same session.** David: *"Please re-pristine riverside now. it'll be firing tomorrow."* Before: Riverside's last check-in was **Aug 31**, 4 check-ins in 7 days. After: **23 in 7 days, last one today.** Verified by the same snapshot query before and after, not by the script's own output.
  - **⛔ THE SCRIPT'S THIRD TRAP, AND A NEW SHAPE.** The first two (`activated_at` 08-26, `open_signup` 09-04) were columns the script SETS on the clinic it inserts. This one is **a column ANOTHER clinic sets, pointing at a user the script destroys**: `clinics.activated_by references users(id)` with **no on-delete clause**, and **RidgePT's `activated_by` was David's `besoulful@gmail.com` row** because he pressed Switch On for RidgePT in `/admin` on 09-04 as the platform admin. That same address is the Riverside manager demo account, which the script deletes and recreates. The delete was refused: `clinics_activated_by_fkey`, SQLSTATE 23503.
  - **🚨 AND THE REFUSAL CAME TOO LATE, BECAUSE THE SCRIPT DELETED COGNITO FIRST.** `resetDemo()` ran `deleteCognitoUser` for all eight Riverside accounts, THEN the DB delete, which failed and rolled back. So for about ten minutes **every Riverside login was gone from the pool, including David's, while all their database rows survived.** Confirmed by `list-users` (5 RidgePT accounts left, none of Riverside) and `select count(*) from users` (still 13). **A Cognito delete has no transaction to roll back into.**
  - **THE FIX, TWO PARTS, BOTH IN `scripts/aws-seed-demo.mjs`:**
    1. **It preserves `activated_by` the way it already preserves `platform_admins`**: capture `(slug, email)` for every clinic whose activator is a demo account, null those references inside `clearDemoRows()`, and restore them by email against the NEW ids after seeding. Run output now shows `preserving activated_by: ridgept <- besoulful@gmail.com` and `activated_by restored on 1 clinic`.
    2. **A PRE-FLIGHT REHEARSES THE DB DELETES INSIDE `begin … rollback` BEFORE ANY COGNITO CALL.** If any constraint will refuse the delete, it refuses while nothing has been destroyed. The three clearing statements live in one function so the rehearsal and the real run cannot drift. **This is the change that would have turned today's ten-minute outage into a clean refusal.**
  - **▶ THE GENERAL RULE THIS LEAVES: when a script touches two systems and only one can roll back, rehearse the one that can BEFORE touching the one that cannot.** Same lesson as the 09-06 Remove ordering (login first, then rows, so every step is re-runnable), arriving from the other direction.
  - **⚠️ WHY THIS CANNOT RECUR IN THE SAME SHAPE, CHECKED RATHER THAN HEDGED: `clinics.activated_by` is the ONLY foreign key into `users` in `db/schema.sql` with no on-delete clause.** The other six all `cascade` or `set null` (`platform_admins.user_id`, `profiles.id`, `consents.user_id`, `checkins.user_id`, `access_log.actor_id` cascade; `staff_invites.invited_by` sets null). So the script can only meet this refusal again if someone adds a NEW restricting FK into `users`. **Grep `references public.users` in `db/schema.sql` whenever one is added, and if it restricts, teach the seed script in the same commit.**
  - **✅ OBSERVED:** before and after snapshots differ only in Riverside's numbers · RidgePT row identical (3 patients, 2 staff, 6 check-ins, last today) · 0 orphan profiles · 13 users · platform admin `besoulful@gmail.com` present · **RidgePT `activated_by` = `149844f8-…` = David's new `users.id` = his Cognito `sub`, all three read independently** · pool at 13 with all eight Riverside addresses back, status CONFIRMED · 45 Riverside check-ins total, feelings 2–5 inside the range constraint · tunnel closed, bastion `stopping`, credential file shredded. **⚠️ David must sign in again as `besoulful@gmail.com` — his stored tokens carry the old sub.**

- **🎯 HOW A PROSPECT GETS DEMOED, DECIDED 2026-09-06 (David). Three rules, and the third is the one that makes the other two work.**
  - David asked, fairly, why he needs both Riverside and RidgePT when a prospect will want to test as a patient and he re-pristines Riverside anyway. **The answer is that the clinic you invite people to cannot be the clinic you reset**: the seed script deletes the Riverside clinic row, check-ins cascade, profiles go `clinic_id = null`, and any prospect invited there becomes a live login pointing at no clinic, meeting **"You're not connected to a clinic yet"** the week after a demo to someone else. That is the V2.4 orphan class reintroduced against the people he most wants to impress.
  - **RULE 1 — RIVERSIDE IS THE ONLY DASHBOARD A PROSPECT EVER SEES.** Live, or screenshots of it. Reset before a demo. **Nobody real is ever invited to it.**
  - **RULE 2 — A PROSPECT WHO WANTS TO TRY THE PATIENT SIDE IS INVITED TO RIDGEPT.** As a patient they see only their own check-ins and reflections, never a roster. Their account survives every Riverside reset because nothing ever resets RidgePT.
  - **RULE 3 — RIDGEPT'S DASHBOARD IS NEVER SHOWN TO ANYONE OUTSIDE.** This is the rule that dissolves the problem. David's own objection was *"if they want to see their own data more often than not, I'll be in RidgePT all the time anyway, so we're back to square one"* — a shared roster shown to prospects would need to be presentable, and presentable means maintained, and Prospect Y would see Prospect X's name next to Charlie and Timmy. **So "show me my check-in on the dashboard" is answered by pointing at Riverside: that is what your clinic sees.** Six patients and a 30-day story prove the loop better than one row would.
  - **⛔ REJECTED: A TRIAL CLINIC PER PROSPECT.** It was proposed (one minute at `/onboard`, perfectly isolated, `/admin` doubles as a pipeline) and **David turned it down flat: *"too much nonsense... it would take way too much explaining around everything to have them test their own clinic before they actually signed up. We need to make this easy for us and them."*** Do not re-propose it. A prospect gets a link and a check-in, nothing more to explain.
  - **The only housekeeping this asks of him:** archive quiet prospects from RidgePT now and then, and even that is cosmetic, since nobody outside sees that roster. **RidgePT costs nothing to keep** — it is not "a second demo to maintain", it is a clinic that exists.

- **✅✅ COPY INVITE WALKED FROM DAVID'S IPHONE INTO IMESSAGE AND BACK OUT, 2026-09-06 14:00. This was the last unwalked thing in the product, and it proved three things at once.**
  - **1️⃣ iOS SAFARI ALLOWED THE CLIPBOARD WRITE.** This was the one real risk and it could not be tested from here: **Safari rejects a `navigator.clipboard.writeText` that does not sit inside the user's tap.** The handler was deliberately written so `writeText` is the FIRST async call, with only synchronous work before it. **That is now confirmed by a green "Invite message for Karen Palmer copied" on his own phone, not by reasoning. ⛔ Do not insert an `await` before the clipboard call in any future edit.**
  - **2️⃣ THE BARE-URL-AT-THE-END RULE PAID OFF EXACTLY AS INTENDED.** iMessage detected the whole 64-character token cleanly, lifted it out and rendered a tappable preview card. **A trailing full stop is precisely what would have made that detection messy.** He then tapped the card and landed on the real Join screen for Karen at RidgePT, so the message survives a round trip through iMessage intact.
  - **⚠️ WHAT HE SAW IS iMESSAGE-SPECIFIC. On Android or plain SMS there is no preview card and the full hex URL renders as text.** Still tappable, but it looks quite different, so do not treat the tidy card as how every patient will see it.
  - **3️⃣ 🎁 THE PREVIEW CARD IS BRANDED FOR FREE, AND IT VALIDATES THE SINGLE-SOURCE LOGO DECISION.** `index.html` has **no Open Graph or Twitter tags at all** (checked, not assumed), so iMessage fell back to `<title>GlowPT</title>` plus **`apple-touch-icon.png`** — the same file that is the browser tab, the home-screen icon and the weekly email logo. **The 2026-08-25 "one declaration only" call paid off in a surface nobody designed for.**
  - **⛔⛔ DO NOT ADD OPEN GRAPH TAGS TO MAKE THAT BUBBLE SAY MORE. THE GENERIC VERSION IS THE PRIVACY-CORRECT ANSWER AND IT IS CURRENTLY GENERIC BY ACCIDENT.** (**To be unambiguous, since "preview card" confused David and it is a fair thing to be confused by: this is the yellow bubble reading "GlowPT / glowpt.app" that MESSAGES generates when it detects the link. It is not part of GlowPT and appears nowhere on any GlowPT screen. Apple builds it by fetching the page.**) A richer card naming the clinic or the role would put **"this person is a physical therapy patient" onto a lock screen**, readable by anyone who glances at the phone, which is individually identifiable health information in exactly the way the standing no-identifier-in-a-URL rule exists to prevent. **A preview card is a lock-screen surface. Treat it like one.** If og tags are ever added for the marketing pages, **the invite route must be excluded.**

- **📱 COPY INVITE COPIES A MESSAGE YOU CAN TEXT, NOT A BARE URL. Shipped 2026-09-06 (commit `649e0e2`), and the question that produced it is better than the fix.**
  - David, having pressed the button: *"What am i supposed to do with the copy link button now? I clicked copy, should I send it in an email in which case i could have just hit resend."* **He had found a real overlap and half-answered it himself** ("if i was in a phone system too, i could text the link"). **He texts patients at work and says it is the preferred way of communicating**, which is the whole justification for this control existing.
  - **⛔ THE FAULT WAS NOT THAT THE BUTTON EXISTED, IT IS THAT TWO BUTTONS ON ONE SCREEN BEHAVED DIFFERENTLY.** Copy Link put a raw `https://glowpt.app/invite/<64 hex>` on the clipboard, while **Copy Message forty pixels below it handed over a finished sentence.** So sending an invite meant writing the explanation yourself every time, and pasting the bare link into a text would have shown a patient a wall of hex from a number they may not recognize. **Diagnosing it required reading what the handler actually copied, not what the button was called.**
  - **WHAT IT DOES NOT REPLACE, because the overlap he spotted is real and narrow:** if email is the channel, **Resend is strictly better** (one click, on-brand, from `no-reply@glowpt.app`). Copy Invite earns its place in exactly three cases: **a channel that is not email** (texting, the main one); **email being broken for that person** (a corporate filter or hard block, where Resend just fires at an address that already failed); and **a patient who is holding the link right now**, where Resend would kill the link in their hand.
  - **⚠️ IT BRANCHES ON THE ROW'S OWN `role`, NOT ON WHICH CARD RENDERED IT.** `PendingList` serves both invite cards, and **a therapist told about "a 30-second check-in you do each day between visits" would be sold the wrong product entirely.** `getInvites` already returns `role` per row, so the row's own truth decides. The two pitches mirror `inviteEmail()` in `infra/lambda/api/index.ts`; **keep them in step.**
  - **⚠️ IT ENDS WITH THE BARE URL AND NO TRAILING PUNCTUATION, DELIBERATELY.** Some SMS and chat clients **swallow a trailing period into the link they auto-detect**, producing a 404 for a link that was built correctly. Do not add a full stop and do not move the URL into the middle of the message.
  - **⚠️ NO EXPIRY IS CLAIMED, AND THAT IS AN ACCURACY DECISION.** The invite email says "expires in 14 days" because it is sent at creation; **this message can be copied on day 10**, so the same sentence would be a lie. `getInvites` uses `expires_at` only as a filter and does not return it, so there is no honest number to state. It says "This link is just for you", which is true whenever it is sent and is also the security property.
  - **`noticeLink` moved from `wordBreak: 'break-all'` to `overflowWrap: 'anywhere'`.** That slot used to hold a bare URL, where break-all was right; **it now holds a whole message, and break-all hacks ordinary words apart mid-letter.** Confirmed by looking: prose breaks at spaces, only the token breaks mid-string.
  - **✅ OBSERVED, not assumed:** the **real exported `PendingList` and `inviteMessage`** driven in a browser with a stubbed clipboard (harness deleted afterwards, `git status` clean) · **both role variants land on the clipboard byte-identical to what the fallback renders** · success notice green `rgb(155,176,106)`, failure red `rgb(231,154,146)` · the fallback shows the whole message · at a **real 375px** the page does not scroll, all three buttons fit, and the text stays inside its box · export guard and build clean · **the live bundle fetched back byte-identical to the build that was tested**, carrying all five new strings with `>Copy Link<` and `Invite link for ` both absent.

- **🔄 NEWEST CHECK-IN FIRST, A VISIBLE RING FOR AN EMPTY SLOT, AND THE PATIENT COLUMN AS TIGHT AS IT HONESTLY GOES. Shipped 2026-09-06 (commit `4352839`), three more from David.**
  - **1️⃣ THE LAST 3 CHECK-INS READ NEWEST FIRST.** They rendered oldest→newest, which is the convention for a sparkline — **but the column is "Last 3 Check-Ins" now, not a trend, and the first thing a manager wants is the most recent day.** `buildRoster` simply stops reversing, since `cs` is already sorted desc.
    - **⛔ THE PADDING HAD TO MOVE WITH IT, AND THIS WOULD HAVE BEEN A SILENT BUG.** `Trend` pads a patient with fewer than three check-ins, and **the ones they do not have are the OLD ones**, so `unshift` became `push`. **Reversing the order and leaving the padding alone would have put "no check-in" where TODAY belongs, on every new patient** — precisely the people a manager is watching hardest. Comments on both halves say they must move together.
  - **2️⃣ THE EMPTY SLOT IS A RING NOW, NOT A FAINT DISC.** David: *"the light gray color blends with the blue background too much."* **He was right and the number says how right: cream at 12% on the row background is a contrast ratio of 1.41, effectively invisible — which is why he had never noticed it in the first place.** Now a 1.5px outline at 45%: **3.80 on a row, 4.06 on the page behind the legend**, measured rather than eyeballed.
    - **The ring also says the right thing.** Every other slot in that column is a filled face, so **an outline reads as an empty slot rather than as a sixth mood nobody can name.**
  - **3️⃣ THE PATIENT COLUMN IS 124 → 104, WHICH IS AS FAR AS IT HONESTLY GOES.** It holds **"Grace Bennett" (100px)**, the widest name across both clinics, and nothing more.
    - **⛔⛔ AND THE REASON IT CANNOT AUTO-SIZE IS STRUCTURAL: EVERY ROW IS ITS OWN GRID.** The header and each patient row are separate elements sharing only a template string, so **`fit-content()` resolves PER ROW** — the header would size to "Patient" at 40px while Charlie's row sized to 49 and a flagged row to 124, and **the columns would stop lining up at all**. ⚠️ **An earlier entry blamed min-content on the wrapping flex cell; that was the wrong diagnosis for the right conclusion, and it is corrected here.**
    - **So a fixed width it is, and a fixed width must hold the longest name that must not wrap.** On a roster of Charlies and Timmys **there will always be air**, and the only way to remove it is to let real names wrap. **That is David's call, and it has been put to him rather than taken silently.**
    - Floor **708 → 688**; phone scroll **365 → 345**.
  - **✅ OBSERVED:** the harness drove the **REAL `buildRoster`** with real-shaped rows, and **the order was read back out of the rendered cells' `title` attributes rather than off the array** — Charlie's 5-then-2 renders `Feeling great | Hard day | No check-in`, and James's single check-in renders `Hard day | No check-in | No check-in`, so newest is left and padding is right. Grid exactly 688, patient track 104, zero header-vs-cell drift, no cell overflowing. At 375px the page does not scroll while the table's box does, and the scroller reaches both ends. Infra suite 3/3. Live bundle byte-identical to the build tested.

- **⚪ WHAT THE GRAY DOT IN THE 3-DAY TREND ACTUALLY MEANS, because David and I both had it slightly wrong (2026-09-06).** He said the "No check-in" legend item was no longer needed *"because it's required now to choose a face"*. **Half right, and the half that is wrong is the half that is on his screen.**
  - **THE DOT HAS TWO SOURCES, and requiring the mood only killed one of them.** In `buildRoster`, `isFeeling(c.feeling) ? c.feeling : null` turns a stored off-scale value into a dot — **that path is now dead**, guarded at three layers since 2026-09-05, and it is the one his reasoning removes. But `Trend` also does **`while (days.length < 3) days.unshift(null)`**: `last3` is `cs.slice(0, 3)`, so **a patient with fewer than three check-ins is PADDED with dots.**
  - **⚠️ IT IS LIVE ON RIDGEPT RIGHT NOW.** Charlie has 2 check-ins and his row reads `● 😊 😊`. **Every new patient shows at least one dot for their first two days**, which is exactly when a manager is watching them most closely. So the legend still has to explain it. **Do not delete the "No check-in" item on the grounds that the mood is required.**
  - **It is right that it is no longer a CARD**, though: the patient scale has five cells and a card with a dash where its number belongs is not a rating. It sits beside the "Daily Feeling" label as the exception to the scale.
  - **✅ THE HEADER NOW SAYS "Last 3 Check-Ins" (David's call, same day).** It said **"3-Day Trend"** while rendering `cs.slice(0, 3)`, the last three CHECK-INS regardless of date: identical for a patient who checks in daily, a fortnight apart for a sporadic one, with nothing on screen saying so. Deliberate since 2026-07-15 when the trend was cut from 7 faces to 3; the label was simply never revisited.
    - **⚠️ THE COLUMN IS NOW HEADER-BOUND RATHER THAN CONTENT-BOUND, and it cost width.** Its content is three 20px slots plus two 4px gaps = **68px**; the label needs **100** to stay on one line. **Floor 680 → 708, phone scroll 337 → 365** — the opposite direction to that morning's tightening, and worth stating rather than burying. Wrapping the header to two lines would have saved the width and cost a ragged header row among six single-line ones.

- **🔴 A DOT I PUT IN A CHAT MESSAGE READ AS A PRODUCT BUG (2026-09-06). Worth keeping, because it is the harness lesson wearing different clothes.**
  - I wrote **`● 😊 😊`** in chat to describe Charlie's trend row. **David's client rendered that `●` (U+25CF) as an emoji-style RED dot**, so he replied *"not sure what this whole dot thing is about. never saw the red dot before you've confused me."* **He had never seen a red dot because there is no red dot.**
  - **THE APP'S DOT IS NOT A CHARACTER AT ALL.** `s.noCheckin` is a CSS `<span>`: 12px, `border-radius: 50%`, `background: rgba(245,239,228,0.12)` — **cream at 12% opacity on navy, so a very faint gray circle.** Verified by reading the computed style off the rendered element, not from the source.
  - **⛔ THE LESSON: A GLYPH TYPED INTO CHAT IS NOT THE THING ON SCREEN.** This doc already records five ways a harness misled me about the product; **the conversation is a rendering surface too, and it has its own font stack and emoji substitution.** When describing a UI element, name it ("the faint gray circle") or show a screenshot of the real thing. Do not reach for a lookalike character.

- **✅✅ THE REQUIRED MOOD IS NOW WALKED IN PRODUCTION BY A PERSON (2026-09-06).** David: *"i walked through as a patient and tested not choosing anything and it wouldn't let me save, and bounced me up to the faces with the copy to choose first."*
  - **That is the whole 2026-09-05 guard proven on the live site**, where before it had only been driven in a browser against the real component. Nothing saved, no AI call, the prompt shown, and the scale scrolled back into view. **The deliberate choice not to gray out the button is vindicated too** — he met a live button and a reason, not a dead control.

- **🙂 THE LEGEND IS NOW THE PATIENT'S OWN CHECK-IN SCALE, THE ROSTER COLUMNS ARE RE-MEASURED, AND THE REFLECTION STOPS QUOTING THE SCORE. Shipped 2026-09-06 (commit `e0c07e1`), five things from David's iPhone.**
  - **1️⃣ THE LEGEND, THIRD SHAPE IN ONE MORNING, AND DAVID'S ANSWER WAS THE RIGHT ONE: *"make the manager dashboard faces legend line up with the patient check in legend which all fits across, no separate rows with faces."*** Equal cells across one row, **face above word, exactly like the five buttons a patient taps** (`PatientApp`'s `feelingScale`). Five labelled flex items was three rows on a phone; flowing text was two; **this is one at every width, because each cell wraps its own word rather than the row wrapping.**
    - **The principle worth keeping: make the legend look like the thing it explains.** Staff and patient already share the faces themselves (`lib/feelings.js`); now they share the layout too.
    - **⚠️ SIX CELLS, NOT FIVE.** "No check-in" is a value the trend genuinely renders, so it belongs **inside** the scale rather than trailing after it as a loose seventh item, **which is exactly what made both earlier versions wrap.**
    - **⚠️ `legendScale` IS CAPPED AT 560px AND THAT IS LOAD-BEARING.** `flex: 1` cells across the full 980 wrap spread the six faces so far apart they stop reading as one scale. 560 is roughly the density of the patient's own row in its 430px shell. `legendWrap` also carries `maxWidth: 680` + auto margins so **its left edge lands exactly on the Patient column's** (measured, both at x=31).
  - **2️⃣ THE SIGN-IN CARD LOST ITS SECOND SENTENCE** ("The same link works for everyone, so there is nothing to look up") **and now says we will EMAIL a code**, which is precisely what `/login` does. David checked that himself: *"it should say we'll email you a code, right."* Yes.
  - **3️⃣ ❓ "THE REFLECTION ANSWERED WITH A NUMBER, IS THAT NEW?" NO, AND THE ANSWER MATTERS BECAUSE IT WILL BE ASKED AGAIN.** `Feeling score: N out of 5` has been in the prompt **since the first V2 commit on 2026-07-12** (checked with `git log -L`, not guessed). **Nothing ever told the model to avoid the digit**, so it was always free to use it and sometimes did: a **2026-09-04 reflection read back "the 4-out-of-5" and this doc recorded it approvingly at the time.**
    - **He is right that it reads oddly.** "I see you're at a 3 today" hands a patient their own metric back, when **the word is the app's shared language everywhere else on the screen.**
    - **⛔ THE SCORE STAYS IN THE PROMPT DATA. Do not remove it.** The number is the clearest signal the model has for pitching TONE; what changed is the instruction, which now says to name the feeling in words and gives an example of each.
    - **⚠️ PROMPT-ONLY, SO A REQUEST RATHER THAN A GUARANTEE — and unlike the dash rule there is NO deterministic backstop, because "you're at a 3" cannot be rewritten into prose by a regex.** If it recurs the answer is a firmer prompt, not code. Said out loud here because this doc's own lesson is that an instruction a model can ignore is not a guarantee.
  - **4️⃣ THE ROSTER COLUMNS ARE RE-MEASURED AGAINST THE REAL RIVERSIDE NAMES, not the two short RidgePT ones.** *"there's so much extra space in the first column patient name, and the last columns too."* **The technique: give the real grid `max-content` tracks and read back what each column actually needs** — patient **172** (the flagged cell, which wraps by design), avg 57, trend 68, streak 36, last 77, therapist 107, archive 40.
    - **Trimmed only where the slack was real.** Therapist **170 → 132** (the select measures 107) and Archive **92 → 56** (the button is 40 and its header is blank) were **more than half air**. Patient **140 → 124**: every name that exists is **≤ 100px**, and **140 was not buying long names anyway** — "Christopher Alvarado" is 144 and wrapped at 140 exactly as it wraps now. **Avg, trend, streak and last were already within a few px of their own headers and were deliberately left alone.**
    - **Sideways scroll on a phone drops 427px → 337px**, and the floor is **680**.
    - **⛔ `fit-content(140px)` WAS TRIED FOR THE PATIENT COLUMN AND COLLAPSED THE TRACK TO 40px.** The cell is a wrapping flex container, so its min-content contribution is one word wide. **Tested rather than assumed** — it looked like exactly the right tool.
  - **5️⃣ ⚠️ TIGHTENING THE COLUMNS CREATED A NEW DESKTOP PROBLEM, AND NOT FIXING IT WOULD HAVE HALF-ANSWERED HIM.** Every roster track is a fixed px, so **nothing absorbs leftover space**: the row BOX stretched to the full wrap while its content stopped at 680, **leaving a quarter of every row empty — a gap the trimming had just made bigger.** `minWidth` now has a matching **`maxWidth` plus auto margins**, so the table is a compact centered block. **Auto margins resolve to 0 when the content is wider than the scroll box, so the left edge is never pushed out of reach** — verified by scrolling the container to both ends.
  - **✅ OBSERVED:** the **real** column spec, styles and `Trend` rendered with the **real Riverside roster** (long names, both flag pills). At 375px: grid exactly 680, widths 124/64/72/44/82/132/56, **zero header-vs-cell drift on ANY of five rows**, no cell whose content overflows its track, legend one row, **the page does not scroll while the table's own box does**, and the scroller reaches both ends. At desktop the legend's left edge lands exactly on the Patient column's. Infra suite 3/3. Live bundle fetched back **byte-identical to the build that was tested**, with the new strings present and the deleted sentence absent.
  - **🪤 THE HARNESS LIED TWICE AND MEASURING CAUGHT BOTH — the count is now five in three days.** Its first measuring rig reported **every column's widest cell as identical to its header**, because an `inline-flex` column stretches its children to the column width; the numbers were unusable and looked plausible. Then its hidden measuring block was `position: absolute` **with no offset**, so it sat in flow and made **the PAGE report horizontal overflow the app did not have.** Both were caught by asking "does this number make sense?" rather than by reading the code.

- **🔗 THE SIGN-IN LINK IS ONE CARD UNDER "Invite a Patient", AND IT COPIES AN INSTRUCTION RATHER THAN A BARE URL. Shipped 2026-09-06 (commits `944625c` then `6981ad3`). READ THE REASONING BEFORE MAKING IT PER-PATIENT OR MOVING IT BACK TO THE ROSTER.**
  - David, after seeing Copy Link on the pending rows: *"i want to always be able to access it for anytime i need to send it to the patient. that's what i was asking for when i started."* Fair, and the pending-only version genuinely did not answer it: **a patient who has already joined is not in the pending list at all.**
  - **⛔ THERE IS NO PER-PATIENT LINK TO COPY ONCE THEY HAVE JOINED, BECAUSE THE INVITE TOKEN IS CONSUMED AT THAT MOMENT.** So the button on the roster copies **`<origin>/login`**, the same string for every patient.
  - **⛔ MINTING EACH PATIENT A PERMANENT PERSONAL LINK WAS CONSIDERED AND REJECTED, AND THE REASON IS NOT "too much work".** For such a link to be worth more than `/login` it would have to resolve to WHICH ADDRESS IT BELONGS TO — and **"this email address belongs to a physical therapy patient" is individually identifiable health information.** It would then sit in a text message forever and be **logged by API Gateway every time it was opened**, against the standing rule that no identifier goes in a URL path or query string. **An invite link accepts that risk for 14 days because it has no choice. A permanent one has a choice.** The reasoning is written into `ROSTER_COLUMNS` above the column, not only here.
  - **`/login` is already the right destination and needed no change**: "Sign In to GlowPT / Enter your email and we'll send you a sign-in code", neutral between patients and staff, with "New patient? Use the link your clinic gave you." underneath. **Checked before assuming.**
  - Derived from `window.location.origin`, never hardcoded, the same reason the onboard page derives its own link. **A failed clipboard write puts the URL on screen instead of claiming success**, matching the pending rows. Quiet text button rather than a filled one: the roster is a table to read, and two solid buttons per row would shout over the data.
  - **📐 THE SIDEWAYS-SCROLL FLOOR MOVED 780 → 858, AND THE LAST 2px ARE MEASURED, NOT LUCK.** Columns total 740, plus 7 gaps of 12, plus 32 padding = **856** — but at exactly 856 **the ROW's 1px border, which the header does not have, eats 2px of its content box, and the only track that can give it up is the Patient column's `minmax`.** The header then sat 140 wide over a 138 cell: the 2026-09-05 header-drift bug in miniature. **The old 780 hid it by carrying 12px of slack.** With 858 the header and cell centers agree on every column, verified by measuring both rows.
  - **⚠️ THIS FLOOR HAS TO GROW WITH EVERY COLUMN ADDED.** Below the real width the grid squeezes the columns instead of scrolling, which quietly undoes the measured ceilings they were given.
  - **✅ OBSERVED:** the **real** `ROSTER_COLUMNS`, `s` and `Trend` rendered with fake rows (temporarily exported, then reverted with `git diff` proving `App.jsx` untouched) · grid measures exactly 858 with columns 140/64/72/44/82/170/76/92 · **header and cell centers agree on every column, which they did NOT at 856** · zero cells whose content overflows · at a real 375px **the PAGE does not scroll horizontally while the table's own box does** (343 visible, 858 scrollable), which is the entire purpose of that container · infra suite still 3/3 · live bundle fetched back **byte-identical to the build that was tested**.
  - **🔁 IT SHIPPED ON THE ROSTER ROW FIRST AND DAVID HAD IT REMOVED WITHIN THE HOUR, which is the more useful half of this entry.** *"Remove it from the patient row, it's extraneous there... I only ever wanted a url to send after they joined, and with maybe the instruction for accessing the check in."*
    - **⛔ A PER-ROW BUTTON IMPLIES A PER-ROW LINK.** Putting one identical string behind eight different buttons invites exactly the question the button cannot answer. **The neutrality that made the link safe is the same thing that made a per-patient control wrong.** One card, one link, said plainly.
    - **It sits directly UNDER the invite form because it does the OPPOSITE job**: inviting brings someone NEW in, this helps someone ALREADY in who cannot find their way back. Folding it into the invite card would put two jobs under one heading.
    - **⛔ IT COPIES THE INSTRUCTION, NOT THE BARE URL, AND THAT IS THE POINT OF IT.** *"Check in with GlowPT at https://glowpt.app/login. Enter your email address and we'll send you a code to sign in."* **Someone who has lost their way back does not only need the address, they need to know what happens when they get there.**
    - **The exact text is RENDERED ON THE CARD**, so what lands in the clipboard is never a surprise, and the URL inside it stays selectable for a manager who wants only that. **The button reports its own result** (`Copy Message` → `Copied ✓` / `Couldn't Copy`, resetting after 4s) rather than writing to a message slot — one control, and the text is already on screen, so **a failed write leaves the manager able to select it by hand.** That is the never-lose-the-link property the pending rows had to be given explicitly.
    - **⚠️ THE SCROLL FLOOR CAME BACK TO 770, NOT TO THE ORIGINAL 780.** The 780 was carrying slack that hid the row's 1px border entirely; the eighth column briefly made the fit exact and surfaced it. **Recompute whenever a column is added or removed.**
    - **✅ ALL THREE BUTTON STATES DRIVEN BY STUBBING THE CLIPBOARD rather than relying on browser focus** — success hands the clipboard a string **byte-identical to the text shown on screen**, failure shows the manual fallback, and it resets itself. **The unstubbed click genuinely failed in a hidden pane, which proved the fallback for real.** Roster re-measured after the column came out: grid exactly 770, **zero header-vs-cell drift**, no cell overflow, page does not scroll at 375px while the table's own box does.
  - **✅ WALKED BY DAVID 2026-09-06 13:20.** He copied the message from this card and sent it to a real new patient (Tommy Olsen), who used it to get back in. **Only the pending-row Copy Link is still unwalked.**

- **🔗 THE INVITE LINK NOW STAYS PUT, THE MOOD LEGEND SHRANK BY HALF, AND A RETURNING PATIENT IS NO LONGER SENT THE WRONG WAY. Shipped 2026-09-06 (commits `ef35916`, `cf7d52a`), all three from David reading his own screens on his phone.**
  - **1️⃣ THE LEGEND WAS SEVEN ITEMS IN A WRAP-HAPPY FLEX ROW** (a title, five labelled faces, the no-check-in dot), three rows deep on a phone above a table it only annotates. His words: *"they take up too much space and cluttered."* It is now **one run of faces with the two ends named**, and it is **flowing text rather than flex**.
    - **⚠️ BOTH HALVES WERE NEEDED AND ONLY MEASURING SHOWED IT. Collapsing seven items to three changed the height NOT AT ALL below 393px** — I had eyeballed a screenshot and called it two lines; it was three, exactly as before. **A flex row can only break at its gaps**, and the scale is 251px against 311px of available width, so it will not sit beside the 149px label or the 83px dot and takes a whole row either way. **As inline text it breaks between words instead.** Measured at 320/375/390/393/430/760/980: **118px → 62px everywhere below 430, 78 → 39 on a desktop**, no overflow at any width.
    - **⛔ SO `s.legend` IS DELIBERATELY NOT A FLEX CONTAINER. Do not "tidy" it back into one.** It also has to state its own `textAlign` (`s.page` centers the screen and inline text would follow it) and its own `lineHeight` (17px faces and a 12px dot share the line with 12px text, **and `body` sets an ABSOLUTE 26.1px that would otherwise inherit unchanged** — the trap recorded for the landing footer on 2026-09-03, biting a second time).
    - **"3-Day Trend ·" left the label** because the roster column header immediately below already says it. The five faces stay one `inline-flex` group so the ramp can never split across lines, and each keeps its `title`.
  - **2️⃣ THE INVITE LINK EXISTED FOR ABOUT TEN SECONDS AND THEN WAS GONE FOREVER.** It lived only in the panel rendered straight after the form, so it vanished the moment you dismissed it, invited someone else, or reloaded. **The link a manager most wants to send is the one for a patient who has not signed in yet — which is exactly the case the panel had stopped showing.** David: *"lets make a copy url button link unique to the patient that can always stay there and be copied at any time."*
    - `GET /clinic/invites` now returns each invite's **URL**, and every pending row carries **Copy Link** beside Resend and Cancel. **Safe: `staff_invites_select_clinic` scopes that table to the manager of its own clinic, so nobody else can read the row at all — and the token is not a credential anyway**, since `accept_*_invite` still requires the signed-in user's VERIFIED email to match.
    - **⚠️ COPY LINK AND RESEND ARE NOT THE SAME THING AND THE DIFFERENCE MATTERS: Copy hands you the CURRENT link, Resend mints a NEW one and KILLS THE OLD.** Reach for Copy when the email went astray, Resend when the link itself needs replacing. The resend handler re-reads the list before reporting, because until it does the copy link on the row below is stale.
    - **`inviteUrlFor(token)` is now the ONE definition of an invite's URL** in the Lambda, shared by the emailed link, the form's response and the pending row. Three places building the same string is how they drift.
  - **🪤 TWO THINGS FELL OUT OF DROPPING THAT PANEL, AND BOTH WOULD HAVE BEEN REGRESSIONS I INTRODUCED:**
    - **A FAILED SEND NOW REPORTS INTO THE CARD'S OWN MESSAGE SLOT, WHICH RENDERED EVERYTHING IN SUCCESS GREEN.** That slot already carried the form's validation errors in green too, so *"Enter the patient's name."* had always been the color of good news. **Notices now carry their kind** (`{ text, bad }`) and a problem renders in the Low-mood red, so the page has one word for bad.
    - **THE PANEL USED TO PRINT THE URL AS SELECTABLE TEXT, so a clipboard write that did not happen was survivable.** The old call was `navigator.clipboard?.writeText(...)` followed by an unconditional "copied" — **it could not report failure even if it tried**, the exact silent-failure shape this doc keeps relearning. Copy Link now awaits the write and, on refusal, **puts the link on screen** for the manager to copy by hand. **`navigator.clipboard` is absent entirely outside a secure context and can reject inside one; do not put the `?.` back.**
  - **3️⃣ NOTHING ANYWHERE EVER TOLD A PATIENT HOW TO COME BACK, AND THEIR ONE LINK ACTIVELY SENT THEM THE WRONG WAY. David: *"we left them hanging at that point."* He is right, and it was worse than hanging.** Grepped and confirmed before changing anything: not the invite email, not the check-in screen, not the reflection.
    - **THE DEAD END: `/invite/<token>` 404s once consumed, and said only "The link may have expired or already been used. Ask your clinic to send you a new one."** That link is **the only GlowPT address a patient has ever been given**, so it is what they bookmark and what they tap again next week. **They did not need an invite. They needed to sign in.**
    - **The screen now leads with Sign In and keeps "ask your clinic" as the quiet fallback** — the same ranking as NoClinic, by what the likeliest reader actually needs. **So ONE link, sent once, works forever: it joins them, then it signs them in.** That is the answer to David's *"can they use this same url to get to the sign in screen too"*: yes.
    - **⛔ THE SINGLE MESSAGE FOR UNKNOWN / EXPIRED / USED IS UNCHANGED, AND MUST STAY THAT WAY** — not being able to tell which is what makes a forwarded link harmless. **That is also why Sign In cannot pre-fill the address**: working out whose token this was is precisely the leak. One field, then the code they already know. The title says **"This link can't be used to join."** rather than "has been used", because we genuinely do not know which of the three it is.
    - **The invite email and the reflection screen now name `glowpt.app`**, the address a person can hold in their head. **The reflection line is shown only while `totalCheckins <= 3`**: a line that never leaves stops being read, and someone on their twentieth check-in has plainly worked out how to get here.
  - **📱 WHAT I TOLD DAVID ABOUT THE HOME SCREEN, because it will come up again: NOBODY CAN PUT AN APP ON SOMEONE'S HOME SCREEN. On iPhone, Apple allows no prompt at all** — Share → Add to Home Screen is the only route and a site may only *instruct*. **On Android, Chrome can prompt, but only with a `manifest.json`, and there is none: `public/` has no manifest and no service worker, so the V2 claim of "PWA install" is four Apple meta tags and nothing else.** A real install prompt was offered as the larger option and **David chose the smaller one**; it is not built. **⚠️ I do NOT know for certain whether current Chrome also wants a service worker before it will offer to install, and said so rather than promising it** — the 2026-09-05 lesson about inventing a third party's behavior.
  - **🙂 THE DAY-TO-DAY CASE WAS ALWAYS FINE, which is why this hid.** Tokens auto-refresh, so opening glowpt.app usually drops a patient straight into their check-in, and **the landing page already carries a Sign In button and a "Are you a patient?" line** for one who lands there signed out. The hole only opens when the session lapses or they change phone or browser.
  - **`PendingList` MOVED TO MODULE SCOPE.** A component declared inside another is a **new component type on every render**, so React unmounts and remounts it rather than updating it. It is also what let the **real** component be rendered for the visual check instead of a copy of it.
  - **✅ OBSERVED, not assumed:** both invite cards and the legend driven in a browser at a real 375px **through the REAL component and the REAL style object** (temporarily exported, then reverted and `git diff`'d to prove `App.jsx` was untouched) · copy, resend and cancel messages checked **by computed color** (green `rgb(155,176,106)`, red `rgb(231,154,146)`) with zero clipped elements and no page overflow · legend height measured at seven widths, before and after, **by swapping the old markup into the live element so every inherited style was identical** · the used-invite screen driven **live on the real route**, its Sign In confirmed to be a genuine link to `/login` · `cdk diff` showed **one Lambda's code, no replacements, bastion absent so the AMI pin still holds** · deploy `UPDATE_COMPLETE` in 62s with `BastionInstanceId` unchanged · `/clinic/invites` 401s with no token **and** with a forged bearer, public slug lookup still resolves, the removed open-signup route still 404s · **the live bundle fetched back and byte-identical to the build that was tested** · the used-invite screen then confirmed rendering on production with no unexpected console errors.
  - **▶ DEPLOY ORDER WAS API THEN FRONTEND, and it is not arbitrary.** An old frontend simply ignores an extra field; **a new frontend against an old API would have copied `undefined` to the clipboard and told the manager it had worked.**
  - **✅ THE SIGN IN SCREEN IS WALKED (2026-09-06 13:20).** David reopened Tommy's spent invite link and got the rebuilt screen: *"This link can't be used to join."* leading with **Sign In**, and "ask your clinic" demoted to the quiet fallback beneath it. **That is the dead end closed, proven by a person.** **⏳ Copy Link on a real pending row is still unwalked** — Tommy joined too fast to need it; invite a junk address and press Copy Link before they act.

- **⚠️ THE INFRA TEST SUITE WENT RED AGAIN ONE DAY AFTER BEING FIXED, AND THE REPEAT IS THE POINT. Fixed 2026-09-06 (commit `cf7d52a`); 3/3 green.**
  - **All three tests, failing at `705b9e9` before any of today's work** — confirmed pre-existing by stashing and re-running, which is worth doing before assuming a red test is yours.
  - **CAUSE: a synth-only stack has no AZ lookup, so CDK hands its VPC `dummy1a/dummy1b/dummy1c`.** Yesterday's Cognito interface endpoint is deliberately pinned to **`us-east-1b`**, so filtering the isolated subnets by that AZ found nothing and **every test died on `CannotCreateEndpointSubnets` before reaching a single assertion.**
  - **⛔ THE FIX BELONGS IN THE TEST, NOT IN `lib/api.ts`.** Making the pin fall back to "whatever AZ this VPC happens to have" would trade a **loud CloudFormation refusal for a silent deploy into an AZ where the endpoint does not exist** — which is the exact failure that rolled the stack back on 2026-09-06. The pin is right; the test simply has to model a real region, and now supplies the six real us-east-1 AZs as context.
  - **⚠️ THE 2026-09-05 ENTRY ALREADY SAID THIS SUITE IS NOT BEING RUN AS PART OF SHIPPING. IT WENT RED AGAIN THE VERY NEXT DAY, VIA A DEPLOY THAT SUCCEEDED.** Same lesson as the Monday heartbeat whose normal state looked broken: **an alarm nobody looks at is not an alarm.** `npx jest` in `infra/` takes eight seconds. **It belongs in the pre-deploy routine beside `cdk diff`, and this is the second consecutive day it would have paid for itself.**

- **📋 A NEW PATIENT INVITE NEVER APPEARED IN THE PENDING LIST UNTIL THE PAGE WAS RELOADED. Fixed 2026-09-06 (commit `7c4e3a3`), and it is the THIRD instance of one specific asymmetry.**
  - David invited a patient, got the confirmation panel with the link, and then **could not find the invite anywhere under "Invited (Waiting for First Sign-In)"** — so **Resend and Cancel were unreachable for the invite he had just sent**, which is exactly when a manager wants them. It is also why the cancel flow stayed unwalked.
  - **THE ROW WAS ALWAYS CREATED CORRECTLY.** `handlePatientInvite` simply never re-read the list, so `invites` stayed as it was until the next page load.
  - **⛔ THE STAFF HANDLER HAS ALWAYS ENDED WITH `setInvites(await fetchPendingInvites())`. THAT ASYMMETRY IS THE RECURRING BUG IN `Dashboard.jsx`, NOT A ONE-OFF.** The patient invite path keeps being built second and keeps missing a step the staff path already had: on **2026-09-05** it was the message slot (a patient invite reported into the Care Team card, two cards away), and now this. **WHEN YOU TOUCH ONE OF THESE TWO HANDLERS, DIFF IT AGAINST THE OTHER.** A comment says so at the call site.
  - **🪤 A DIAGNOSTIC DEAD END WORTH KNOWING: I READ THE INVITE TOKEN OFF DAVID'S SCREENSHOT AND QUERIED THE PUBLIC LOOKUP ROUTE WITH IT. IT RETURNED 404, AND THAT PROVED NOTHING.** A token is 64 hex characters; one mis-read character gives exactly the same 404 as a missing invite. **Do not treat an OCR'd identifier as evidence.** The row's existence is instead certain from the API contract: `invite_patient` commits the row BEFORE sending, and the frontend only renders that panel once the call returns an `invite_url`.
  - **⚠️ THE DATABASE WAS NOT CHECKED DIRECTLY, because `aws ec2 start-instances` on the bastion returned `InsufficientInstanceCapacity` on five attempts** — an AWS-side transient in that AZ, not a configuration problem. **If this recurs and a DB check is genuinely urgent, the options are to wait, or to change the bastion instance type (which is a stack change, and the AMI is deliberately pinned).**
  - **✅ VERIFIED IN A BROWSER against the real component**, with a stub mirroring the server's upsert-and-filter: inviting now makes the pending row appear **immediately**, carrying Resend and Cancel; **cancelling asks "Cancel the invite to X? Their link stops working immediately.", removes the row, empties the section and confirms.** Live bundle fetched back and byte-identical to the build tested.
  - **✅✅ AND THEN WALKED IN PRODUCTION BY DAVID THE SAME DAY** — invited `Tricky Dicky`, saw the pending row appear with Resend and Cancel, cancelled it, and watched the row go. **That closes the last unwalked item in the remove/cancel feature.** Note the empty list is a genuine server re-read (`handleCancelInvite` calls `fetchPendingInvites` again), so the invite is really gone rather than hidden locally.
  - **▶ FLAGGED, NOT CHANGED: the per-card confirmations ("Invite to X cancelled.") do NOT self-clear**, unlike the roster's `flash`. That is defensible rather than an oversight — **they already clear the moment you type in that card's fields**, and after a cancel the message is the only remaining evidence of what happened, since the row it describes has gone. Making them time out would mean a per-card flash slot each, because **a message must stay beside the control that produced it** (the 2026-09-05 rule) and the roster's `flash` renders in the wrong place for them.

- **🗂️ DISCHARGE IS NOW "ARCHIVE", AND A MANAGER CAN PERMANENTLY REMOVE A PATIENT WHO HAS CHECK-INS. Shipped 2026-09-06 (commits `d7b9ceb`, `7dee45e`).**
  - **▶ HOW THIS STARTED, AND IT IS THE LESSON: DAVID ARCHIVED FELIX, SAW ONLY "Restore", AND ASKED WHY THE FEATURE WE BUILT DID NOT EXIST.** It did, and it had worked: Discharge is the mark-inactive, and the collapsible list is the toggle. **Remove was hidden because Felix had a check-in, exactly as designed — but the screen said nothing about why.** An option deliberately withheld that gives no reason reads as an option that was never built. **That silent absence is the same shape as every other silent failure in this doc.**
  - **⚠️ HIS ACTUAL REQUIREMENT WAS ALWAYS THE SMALL ONE, AND IT GOT BURIED.** His words: *"all I ever wanted was a way to mark the patient inactive so that the manager's screen wasn't cluttered. then we came into all these other reasons."* Deletion is real but secondary. **When a request grows retention rules, guards and legal reasoning around it, check the original sentence still describes what shipped.**
  - **🔤 THREE WORDS, THREE MEANINGS, WHICH IS WHY IT IS "ARCHIVE".** **Inactive** = the AUTOMATIC flag (no check-in for 5+ days). **Archive** = the manager clearing their roster, reversible, nothing destroyed. **Remove** = permanent. One word was doing three jobs; "Discharge" read as a clinical decision when all it does is tidy a screen, and "Inactive" was already taken.
  - **⛔ THE UI WAS RENAMED, THE DATABASE WAS NOT.** The column is still `discharged_at` and the RPCs are still `discharge_patient` / `restore_patient`. Renaming live objects for a vocabulary change means a patch touching RLS policies, functions and tests for **zero behavioural gain** — the same call the invite functions got when they kept their `staff_*` names. **Commented in `Dashboard.jsx` and in the SQL so nobody later reads `discharged_at`, sees "Archive", and thinks they found a bug.**
  - **🔁 ARCHIVING NO LONGER ASKS FOR CONFIRMATION.** It destroys nothing and reverses in one click, so a dialog was friction for its own sake. It now shows **"Archived Charlie · Undo"** beside the roster. **Undo is the right pattern for a reversible action; a confirm dialog is for an irreversible one.**
  - **⚖️ THE REASONING BEHIND ALLOWING REMOVAL OF A REAL PATIENT, because it will be questioned again.** **HIPAA gives patients a right to ACCESS and AMEND, not a right to erasure** — there is no "delete my data" right, and state law typically *requires* PT records be kept for years. So a patient asking is not a patient entitled. **The clinic decides, which is exactly what the privacy notice already says.** And the manager IS the covered entity, so a manager-initiated deletion is the covered entity acting; GlowPT is the business associate carrying out their instruction. **Blocking it was us overriding the only party entitled to decide.**
  - **📋 ONE QUESTION ADDED TO THE COUNSEL LIST (cheap, and it decides the whole area): is a GlowPT check-in part of the clinic's DESIGNATED RECORD SET?** If it is a wellness adjunct nobody treats from, deletion is easy. But the product's pitch is that therapists read the roster and act on it, which argues it is, and then retention rules bite.
  - **🔑 "REMOVE" NEVER ACTUALLY REMOVED THE PERSON UNTIL NOW, AND NOTHING IN THE APP EVER DELETED A COGNITO ACCOUNT.** The only `AdminDeleteUserCommand` in the whole repo was in the demo seed script. So every removal wiped the rows and **left the login alive forever** — the person could still sign in, and their email address (which in this context says they were a physical therapy patient) sat in the pool. **A deletion that leaves the identifier behind is not a deletion.**
  - **⚠️ THE ORDER IS LOGIN FIRST, THEN ROWS, AND IT IS LOAD-BEARING.** `purge_target` returns the address under the SAME guards without changing anything; the Lambda deletes the Cognito user; then `purge_patient` deletes the rows with the guards re-applied inside that transaction. **Rows-first would mean a Cognito failure left records gone, a login behind, and NO WAY TO RETRY — the guards read the very row that had just been deleted.** Login-first makes every step re-runnable, because `UserNotFoundException` is treated as success.
  - **🪤 THE DEPLOY FAILED FIRST TIME, AND THE CAUSE IS WORTH KEEPING: AWS DOES NOT OFFER THE `cognito-idp` VPC ENDPOINT IN EVERY AZ.** It exists in **us-east-1b, 1c, 1d but NOT 1a**, and this VPC's isolated subnets are in 1a and 1b. CloudFormation refused with *"does not support the availability zone of the subnet"* and **rolled the whole stack back cleanly, nothing half-applied.** **Checking that the CDK enum existed was NOT the same as checking the service is available where we need it** — a quieter cousin of the 2026-09-05 "I invented an API's error behavior" lesson. The endpoint is now pinned to `us-east-1b`, with the re-check query in the comment. **SES is offered in all six AZs, which is why weekly-summary's endpoint could take the whole subnet group; do not assume a new one can.**
  - **💲 IT COSTS ~$7/mo** (one ENI, since only one AZ is eligible). The API Lambda has no NAT, so every AWS call it makes needs an endpoint. IAM is scoped to **exactly `cognito-idp:AdminDeleteUser` on exactly this pool** — do not widen it; the API Lambda is the internet-facing surface.
  - **🧾 THE REMOVAL IS NOW AUDITED, AND DELIBERATELY DOES NOT NAME THE PATIENT.** `purge_patient` wrote no `access_log` row at all before (the same gap flagged for the open-signup switch). It now records **actor, clinic, action** — but **not `target_user_id`**, because a pointer to the person we just deleted, combined with the 35-day RDS backups, re-identifies them and defeats the deletion.
  - **📖 WHAT "DELETED" HONESTLY MEANS, verified rather than assumed:** rows and login gone immediately; **RDS backups hold a copy for 35 days** (unavoidable, and backups are a HIPAA safeguard, not a bug — the confirmation says so out loud); CloudWatch keeps API error logs 6 months and a constraint violation *can* quote a value; CloudTrail keeps control-plane activity 6 years but **no check-in content**; already-delivered weekly emails and anything Anthropic retains are beyond our reach entirely. **`access_log.target_user_id` turned out to be written by nothing at all, so there is no residue there.**
  - **✅ OBSERVED, not assumed:** whole flow driven in a browser against the real component (**no confirm dialog on archive**, undo restores, the confirmation names the count, the button stays disabled on a partial name **and on the wrong patient's name entirely**, a case-insensitive trimmed match enables it, purge fires with the right id, no console errors) · patch rehearsed against a **pre-patch** database, re-run as a no-op, and diffed against a fresh build · applied to production with `GUARDS PASSED` then **verified by independent query** (owner `glowpt_auth`, PUBLIC denied, `glowpt_app` granted, `glowpt_weekly` denied, the check-ins refusal gone from the body, nothing moved) · `cdk diff` showed no replacements and the bastion absent, deploy `UPDATE_COMPLETE` in 92s with `BastionInstanceId` unchanged · both purge routes 401 with no token and with a forged bearer · **the whole chain run against real RidgePT data as the real `glowpt_app` role with the real manager's sub, INSIDE A TRANSACTION THAT WAS ROLLED BACK** (active patient refused, archived patient with a check-in removed, check-ins gone with them, audit row written without naming them), then confirmed nothing moved · **`cognito-idp.us-east-1.amazonaws.com` resolves to 10.0.3.87 from inside the VPC**, so the endpoint is genuinely carrying traffic · live bundle fetched back and byte-identical to the build tested.
  - **⏳ THE ONE THING NOT YET OBSERVED: a real `AdminDeleteUser` call.** The plumbing is proven (endpoint live, private DNS resolving in-VPC, SG ingress from the Lambda SG, IAM scoped) but **nobody has yet removed a patient through the screen**, which is the only thing that exercises the Cognito call itself. **When David does it, confirm the account is gone from the pool.**
  - **🪤 A REHEARSAL TRAP WORTH COPYING: THE FIRST PATCH WAS *NOT* IDENTICAL TO A FRESH BUILD, AND ONLY THE DIFF CAUGHT IT.** Owner and ACL matched; the **function bodies differed**, because I had written the patch's comments by hand rather than copying schema.sql's. Functionally identical, but a patched database would not have matched the schema of record. **Fixed by splicing the definitions verbatim out of `db/schema.sql` when generating the patch — do that from the start next time.**
  - **⚠️ AND A SMALLER ONE: `glowpt_app` CANNOT LOOK ANYTHING UP BEFORE `app.user_id` IS SET.** The first attempt at the rolled-back proof tried to find the manager's id as `glowpt_app`, and RLS correctly returned nothing, so the whole script ran against a null id. **Resolve ids as `glowpt_admin` first, then act as `glowpt_app`.** (Also: psql `:variables` do NOT interpolate inside a `$$ ... $$` block — pass them via `set_config` and read with `current_setting`.)
  - **✅✅ THE COGNITO DELETE IS NOW PROVEN, NOT JUST PLUMBED. David removed Felix through the screen on 2026-09-06 and his account is GONE from the pool** (12 users left, no Felix). That was the one thing the rolled-back transaction could not reach, and it is the first time a GlowPT removal has actually removed the person rather than only their rows.
  - **🖥️ TWO THINGS DAVID CAUGHT BY WATCHING THE LIVE SCREEN, both fixed the same day (commit `42f9bf7`):**
    - **"Felix was removed." SAT THERE UNTIL HE REFRESHED THE PAGE.** It is a confirmation of something that already happened, and the roster below it is the real proof, so it now clears itself after six seconds. **⚠️ ERRORS DO NOT, AND SPLITTING THEM IS THE WHOLE POINT: a failure the user did not happen to be looking at is a failure they never saw.** `flash` self-clears, `notice` stays. **The undo line is deliberately persistent too — it is an OFFER, not a report, and an offer that expires while you are reaching for it is worse than one that lingers.**
    - **ARCHIVE IS ITS OWN COLUMN**, not a link stacked under the therapist dropdown ("that looks crazy"). They shared a cell because both are manager-only, **which is a reason to share a ROLE, not a reason to share a CELL** — one sets who treats the patient, the other takes them off the roster. Header is blank on purpose: a column labelled "Archive" above a button reading "Archive" is noise. **The sideways-scroll floor moved 680 → 780 to match the real width**, or the grid squeezes the columns instead of scrolling and quietly undoes the measured ceilings.
  - **Tests 64 → 69.** **T53 was INVERTED**: it used to assert a patient with check-ins could NOT be removed and now asserts they can, with T53pre proving they really had history first and T53c proving the audit row exists and does not name them. T54pre/T54pre2 cover `purge_target` answering and changing nothing; T55b covers a non-manager being refused by it too.

- **🛡️ THE APP HAS AN ERROR BOUNDARY, AND A FAILED CHECK-IN SAVE NOW SAYS SO. Shipped 2026-09-05 (commit `7e0c5ea`), both at David's request after the `feeling = 0` crash exposed them.**
  - **⚠️ THERE WAS NO ERROR BOUNDARY ANYWHERE, WHICH IS WHY ONE BAD INTEGER READ AS A TOTAL OUTAGE.** Any exception thrown while React was rendering unmounted the whole tree and left a blank page with nothing on it at all, for the patient AND for us. `src/ErrorBoundary.jsx` now wraps everything in `src/main.jsx`.
  - **⛔ IT DOES NOT MAKE A CRASH HARMLESS AND IS NOT A SUBSTITUTE FOR FIXING ONE. It turns "the app is dead" into a message that says so, with a way out and the real error somewhere a person can read it.** It is a SINGLE top-level boundary, so a crash in the roster still takes the whole dashboard, just legibly. Per-card boundaries were deliberately not built.
  - **It sits OUTSIDE the router and `AuthProvider` on purpose**, so a crash in either is caught, **and that is exactly why it signs out through `lib/cognito` directly rather than through the auth context** — at the moment it renders, that context may be the broken thing.
  - **⚠️ SIGN OUT IS ON SCREEN, NOT ADVICE, AND THAT IS LOAD-BEARING.** If a crash is tied to the account's own data (today's was), **reloading lands straight back on it**, so Reload alone is an infinite loop. Signing out is the only exit.
  - **The technical detail is behind "Show Details", hidden by default.** A patient should not be shown a stack trace and this app handles PHI, so nothing technical appears in an incidental screenshot; but **a screenshot of a blank page told us nothing today and one line of this would have told us everything**, so it is one tap away. The full error and component stack always go to `console.error` regardless.
  - **💾 A FAILED SAVE USED TO BE COMPLETELY INVISIBLE.** `api.saveCheckin` failing was caught, `console.log`ged, and the patient was then shown their reflection **exactly as if it had saved** — a check-in could fall on the floor with nobody any the wiser. That is the house's recurring silent-failure shape, and David's stated bar is that nothing falls off.
  - **⚠️ THE RETRY RE-SENDS THE HELD PAYLOAD AND MAKES NO SECOND AI CALL.** The reflection is already on screen and already paid for; regenerating would cost another Anthropic call **and hand the patient different words for the same day**. The banner sits with the reflection rather than at the top of the screen, because what failed is the record of the day, not the reflection, and the copy says so.
  - **✅ VERIFIED IN A BROWSER against the real components, not mocks of them:** the boundary catches the exact `TypeError` today's bug threw, hides then reveals the detail, and logs the stack · a failing save shows the banner after **1** AI call · a retry while still failing leaves the banner up, **still 1** AI call · a retry once it recovers saves feeling 4, clears the banner and keeps the reflection, **still 1** AI call · live bundle fetched back and byte-identical to the build tested.
  - **🪤 AND THE HEADLESS-CHROME 500px CLAMP CAUGHT ME AGAIN, EXACTLY AS THIS DOC WARNS.** A `--window-size=430` screenshot of the crash screen looked like the body text was clipped at the right edge. It was not: Chrome laid out at 500 and **cropped** the image. Measured in the browser pane at a real 375px, the text ends at 342 against a panel ending at 350 with **no horizontal overflow**. **Measure before believing a narrow screenshot; the trap is now three-for-three.**

- **🚨 ONE CHECK-IN STORED WITH `feeling = 0` BLANKED THE WHOLE CLINIC DASHBOARD. Found and fixed 2026-09-05, 3rd session (commits `10aab43`, `5a6a655`). THIS IS THE BEST WORKED EXAMPLE IN THE DOC OF THE HOUSE'S OWN SILENT-FAILURE DISEASE, SO READ THE MECHANISM.**
  - **THE REPORT:** David signed in as `ridgeptmanager` and `ridgeptptpete` and both "flash for a second, then go blank", on iPhone and desktop, across browsers. **Patient check-in worked fine.**
  - **THE CHAIN, and every link was a thing that looked correct:** the check-in screen let you submit **without tapping a face**, so `selectedFeeling` was `null` · the API did **`const feeling = Number(b.feeling)`**, and **`Number(null)` is `0`** · **`Number.isInteger(0)` is `true`, so the guard written to REJECT a missing rating passed it through** · `checkins.feeling` had **no range CHECK**, so 0 was stored · `buildRoster` averaged it, and the dashboard's guard was **`r.avg != null`**, which **0 passes** · `FEELINGS` is keyed **1-5**, so `FEELINGS[Math.round(0)]` was `undefined` and reading `.word` threw mid-render · **there is no error boundary, so React unmounted the whole tree.** Hence: sign in, one frame of dashboard, blank page.
  - **⚠️ IT HIT BOTH STAFF ROLES AND NEITHER PATIENT SCREEN, WHICH IS THE DIAGNOSTIC.** Felix was on PT Pete's caseload and a manager sees the whole clinic, so **one bad row took down every staff member who could see that patient.** The patient app was untouched because `monthSummary` already does `Math.max(1, Math.min(5, Math.round(avg)))` — **that clamp is the only reason this was not a total outage, and it is load-bearing.**
  - **▶ HOW IT WAS FOUND, because the technique is the transferable part.** Reading the code found nothing: it renders correctly for well-formed data, and a harness with plausible mock data rendered both roles perfectly. **Every failure path in the load is inside a try/catch, so an API error CANNOT blank this screen — which proves by elimination that it had to be a render crash on data the mock did not model.** So: query production read-only for the actual rows, feed those verbatim into the harness. It crashed instantly with `TypeError: Cannot read properties of undefined (reading 'word')`. **When code review and a mock both say "fine" and the user says "broken", get the real rows.**
  - **THE FIX IS THREE LAYERS, because the screen is not the guard:**
    1. **`src/lib/feelings.js` now owns `isFeeling(f)`**, the single definition of a valid rating, because `FEELINGS` is indexed by it directly. `buildRoster` filters the average through it and normalises an off-scale value in the 3-day trend to "no check-in". **Do not test `!= null` or `typeof === 'number'` on a feeling; both let a 0 through.**
    2. **The API stops using `Number()`** and requires an integer on the 1-5 scale. The old predicate accepted `null`, `''`, `false`, `'4'`, `0`, `6` and `-1`; all are now refused.
    3. **`checkins_feeling_range`, a named CHECK constraint**, declared identically in `db/schema.sql` and `db/patches/2026-09-05_feeling_range.sql` so a patched database and a fresh build produce the same object.
  - **✅ THE MOOD IS NOW REQUIRED, AND ONLY THE MOOD (David's call: "require it, that's the whole point of the app").** He spotted the wider hole himself: **nothing on that screen was required at all.** Movement and the note stay optional **because neither ever reaches the clinic dashboard or the weekly email** — requiring them adds friction to a 30-second check-in and tells the clinic nothing, and **a mandatory free-text box only teaches people to type "ok" to get past it, which is worse than empty** in the one field a therapist might read.
  - **⚠️ THE GUARD RUNS BEFORE THE AI CALL, DELIBERATELY.** The reflection is generated first and the check-in saved second, so validating afterwards would make a patient wait for a reflection and then lose the check-in to a rejection **they never see** (`saveCheckin`'s failure is only `console.log`ged). It also spends an Anthropic call on a check-in that was never going to be stored.
  - **⚠️ NOT A GREYED-OUT BUTTON, ON PURPOSE.** A dead control with no explanation reads as "the app is broken", which is exactly the report that started this session. The button stays live; missing the rating shows "Choose how you're feeling to save your check-in." and **scrolls the scale back into view**.
  - **🔍 FELIX'S OWN ROW ARGUED FOR REQUIRING THE MOOD.** `created_at` is NOT in the upsert's update list, so that row was his **original** check-in, not something that overwrote a good one. **He picked "Rest day" and wrote a note, and skipped only the rating** — which is why his reflection sounded real. The field that matters most is the easiest to miss, because it is a tap rather than typing.
  - **✅ OBSERVED, not assumed:** crash reproduced against the real RidgePT rows and confirmed gone with the fix · check-in screen driven in a browser at 375px (**blocked with no AI call and nothing saved**, prompt shown in the accent amber and scrolled into view, cleared on tap, then a real save at feeling 4) · patch rehearsed against a **pre-patch** database (refuses while a bad row exists and rolls back whole, applies clean once fixed, refuses 0 and 6, no-op on re-run) and the result **diffed against a fresh build, identical** · applied to production with `GUARDS PASSED` then **verified by independent query** (constraint present and validated, a 0 refused inside a rolled-back transaction, 50 check-ins untouched, range 2-5) · `cdk diff` showed **exactly one Lambda's code, no replacements, bastion absent so the AMI pin still holds** · deploy `UPDATE_COMPLETE` in 57s with `BastionInstanceId` unchanged · both live bundles fetched back and **byte-identical to the builds that were tested**.
  - **▶ DEPLOY ORDER WAS FRONTEND → API → DB, and it matters.** Each step only narrows what is accepted, so there is never a window where a live client sends something the server has started refusing. **Doing it the other way would have made real patients lose check-ins silently.**
  - **Tests 62 → 64.** T14b an off-scale feeling is rejected, dated **yesterday** on purpose so the one-per-day unique index cannot reject it first and make the test pass for the wrong reason; T14c a valid rating on that same day still inserts, so T14b cannot pass merely because nothing can be written.
  - **✅ BOTH OF THIS ENTRY'S FLAGGED-NOT-FIXED ITEMS WERE DONE THE SAME DAY at David's request — see the error-boundary entry above. Original text kept for the reasoning:** **a failed check-in save is invisible to the patient.** `api.saveCheckin` failing is caught and `console.log`ged, and the patient is shown their reflection as though it saved. Not made worse by this change (with the mood required, the only remaining rejections are network or auth failures that already existed), but it is the house's recurring silent-failure shape and David's stated bar is that nothing falls off. **Worth its own change.**
  - **⚠️ THE APP HAS NO ERROR BOUNDARY AT ALL.** Any render exception anywhere unmounts the entire tree to a blank page, with nothing on screen saying what happened. That is why a single bad integer read as a total outage. **Adding one is not a fix for this bug and was deliberately not bundled in, but it would turn the next one from "the app is dead" into "this card could not load".** Not yet proposed to David.

- **🗑️ CANCEL A PENDING INVITE, AND REMOVE A PATIENT ENROLLED BY MISTAKE — SHIPPED 2026-09-05 (commit `16873de`). David asked for "a way for a clinic manager to mark patients as inactive and/or delete them. And to toggle that view on/off."**
  - **⚠️ TWO THIRDS OF IT ALREADY EXISTED, AND SAYING SO WAS THE FIRST USEFUL THING.** **Discharge** on each roster row IS the mark-inactive (soft delete, reversible, check-ins kept, manager only, since V2.3 on 2026-07-15), and the collapsible **"Discharged (N)"** list IS the toggle. **It only renders once at least one patient is discharged (`discharged.length > 0`), which is why David had never seen it.** Left gated on purpose; showing "Discharged (0)" is clutter. **Do not build a second way to do either.**
  - **THE NAMING TRAP, worth knowing before anyone renames anything:** the roster already uses **Inactive** as an *automatic* flag for 5+ days without a check-in. A manual "mark inactive" would put two meanings of one word on the same screen. **That is why it is called Discharge.**
  - **✂️ THE OPTIONS WERE PUT TO DAVID AND HE CHOSE "purge mistakes only".** The framing that decided it: **removing a mistake is not the same as deleting a patient's history.** A test account, a typo'd address, someone enrolled by error has no records and clearing it is clean; a real patient's check-ins are a retention decision the clinic owns.
  - **⛔ `purge_patient` HAS THREE GUARDS, ALL IN SQL SO THEY HOLD WHATEVER A SCREEN DOES:** manager of that clinic; **the patient must ALREADY BE DISCHARGED**, so removal can never be one click from the roster even if a future screen puts a button there; and **ZERO check-ins**. The UI only offers Remove where there is no history, but **the UI is not the guard**. **Do not relax the check-ins rule without a conversation about retention** — many states require PT records be kept for years, and the BAA §5.4 already describes destruction as something that happens at termination, with a process.
  - **⚠️ `revoke_invite` CLOSED A LIVE HOLE, NOT AN INCONVENIENCE, AND IT WAS FOUND BY LOOKING RATHER THAN ASKED FOR.** A pending invite could not be cancelled at all: one sent to the wrong address stayed claimable for its full **14 days**, and whoever holds that address could accept it and land inside the clinic. **Re-inviting the right person mints a token for THEM and does nothing about the wrong one.** The Pending list now has Cancel beside Resend.
  - **⚠️⚠️ THIS ADDED THE ONLY DELETE RIGHTS IN THE APP.** `glowpt_auth` held select/insert/update and nothing else, deliberately, because until today nothing in GlowPT deleted anything. It now has **DELETE on `users` and `staff_invites` only**. **Postgres performs `ON DELETE CASCADE` with the table owner's rights, not the caller's**, so clearing a users row still cascades profiles and consents without granting delete on either — verified, not assumed.
  - **🪤 THE CATCH THAT WOULD HAVE REACHED PRODUCTION, AND IT IS A GENERAL ONE: A SECURITY DEFINER FUNCTION RUNS AS ITS OWNER, AND LOCALLY THAT OWNER IS A SUPERUSER.** The new functions were owned by whoever applied them. On the local test database that is a superuser, which **masked the missing DELETE grant completely — the tests passed for the wrong reason.** Adding them to the schema's `alter function … owner to glowpt_auth` block exposed the real permission surface. **Every new definer function must be added to that block in the same commit.**
  - **⚠️ A CORRECTION I MADE MID-TASK, RECORDED SO IT IS NOT REPEATED: I told David a delete would erase the audit trail. IT DOES NOT.** `access_log.target_user_id` carries **no foreign key**, and the actors are staff and admins, never patients — only `actor_id` cascades. The warning was wrong and was withdrawn before anything was built on it.
  - **A TEST FAILED FOR THE RIGHT REASON AND TAUGHT SOMETHING:** the first draft asserted `select count(*) from users` and died on `permission denied for table users`. **`glowpt_app` has no SELECT on `users` at all** — correct, and the function underneath had worked perfectly. **Assert through what the app role can actually see**; the rewrite proves removal by re-calling the function and getting "Patient not in your clinic", which rules out RLS merely hiding the row.
  - **Tests 53 → 62.** New: a patient cannot cancel an invite or remove anyone; a pending invite is cancelled and cancelling a missing one is **refused rather than silently succeeding** (the house's recurring silent-failure shape); an active patient is refused; a discharged patient **with** history is refused and survives; one without history is removed and is gone for real.
  - **✅ OBSERVED IN PRODUCTION (house rule: shipped means observed working):** patch rehearsed against a **pre-patch** database, re-run to prove it is a no-op, and the result **diffed against a fresh build** — owner, ACL and function bodies identical · applied with `GUARDS PASSED` then **verified by independent query** (owner `glowpt_auth`, PUBLIC cannot execute either, DELETE grants on exactly two tables, no data moved) · `cdk diff` showed two routes, two invoke permissions and the handler, **no replacements and the bastion absent, so the AMI pin still holds** · deploy `UPDATE_COMPLETE` in 57s · both routes 401 with no token **and** with a forged bearer · **then the whole chain run against real RidgePT data as the real `glowpt_app` role with the real manager's sub, INSIDE A TRANSACTION THAT WAS ROLLED BACK**: an invite created and cancelled, Charlie refused while on the roster, refused again once discharged because he has check-ins. Confirmed afterwards that nothing moved. Bastion stopped, tunnel closed.
  - **✅ WALKED BY DAVID 2026-09-06** — twice (Tricky Dicky, then Tiny Teflon). Nothing outstanding on cancel.
  - **🪤 A BASTION GOTCHA THAT COST A FEW MINUTES:** `aws ec2 start-instances` bundled into a compound command that later hangs **does not necessarily fire** — the instance was still `stopped` afterwards. **Start it in its own command and read the `CurrentState` back.**

- **🎨 THE APP BAR IS ONE `BrandLockup` NOW, AND THE DASHBOARD GAINED THE LOGO (2026-09-05, commit `a795d1d`, David: "Make both wordmarks at top left render correctly both with the logo which looks much nicer and consistent").** The dashboard bar had no logo; the admin bar did. Their labels also sat at **14px and 13px**.
  - **⚠️ THIS IS THE FOURTH TIME A DUPLICATED BRAND DECLARATION HAS DRIFTED** — after the favicon vs the touch icon, `LogoMark` declared in both `AuthShell` and `PatientApp`, and the wordmark across three screens. **So it is declared once**, in `AuthShell.jsx`, and both screens deleted their four local styles. Each of those styles was used exactly once, checked before removal.
  - `label` is optional so the dashboard degrades cleanly before the clinic loads: mark and wordmark alone rather than a stray divider. Settled on **14px**, the dashboard's value, since that is the bar people live in.

- **🕐 THE CHECK-IN SCREEN GOT THE LOGO, AND ITS DATE WENT BACK TO BEING A TIMESTAMP (2026-09-05, commits `7e74754`, `5f2e39c`, `d3ec076`).**
  - **The missing logo was NOT a regression** — the patient app has had exactly three since the AWS cutover (splash 120, welcome 208, reflection 132) and the check-in screen never had one. **Checked the count at that morning's commit and at the cutover before answering.** Now 132, matching the reflection screen, with the top padding dropped 56px → 20px so the header grows by 112px rather than 148.
  - **⚠️ THE DATE HAD NEARLY DOUBLED THE DAY BEFORE AND DAVID SPOTTED IT COLD:** 12px → 22px in the 2026-09-04 label sweep (`89c1905`), and an 11px uppercase eyebrow before that. It is now **`CARD_LABEL_SIZE` (15)**, not `LABEL_SIZE`. **The reasoning, so nobody "restores" it: the date is a timestamp, not a section label, and at 26 characters it is the longest string in that family — at 22px it rendered as a 304px full-width band of brand amber above the 32px question, which is the tier's own rule (a label must not outgrow what it introduces) failing in spirit while passing in arithmetic.** The tiers themselves are untouched.
  - **The year is gone** ("Saturday, September 5"): on a daily check-in it is noise. Display only; `dateStr` is used in exactly one place and nothing is stored from it.

- **🚨 `#root` NO LONGER CENTERS ANYTHING, AND THE BUG IT CAUSED WAS ONE I TALKED DAVID OUT OF (2026-09-05, commits `ae406bb`, `01b5740`, `86b443f`). THIS IS THE MOST REUSABLE FINDING OF THE DAY.**
  - **THE MECHANISM:** `src/index.css` set `text-align: center` on `#root` — Vite scaffold CSS every screen inherited **invisibly, nowhere near the screen it broke**. A plain text cell obeyed it; **a FLEX container did not, because text-align never moves flex items.** The roster's Patient and Therapist cells are flex, so they sat left while their headers sat center: **~115px adrift on Patient, ~240px on Therapist.** The columns in between looked fine only because header and cell were centered equally.
  - **⛔⛔ I SAW IT IN DAVID'S FIRST SCREENSHOT, FAILED TO REPRODUCE IT, AND TOLD HIM IT WAS MY MISREADING OF A JPEG. IT WAS REAL.** My harness rendered the table **without the page around it**, so there was no `#root` to inherit from. **When David reports something you cannot reproduce, the harness is the suspect, not his eyes.**
  - **THE FIX WAS STRUCTURAL, NOT A NUDGE.** Header and body cells were written out separately, which is what let them disagree; they now render from one **`ROSTER_COLUMNS`** list carrying each column's label, width and alignment. **Every column states its own alignment, and flex cells translate it through `FLEX_ALIGN` (`justify-content`/`align-items`) — writing it twice is exactly what caused the drift.**
  - **THE GLOBAL IS GONE, and it was checked rather than swept blind.** **AuthShell already centered itself** (login, join, onboard, no-clinic, code-verify, invite, splash) and **Landing does too**, so only **Dashboard, Admin and PatientApp** ever depended on it — they each declare `textAlign: 'center'` on their own top-level container now. **Behavior-preserving by construction:** every route renders exactly one of those, and each container covers all of its own branches. **David supplied screenshots of all three, which is the only reason it was safe to do rather than guess at.**
  - **⚠️ IT DOES NOT MAKE THE CLASS OF BUG IMPOSSIBLE.** A flex container still ignores an inherited text-align — that is CSS. What protects the roster is the one-declaration column spec. **The win is that centering is now visible in the file you are editing.** The full warning lives in `src/index.css`; read it before putting a global back.

- **📐 THE ROSTER IS REORDERED, MEASURED AND CENTERED (2026-09-05, commits `3674e47`, `ae406bb`, `01b5740`). Order is now Patient · Avg Mood · 3-Day Trend · Streak · Last Check-In · Therapist.**
  - **David: "the average mood is probably one of the most important things to see, move it right after patient"**, then **"the headers centered over their columns, and the contents of the columns centered"**, then Streak next to the trend. It also puts the average **on screen at phone width**, where the table scrolls sideways and it used to sit three columns out of reach.
  - **⚠️ HE THOUGHT THE ORDER HAD BEEN FIXED BEFORE. IT HAD NOT** — 2026-07-14 aligned the columns and gave Avg its mood face, and the order was untouched since 2026-07-13. Worth saying plainly rather than letting it stand.
  - **THE GAP WAS THE `fr` UNITS, NOT THE ORDER, and reordering alone does not close it.** The Patient column was `minmax(150px,1.6fr)`, and an `fr` grows to fill whatever is left, so at desktop width it took **264px to hold a ~60px name**. Every text column now has a **measured ceiling**, and **only Therapist may absorb slack, because it ends the row where leftover space reads as margin rather than as a hole.** Therapist is capped at 170 rather than left flexible: rendered both, and a flexible track floats the dropdown into the middle of the empty space.
  - **The ceilings are measured, not guessed** — widest real patient cell is a name plus one flag pill ("James Okafor [Inactive]" 167px), and the rest are the wider of the header label and the widest value. **Patient is capped at the longest real NAME, so a flagged patient wraps their pill to a second line: deliberate, it costs nothing to a patient who is on track and a taller row suits one needing attention.** Re-measure before widening any of them.

- **🪤 THREE WAYS A HARNESS LIED ON 2026-09-05, ALL LOOKING LIKE APP BUGS AND NONE OF THEM REAL. Worth keeping as a checklist, because two of them cost real time.**
  1. **It lacked the page shell.** No `#root`, so an inherited-CSS bug could not appear at all. See the entry above.
  2. **Headless Chrome clamps its window to a 500px MINIMUM.** `--window-size=390` lays out at 500 and the screenshot **crops** it, which reads exactly like horizontal overflow. **Measure `innerWidth` before believing a narrow-width screenshot.** The browser pane at an emulated width is the reliable route.
  3. **A crude CSS converter broke font stacks on their internal commas**, so the wordmark rendered as plain sans with no gaps. **A harness is code too, and a wrong one accuses the wrong file.**
  - **The technique that DOES work, and produced every good screenshot today: extract the real style objects and constants out of the source file, render those, and re-extract after editing so the picture comes from the real file.** Then measure rather than eyeball — header-vs-cell edges, text width via a `Range`, `scrollWidth` vs `clientWidth`.
- **❓ "WHY WAS FELIX SO HARD WHEN CHARLIE AND TIMMY WERE NOT?" — the answer, because a future session will ask it too (2026-09-05).**
  - **THE DIFFERENCE IS ONE ACT, NOT ONE FEATURE.** Charlie (Cognito 2026-09-04 13:48, walk-in link) and Timmy (2026-09-04 18:47, invite link) were **brand-new addresses that went through in a single pass**. Felix (2026-09-05 10:04) **abandoned his first attempt at the code screen.** `SignUp` takes the address immediately, so that one act left an account that **exists in Cognito and is invisible to the database** — a state nothing in the system had ever met, and the origin of every problem that followed.
  - **⚠️ IT WAS NOT CAUSED BY REMOVING THE QR AND WALK-IN SIGN-UP.** Checked rather than asserted: that change touched the dashboard, the `open_signup` route, NoClinic copy and the seed script; **it does not touch authentication or clinic attach.** And the walk-in link would not have rescued him either, because `join_clinic` ends at the same `profiles` foreign key that `accept_patient_invite` did. **The door was never the problem; the half-finished account was.**
  - **SIX PROBLEMS. FIVE PREDATE TODAY:** the missing identity row (parked as a rare edge case on **2026-08-18**), the email guard failing open (**09-04**), the re-attach failing silently (**08-22**), the closed loop between NoClinic and the invite link (**09-04**), and sign-in errors collapsed into one unactionable message (**08-22**). **Felix was simply the first person to take a path that reached them.**
  - **⛔ ONE WAS MINE, AND IT COST HOURS: the `ResendConfirmationCode` probe**, built on an untested assumption and shipped in `99afcc6`. It made every returning invitee's code unusable. **Separately, my scripted edit inside the QR-removal commit `c9e2f73` deleted six API exports and broke invite and onboard for about half an hour.** So the QR commit *did* cause harm, through my carelessness rather than through the feature change it carried. **Both are recorded above in full; neither should be softened in a later summary.**
  - **The private window is a clue worth keeping:** it worked because it had no stale session and made exactly one code request. **Every new code kills the previous one**, and a second tab or an earlier attempt is enough to invalidate the one being typed.
  - **▶ WHAT IS TRUE NOW:** a fresh address goes through in one pass as Charlie and Timmy did, **and an abandoned sign-up now recovers on its own** — the identity row gets created, the invite link works whether signed in or out, the failures say which failure they are, and Resend stays on screen. **The Felix path is no longer a dead end; it is just a longer one.**

- **✅✅ THE PATIENT INVITE FLOW IS PROVEN END TO END IN PRODUCTION, 2026-09-05. Felix is in.** David opened the invite link, signed in, checked in, got a **real AI reflection** (it referenced his rest day and his work, so not the fallback), and **appears on the RidgePT roster under PT Pete with a 1-day streak and today's check-in**. The dashboard reads 3 patients: Charlie, Timmy, Felix.
  - **This closes the day's whole thread.** Getting here needed **six** separate fixes: the missing identity row (`ensure_self`), the email guard failing open, the unconfirmed-account probe, the silent re-attach failure, the closed loop between NoClinic and the invite link, and the Cognito ordering regression. **Only the last three were visible from the outside; the rest were found by reading the code around them.**
  - **The house bar is met: shipped means observed working.** Everything above was proven at the database or the API during the day; this is the first time a person walked the whole thing.

- **✂️ THE DASH RULE IS NOW ENFORCED IN CODE, BECAUSE THE PROMPT DOES NOT HOLD. New file `src/lib/houseVoice.js` (2026-09-05, commit `63bbe1c`).** Felix's live reflection ended *"...what your body needs each day—you're doing great."* — **an em dash, in front of a patient, in production.**
  - **⚠️ THIS IS THE THIRD ROUND WITH THE SAME RULE AND THE ESCALATION IS THE POINT.** 2026-08-27: *"Do not use em dashes"* → the model substituted **a spaced hyphen and an en dash**. 2026-09-04: *"Never join two clauses with a dash of any kind, not an em dash, an en dash or a hyphen"* → it used **an em dash anyway**. **Each rewrite was better and none was sufficient.**
  - **⛔ THE LESSON, WHICH GENERALISES WELL BEYOND DASHES: AN INSTRUCTION IS A REQUEST, NOT A GUARANTEE. Anything that MUST always hold has to be enforced by something deterministic.** The 2026-09-04 entry got halfway there ("a prohibition a model can satisfy with a near-synonym is not a prohibition"); the full version is that a prohibition a model can simply ignore is not one either.
  - `stripClauseDashes` turns a clause-joining dash into a comma. **Hyphens inside words are untouched**, so `check-in` and `30-second` survive; only an em dash, an en dash, or a hyphen with whitespace around it counts as a joiner. Verified against six cases including the exact sentence Felix received.
  - **✅ THE PROMPT INSTRUCTION STAYS, DELIBERATELY.** A model that complies writes a better sentence than one corrected afterwards. This is the backstop, not the replacement. **Do not delete the prompt line as now-redundant.**
  - **Applied on the way IN and on the way OUT.** In, so the SAVED text is clean for the journal, for a clinician reading it, and for any later export. Out, so entries written before today read correctly as well.
  - Checked while there: the Lambda `FALLBACK` is clean on **both** `main` and `bedrock-ai-response`, and the remaining dashes under `infra/` are code comments and arithmetic (`total - active`), both exempt.

- **🚨 MY OWN REGRESSION: EVERY RETURNING INVITEE GOT A CODE THAT COULD NEVER WORK. Shipped in `99afcc6` and caught the same day; fixed in `b2bc787`.** David saw **"That code didn't work"** three times, with three **"Verify your new account"** emails.
  - **⚠️⚠️ THE FALSE ASSUMPTION, WRITTEN INTO A COMMENT AS THOUGH IT WERE FACT: "ResendConfirmationCode succeeds only for an unconfirmed account and throws InvalidParameterException otherwise." IT DOES NOT.** Tested directly against this pool afterwards: `resend-confirmation-code` for **Grace, `UserStatus: CONFIRMED`**, returned `CodeDeliveryDetails` and sent the email. **So the probe answered "unconfirmed" for EVERYONE.** Every person whose account already existed took the sign-up branch, and the `ConfirmSignUp` that followed **could never succeed**. The code in the email was real and was rejected every time.
  - **▶ THE DIAGNOSIS CAME FROM THE EMAIL TEMPLATE, WHICH IS WORTH KEEPING AS A TECHNIQUE.** Cognito uses **different wording per flow**: sign-in says *"Your authentication code is …"* (8 digits), sign-up verification says *"The verification code to your new account is …"* (6 digits). David's screenshot showed the **sign-up** template going to an account that `admin-get-user` reported as **CONFIRMED**, which is impossible if the probe worked. **The template names the branch taken; read it before reasoning about the code itself.**
  - **THE FIX IS ORDERING: ASK THE QUESTION OF SOMETHING THAT ANSWERS HONESTLY.** `InitiateAuth` now goes FIRST for an existing address; it succeeds for a confirmed account, and email-OTP sign-in also confirms an unconfirmed one. **Only if sign-in itself refuses with `UserNotConfirmedException`** do we resend and finish the sign-up. The clinic attach then runs from the frontend safety net (the pending key saved before the call), **which is what that net is for and which now works because `ensure_self` can create the identity row**.
  - **⛔ THE GENERAL LESSON, AND IT IS THE BIGGEST ONE OF THE DAY: I INVENTED AN API'S ERROR BEHAVIOR AND BUILT A BRANCH ON IT WITHOUT TESTING IT.** The morning's other two failures were caught by tooling; this one could not be, because the code was correct in every checkable way. **Nothing in a build, a lint, a type-check or a schema test can catch a false belief about a third-party API. Only calling it can.** When a decision turns on "this call fails in situation X", make the call in situation X first, against the real service.
  - **✅ A GENUINE POSITIVE FOUND IN THE SAME QUERY: `ensure_self` HAS RUN IN PRODUCTION.** Felix now carries a real `users` row and a bare `profiles` row where he had neither this morning. **The 2026-09-05 identity fix is proven live, not merely deployed.**
  - **A stray "verify your new account" email reached David's inbox for GRACE** from the one-off test that proved this. Harmless; the code was never used and her account is unchanged.
  - **⚠️ RESENDING AN INVITE KILLS THE PREVIOUS LINK, WHICH MATTERS WHEN DEBUGGING FROM A SCREENSHOT.** The token in David's earlier screenshot returned `invite_not_found` afterwards, which looks alarming and was not: he had pressed Resend, and `invite_patient` upserts with a fresh `new_invite_token()`. **When chasing an invite problem, confirm which token is current before concluding a link is broken.**

- **🔁 AN INVITED PATIENT WHO SIGNED IN FIRST WAS IN A CLOSED LOOP. Fixed 2026-09-05, commit `b1bc228`. This is the THIRD distinct way an invited patient could be stranded today, and the most complete one.**
  - David signed in as Felix at `/login` and got the plain **"You're not connected to a clinic yet"**, with a live unconsumed invite sitting in that inbox.
  - **⚠️ NOTE WHICH SCREEN HE GOT: the PLAIN one, not the "We couldn't finish connecting you" error added that morning.** That is diagnostic. `attachError` was null, so **no attach was even attempted** — which immediately rules out the morning's bug and points at the branch that chose to do nothing. **When two failure screens exist, which one appears is evidence; read it before theorising.**
  - **FAULT 1 — nothing tried to attach him, correctly.** Signing in at `/login` leaves **no pending key in localStorage**, so `loadProfile` falls through to the blind safety net, and that net **deliberately excludes patient invites** (`role <> 'patient'`, asserted by T37b) because **it cannot record consent**. Working exactly as designed, and a dead end.
  - **FAULT 2, THE WORSE ONE — the way out was also shut.** `/invite/<token>` began with `if (session) return <Navigate to="/dashboard" />`. So the invite link was **useless to anyone already signed in**: it bounced them to `/dashboard`, which for a patient with no clinic renders NoClinic again. **Fault 1 put him on that screen and fault 2 stopped him leaving it.** Neither is a crash; together they are a locked room.
  - **THE FIX: `InviteJoin` now HANDLES the signed-in case instead of refusing it.** Same name and consent step, but it **claims the invite directly with the session already in hand** (`acceptPatientInvite` / `acceptStaffInvite`) rather than emailing a code nobody needs. **Only `session && profile?.clinic_id` still redirects** — someone already attached has nothing to claim.
    - **⛔ THE CONSENT RULE IS UNTOUCHED AND MUST STAY THAT WAY.** The signed-in path still shows the privacy notice and still passes `CONSENT_VERSION`, so a consents row is still written. **The blind net still refuses patient invites.** The fix removes a redirect; it does not create a second consent-free door.
    - Signed in as a **different** address gets told so plainly, with a Sign Out, rather than being refused by the database with a message it cannot explain.
    - The fine print no longer promises a code to someone who needs none. **That is the same contradiction as the invite email, caught in a second place the same day.**
  - **NoClinic now LEADS with the way out.** "Been invited already? Open the link in that invite email" was **fine print underneath a big amber Sign Out button**, so the most useful sentence on the page was the quietest and the least useful action was the loudest. Sign Out is now a quiet link. **The general point: on a dead-end screen, rank by what the reader most likely needs, not by what is easiest to render as a button.**
  - **✅ VERIFIED by rendering all three states** against the live API with an injected session: signed in as the invited address (stays on the link, "Signed in as", consent box, **`Join RidgePT →`**, no code step), signed in as someone else (**"This invite is for someone else."** naming both addresses), and the NoClinic screen itself. Live bundle confirmed to carry both. **Felix's invite is still unconsumed**, so David can still walk it.

- **✉️ THE INVITE EMAIL CONTRADICTED ITSELF, AND A PATIENT COULD NOT TELL WHICH HALF TO BELIEVE. Fixed 2026-09-05, commit `91f3c01`.** David: *"the button says start checking in, but the text below says that we will email you a code. which is it?"*
  - **BOTH, IN THAT ORDER, WHICH THE EMAIL NEVER SAID.** The real sequence is: tap the button → land on `/invite/<token>` → **confirm your name and tick the privacy box** → tap **Send My Code** → *only now* is a code emailed → enter it → in. **Nothing is sent until the person acts on the page.** The fine print led with *"No password is needed. We will email you a code."*, which reads as though a code is already on its way and makes the button look like a lie.
  - **The fix is ordering, not wording:** *"You will confirm your name, then we will email you a code to sign in. There is no password to create. This link works only for this email address and expires in 14 days."* **A comment in `inviteEmail()` says the order is the point, so nobody "tidies" it back into a shorter sentence.**
  - **⚠️ THE GENERAL LESSON: a button names a DESTINATION and fine print describes a PROCESS, and when they are written separately they drift into contradicting each other.** Neither sentence was false. Read together they were unusable. **Read a CTA and its fine print as one paragraph, out loud, before shipping.**
  - The CTA also gained Title Case (`Start Checking In →`, `Accept the Invitation →`) per the house label rule; **"In" is a phrasal-verb particle so it capitalises even though it is short.** ⚠️ **Noted, deliberately NOT swept: the app's own buttons are inconsistent on this** (`Send My Code →` and `Invite Patient →` are Title Case; `Verify & sign in →` and `Create my clinic →` are not). That is a real drift and a separate decision.

- **✖️ THE INVITE RESULT PANEL HAD NO WAY OUT, AND ITS HEADLINE OVERFLOWED ON A PHONE (both fixed 2026-09-05).** The confirmation added that morning **sat there until you invited someone else or reloaded the page**, which is exactly what David hit.
  - It now has a **dismiss**, and **clears itself when you start typing the next person**, so the card can never show one patient's link above another's form.
  - **🪤 THE OVERFLOW WAS FOUND ONLY BY MEASURING THE TEXT RATHER THAN THE ELEMENT.** The headline ends in an email address, which has **no natural break opportunity**, so at 375px it ran past the panel edge and under the new ✕. The first check compared the head div's bounding box to the button and reported it was fine, **because the BOX respected its `paddingRight` while the TEXT inside it did not.** Measuring a `Range` over the text node showed `textRight 299` against a panel ending at `277`. **When checking whether text clears something, measure the text: `const r = document.createRange(); r.selectNodeContents(el); r.getBoundingClientRect()`.**
  - `overflowWrap: 'anywhere'` on `inviteResultHead` is load-bearing and commented as such. The link beneath it already had `wordBreak: 'break-all'`; **the headline was overlooked because on a desktop it fits on one line**, which is where it had always been looked at.
  - **✅ THE LINK ITSELF STAYS. David: "I want a unique patient link to be able to send, i like that idea."** It was considered for hiding behind a disclosure now that Resend exists, and that was **rejected on his instruction**. Its role has changed though: **with Resend covering "I did not get it", the link is the backup for when email is broken for that person entirely** (a corporate filter, a hard block), or for handing to someone standing at the desk. **It grants nothing on its own** — the database still requires the signed-in user's verified email to match the invite.

- **🧭 THE `/onboard` SLUG IS INTERNAL AND NEVER SHOWN. Signup is THREE fields. (2026-09-05, David: "yes get rid of that patient link line on onboard", commit `70bfe00`.)**
  - It was previewed as `Patient link: glowpt.app/join/<slug>` with an **Edit control inside that same line**. That preview existed because the link was how patients enrolled themselves; **with walk-in sign-up gone the link enrols nobody**, so the label promised something that does not happen.
  - **⚠️ REMOVING THE LINE REMOVES THE EDIT CONTROL WITH IT, so the slug became fully automatic, and that is what needed real thought.** Two consequences handled:
    1. **A collision had no field left to fix it in.** `findFreeSlug` now walks `riverside-pt`, `-2`, `-3` up to 25. **Two clinics genuinely sharing a name in different towns is ordinary**, and handing that back as an error to someone with nothing to change would be a dead end. **`getClinicBySlug` 404s when a slug is FREE, so a 404 is the success case in that loop** — easy to read backwards.
    2. **The resolved slug is pinned in state once the code is sent**, so a resend cannot claim a different slug than the first attempt. `sendCode` takes an override for the first send because `setResolvedSlug` has not landed in state at that moment.
  - **The sub-line still promised "a private link to share with your patients"** and now says they invite patients by email. **Third instance today of the same lesson: when a door is removed, sweep the copy that points at it.**
  - The remaining validation can only fire if the clinic name has no letters or digits at all, so it now says so ("Please use letters or numbers in your clinic name") rather than talking about a "clinic web name" the person can no longer see. **That jargon is what confused David on 2026-08-26; it is now gone entirely.**
  - **✅ VERIFIED AGAINST THE LIVE API, creating nothing:** a fresh name yields its plain slug, **`Riverside PT` resolves to `riverside-pt-2`**, `RidgePT` to `ridgept-2`, and `!!!` produces an empty slug that validation refuses. Form rendered and confirmed at three fields plus the consent box.

- **🛡️ THE BUILD GUARD NOW CATCHES TWO RUNTIME-ONLY CLASSES, AND THE SECOND ONE WAS FOUND THE SAME HOUR.** Removing the Edit control left `slugEdited` and `setSlug` referenced **inside a JSX onChange handler**: a `ReferenceError` on the first keystroke, with a **completely green build**. Same shape as the deleted exports earlier that day.
  - `scripts/check-namespace-imports.mjs` (run as `prebuild`) now does two things: resolve every `alias.name` against its module's real exports, **and** run eslint over `src/` failing on **`no-undef` only**.
  - **⛔ ONLY THAT RULE, DELIBERATELY.** `src/` carries **15 other lint findings** (`react-hooks/*`, `react-refresh/*`, one `no-unused-vars`) which are style, not faults. Failing the build on those is a **separate and much larger decision** and has not been made. **Do not "tidy" this into a full `npm run lint` gate without deciding that first** — and note `npx eslint .` reports **409 `no-undef` errors, ALL of them inside `infra/cdk.out*` build artifacts**, which is why the check is scoped to `src/`.
  - **Proven by breaking it on purpose in both directions**, then restoring. **A checker is code too:** the first version of the export scan missed `export async function` and produced six false positives, caught only by running it.

- **🚨 A SCRIPTED EDIT DELETED SIX API EXPORTS AND SHIPPED THEM TO PRODUCTION (2026-09-05). Broken in `c9e2f73`, fixed in `13c793f`, guarded in `57c53de`. Read this before making a bulk edit.**
  - **WHAT HAPPENED:** removing `setOpenSignup` from `src/lib/api.js` was done with a script that found the export, then scanned **backwards to the previous blank line** to catch its comment. There was no blank line between it and the rest of the RPC block, so the scan swallowed **`provisionClinic`, `joinClinic`, `getStaffInvite`, `acceptStaffInvite`, `invitePatient` and `acceptPatientInvite`** — the onboard flow, BOTH invite flows, and the re-attach safety net fixed hours earlier the same day.
  - **⚠️⚠️ IT PASSED THE BUILD, AND THAT IS THE STRUCTURAL PROBLEM, NOT THE TYPO.** Every screen does `import * as api from '../lib/api'`. **A namespace import resolves at RUNTIME**, so `api.invitePatient(...)` against a module with no such export is `undefined` and throws only when a person clicks the button. `vite build` is perfectly happy. Named imports would have failed the build; a namespace import cannot.
  - **HOW IT GOT PAST REVIEW:** after the edit the file was spot-checked by printing the lines **around** the edit point, which looked plausible. **That checks what remains, not what was removed.** The only sound check is a diff of the whole file against the previous commit. That is what found it.
  - **✅ GUARDED SO IT CANNOT RECUR: `scripts/check-namespace-imports.mjs`, wired as `prebuild` in `package.json`.** It resolves every `alias.name` call against the exports its module actually provides and fails the build. **It runs on `npm run build`, which means NETLIFY RUNS IT ON EVERY DEPLOY** — deliberately, because this project already has one suite nobody runs (the infra tests were red for two weeks), and an un-run check is not a check. **Proven both directions: clean on the real tree, and it catches this exact deletion when the export is removed again.**
    - ⚠️ Its first version had a false-positive bug of its own: the regex missed **`export async function`**, so it flagged six real `cognito.*` exports. **Caught by running it rather than trusting it.** A checker is code too.
  - **▶ THE RULES THIS LEAVES BEHIND.** (1) **After any scripted or bulk edit, diff the whole file against HEAD.** Reading the surrounding lines is not the same check. (2) **A green build says nothing about a namespace import.** (3) If the app is ever refactored to named imports (`import { invitePatient } from '../lib/api'`), the bundler enforces this for free and the guard becomes redundant. That refactor is worth doing and has not been proposed to David.
  - **Observed in the shipped bundle afterwards**, not just locally: all six routes present in the live JS, `Resend` present, `set-open-signup` and the QR absent.

- **📨 RESEND AN INVITE FROM THE PENDING LIST (2026-09-05, David asked: "should I be able to just click resend somewhere to make it easier").** Each row under "Invited (Waiting for First Sign-In)" now carries a **Resend**, for patients and staff alike, so nobody retypes a name and address the clinic already gave us.
  - **It reuses the SAME RPC as the form, which upserts.** So the person keeps their place in the list, gets a **fresh token**, and **the old link dies**. That last part is not a side effect worth hiding: it is how a link sent to the wrong address gets killed, which the 2026-09-04 entry already relied on.
  - Rendered by one `PendingList` component used by both cards, for the same non-drift reason as `InviteResult`.
  - **Verified at 800px and at a real 375px viewport, measured rather than eyeballed:** no horizontal overflow, and each button wraps onto its own line under its person.
  - **🪤 A SCREENSHOT TRAP WORTH REMEMBERING, hit twice today.** Headless Chrome at `--window-size=390` **clipped the page and looked like a layout bug** in a harness that had no `<meta name="viewport">`, and again even with one. The browser pane at an emulated 375px, plus reading `document.documentElement.scrollWidth` against `innerWidth`, gave the true answer both times. **For narrow-width checks, measure in the browser pane; do not trust a headless screenshot's width.**

- **🔒 WALK-IN SIGN-UP IS GONE. AN INVITE IS THE ONLY WAY A PATIENT JOINS. Shipped 2026-09-05 (David: "Let's get rid of the qr code all together now... Keep it simple and locked down best"), commit `c9e2f73`.**
  - **REMOVED FROM THE PRODUCT:** the Patient Sign-Up switch, the shareable `/join` link card, and the printable QR, plus the `qrcode` dependency and thirteen now-dead styles.
  - **⛔ LOCKED DOWN, NOT MERELY HIDDEN. Three layers, and the middle one is the point:** every clinic was set `open_signup = false` in production (**Riverside was still open**); **the `POST /rpc/set-open-signup` route and its handler are deleted**, so there is no live path to turn it back on; and `join_clinic` still reads the flag, so the database refuses regardless. **Proven by the difference between two status codes: the removed route now returns 404 while `/rpc/invite-patient` returns 401. A route that is gone and a route that merely wants a token are not the same thing, and only the status code tells them apart.**
  - **✅ KEPT DELIBERATELY, DO NOT "FINISH THE JOB" BY DELETING THESE:** the `clinics.open_signup` column, the `join_clinic` gate that reads it, `set_clinic_open_signup` in the database, and the `/join/<slug>` route. Reasons, in order of importance: **the gate is what actually enforces this**; **join links already exist in the wild** (in this doc, in old QR images, in David's notes) and must land on a real page rather than a broken one; and a clinic that later wants a code on its front desk can have the feature back **without rebuilding it**. The only thing missing is the route, which is one line.
  - **`/join/<slug>` now renders `Join.jsx`'s "You'll need an invite." for every clinic**, which is exactly what that screen was written for on 2026-09-04. **It needed no change**, which is a good sign the 09-04 design was right.
  - **⚠️ EVERY SCREEN THAT SAID "open your clinic's link or scan the QR" IS NOW WRONG, and that is the easy half to forget.** `NoClinic.jsx` told a stranded patient to do exactly that; with invites the only door it now says to ask the clinic for one. **When a door is removed, sweep the copy that points at it.**
  - **No marketing copy on either site mentions the QR or the walk-in link** (checked before starting), so **there is no cross-repo change to make.** Worth knowing: the QR was never part of the pitch, only of the dashboard.
  - **▶ THE SALES CONSEQUENCE, FLAGGED NOT ARGUED:** "print this for your front desk" is no longer something to show a prospect. The counter-argument, which is why this is probably right anyway: **an invite-only product is an easier sell to a HIPAA-conscious clinic owner**, and it removes the entire class of "a stranger enrolled in my clinic".
  - **✅ OBSERVED LIVE:** `cdk diff` showed the route **and its Lambda invoke permission** destroyed plus the handler code, nothing else · deploy `UPDATE_COMPLETE` in 55s · the removed route 404s while a live one 401s · both clinics report `open_signup:false` at the public lookup · **glowpt.app/join/riverside-pt and /join/ridgept both render "You'll need an invite." with zero console errors** · the shipped bundle contains no QR code at all.
  - **🪤 THE CDK DIFF CAUGHT A HALF-DONE REMOVAL, WHICH IS THE REUSABLE LESSON.** Removing the route from the handler's dispatch table changed only the Lambda code in the diff. **An API route lives in TWO places: `infra/lambda/api/index.ts` (the dispatch table) and `infra/lib/api.ts` (the gateway route).** Deleting only the first leaves the gateway route live, pointing at a handler that no longer knows it. **The tell was the diff being smaller than expected. When a diff does not show what you meant to do, the change is incomplete, not the diff.**

- **🐛 AN INVITED PATIENT WAS LISTED UNDER "Care Team", AND EXPIRED INVITES NEVER LEFT THE LIST (both fixed 2026-09-05).** David spotted Felix sitting under Pending in the Care Team card.
  - **CAUSE: `getInvites` selected every unconsumed row from `staff_invites` with no role filter**, and patient invites live in that table **by design** (the 2026-09-04 entry explains why the internal names stayed `staff_*`). The dashboard then rendered the lot under Care Team. Patients and staff now have **their own pending lists, each under its own form**.
  - **The same query had no `expires_at > now()` test**, so a dead invite sat in Pending forever, indistinguishable from someone who simply had not got round to signing in. **A list of "waiting" people that silently includes people who can no longer act is worse than no list.**
  - **⚠️ `scripts/aws-seed-demo.mjs` WOULD HAVE REOPENED RIVERSIDE ON THE NEXT RE-PRISTINE**, so it is now taught its **third** flag. (**And on 2026-09-06 a fourth thing of a different shape: `clinics.activated_by` pointing at a demo account from ANOTHER clinic. See that day's re-pristine entry.**) The doc already recorded this trap firing twice (`activated_at` on 2026-08-26, `open_signup` on 2026-09-04). **The script inserts the clinic directly instead of through `provision_clinic`, so it is the ONE path that can recreate the demo in a state nobody wants. Any future column with a safe default needs teaching here, and this is now a pattern rather than an accident.**

- **📣 THE DASHBOARD HAD ONE MESSAGE SLOT AND IT WAS IN THE WRONG CARD. Fixed 2026-09-05, commit `92f1b51`.** David invited patient Felix, **the fields blanked and nothing else happened**: no confirmation, no record that an email had been sent. He was right that it looked broken, and it was.
  - **THE CAUSE: a single shared `notice` and a single shared `inviteLink`, BOTH rendered inside the Care Team card**, below the *therapist* invite form. So a patient invite's confirmation appeared **two cards further down**, under a different heading, off the bottom of a phone screen.
  - **⚠️ THE WIDER FINDING, WHICH IS THE ONE THAT MATTERS: FIVE OF THE SIX FLOWS THAT WROTE TO THAT SLOT ARE SOMEWHERE ELSE ON THE PAGE.** Patient invite (confirmation **and** validation errors), the patient sign-up switch, assignment, discharge and restore all reported into the Care Team card. **Only the therapist invite reported where the user actually was.** So the patient invite form could also fail validation entirely silently, which is the house's own recurring silent-failure shape in a new costume.
  - **THE RULE THIS LEAVES BEHIND: a message belongs beside the control that produced it.** Each now has its own slot: the patient card shows its own result and errors, the sign-up switch confirms in place, Care Team keeps its own, and what remains of `notice` is roster work rendered **beside the roster**. **Do not reintroduce a single page-wide message slot.**
  - **Both invite results share ONE `InviteResult` component**, so the patient and staff confirmations cannot drift the way their message slots did. Same one-declaration rule as `lib/feelings.js` and `LogoMark`, which this codebase has now been bitten by four times.
  - **The sign-up switch now says what it did** ("Saved. Only patients you invite by email can join."). It used to change in **total silence**, which is part of why a clinic could end up open with nobody sure when or why. See the entry below.
  - **Verified by rendering both changed cards at 1000px and 390px** from the REAL style object (temporarily exported for the render, then reverted and diffed back to prove it). The dashboard sits behind an OTP login, so this is not the live screen.

- **🔀 HOW RidgePT GOT SWITCHED TO OPEN IS UNANSWERABLE, AND THAT IS ITSELF THE BUG (2026-09-05).** David found RidgePT showing the QR and walk-in link when he believed he had set it invite-only, and could not account for it. **He has since set it back to Invite only and confirmed the setting survives a sign-out and sign-in.**
  - **WHAT WAS RULED OUT BY READING THE CODE, not guessed:** the column defaults to `false`; the 2026-09-04 patch set every clinic except Riverside to `false`; `provision_clinic` does not touch it; and the **only** writer is `set_clinic_open_signup`, which refuses anyone who is not a manager of that clinic. The seed script sets it true for **Riverside alone**. So nothing in the system flips it on its own: it was a click.
  - **⚠️ BUT `set_clinic_open_signup` WRITES NO `access_log` ROW**, unlike `admin_set_clinic_active`, which logs every flip. **So there is no record of who opened a clinic, or when.** For the switch that decides whether anyone holding a link can enrol themselves as a patient, that is a real gap.
  - **AND IT IS A ONE-TAP CONTROL WITH NO CONFIRMATION.** Two large buttons, optimistic update, immediate effect. On a phone they sit directly above the invite form a manager uses constantly. **A stray tap opens the clinic**, and until today it did so silently.
  - **▶ OPEN QUESTION FOR DAVID, NOT YET DECIDED:** should opening a clinic to walk-ins (a) ask for confirmation first, and (b) write an `access_log` row? Closing it again needs neither. **The in-place confirmation shipped today is the minimum, not the answer.**
  - **The doc's 2026-09-04 claim that "RidgePT = invite only" was therefore accurate when written and had drifted by 09-05.** Live state as of 2026-09-05: **Riverside = open link + QR, RidgePT = invite only**, as originally intended.

- **🐞 THE INVITED PATIENT STRANDED ON THE NoClinic SCREEN — FOUND, REPRODUCED AND FIXED 2026-09-05. Commit `99afcc6`.** David invited patient `Felix` at RidgePT, followed the whole flow, and landed on **"You're not connected to a clinic yet."** with no way forward.
  - **🔍 THE TELL WAS IN THE SCREENSHOT: AN 8-DIGIT CODE.** Sign-up confirmation codes are **6** digits and sign-IN codes are **8** (recorded in this doc since 2026-08-22). So he was never signing up; Cognito was signing him in. **When a screenshot of an auth flow is available, read the code length before theorising: it says which path ran.**
  - **THE CHAIN, each link read in source and then REPRODUCED, not assumed:** `SignUp` creates the Cognito account **immediately and UNCONFIRMED**, so abandoning an invite at the code screen **still takes the address**. Felix had done exactly that earlier (he confirmed it). His next invite therefore threw `UsernameExistsException`, and `lib/cognito.js` **quietly fell back to `beginSignIn`**. **Sign-in never runs `ConfirmSignUp`, so the post-confirmation Lambda never fires, so `register_user` never runs**, leaving an authenticated user with **no `public.users` row**. `accept_patient_invite` then died inserting his profile, because `profiles.id references public.users(id)`. **Reproduced locally before a line was changed: `profiles_id_fkey`, SQLSTATE 23503.** `auth.jsx` caught it, wrote `console.log('Profile re-attach failed:')`, and carried on as though he simply had no clinic.
  - **⚠️ THIS IS THE "PARKED PHASE-3 EDGE" THE DOC HAS CARRIED SINCE 2026-08-18, AND THE FILING WAS WRONG ABOUT IT.** It was recorded as a rare *total post-confirm miss*, "a small follow-up, not blocking". **The `UsernameExistsException` fallback makes it reachable through the front door**, and it is the normal outcome for anyone re-invited or returning to an abandoned invite. **A parked edge case is only as rare as the paths that reach it; when a new path is added, the parked note has to be re-read.**
  - **⚠️⚠️ ALL FOUR ATTACH ROUTES HAD IT, not just patient invites.** `provision_clinic`, `join_clinic`, `accept_staff_invite` and `accept_patient_invite` all end by writing `profiles`, so the **walk-in `/join/<slug>` link and `/onboard` were equally affected.** All four are fixed together.
  - **🔓 THE SECURITY HALF, WHICH IS THE MORE IMPORTANT FINDING. The email guard was FAILING OPEN.** Both invite doors read `if lower(v_inv.email) <> lower(v_email)`. With no users row `v_email` is NULL, so **the comparison is NULL rather than true and the guard FELL THROUGH.** This doc states that a forwarded invite link grants nothing *because* the verified email must match. **That promise was being kept by `profiles_id_fkey`, not by the check written to keep it.** Nothing was ever exploitable, but the guarantee rested on the wrong thing.
    - **⛔ IT HAD TO BE FIXED FIRST, AND THE ORDER IS THE WHOLE POINT.** Making the users row appear without closing the guard would have converted a harmless fall-through into a **real hole where any signed-in person could claim any invite link.** Both doors now test `v_email is null or ...`, and **T44 / T44b assert the refusal comes from the EMAIL guard rather than from the foreign key.** The `is null` clause is load-bearing; a comment in `db/schema.sql` says so.
  - **THE FOUR FIXES:**
    1. **The email guard fails closed** on both doors.
    2. **New `public.ensure_self(citext)`** so the API can create the **caller's own** identity row. **⛔ THE DIFFERENCE FROM `register_user` IS THE WHOLE DESIGN: `ensure_self` takes NO id.** It uses `current_user_id()`, which comes from the verified Cognito sub the API set with `set_config`, so `glowpt_app` can only ever create a row for the already-authenticated caller. **`glowpt_app` still does not hold `register_user`, and the patch asserts that.** Do not add an id parameter, and do not "simplify" by granting `register_user` instead. Wired in via `withUserEnsured` in `infra/lambda/api/index.ts`, which takes the email from the **verified JWT claim, never the body**.
    3. **An unconfirmed account now FINISHES its sign-up** instead of falling to sign-in. `ResendConfirmationCode` succeeds only for an unconfirmed account, so it doubles as the test for which case we are in. That fires the Lambda and attaches the clinic the intended way.
    4. **The re-attach stops failing silently.** The pending localStorage keys are now cleared **after a successful attach, not before the attempt** (clearing first meant one blip discarded the only record of which invite was being claimed), `attachError` is exposed on the auth context, and **NoClinic renders a different screen** for a failed attach: "We couldn't finish connecting you." with a **Try Again** button. The old copy told an invited patient to go and find a join link, **advice they had already followed and could not act on.**
  - **✅ WHAT WAS OBSERVED IN PRODUCTION (house rule: shipped means observed working):** the diagnosis **confirmed on the real account** (Felix had **no `users` row at all**, and a sweep of the whole pool showed him the **only one of 13** in that state, with **0 DB orphans**) · patch applied with `GUARDS PASSED` and then **verified by independent query rather than by the patch's own word** (ensure_self present, `glowpt_app` yes, **PUBLIC no**, `register_user` still no, both guards closed) · `cdk diff` showed **exactly one changed Lambda, no replacements, bastion absent from the diff** so the AMI pin still holds · deploy `UPDATE_COMPLETE` in 55s with `BastionInstanceId` unchanged · **the whole chain run against real production data as the real `glowpt_app` role, with Felix's real sub and real token, INSIDE A TRANSACTION THAT WAS THEN ROLLED BACK** → attached to RidgePT as a patient with a consent row, then reverted, leaving his invite live for David to walk himself · **a different real signed-in user (Charlie) refused Felix's invite with "This invite is for a different email address"** · public invite and slug lookups still resolve, a forged token 404s, and all four attach routes 401 both with no token and with a forged bearer.
    - **▶ THE ROLLED-BACK TRANSACTION IS WORTH COPYING AS A TECHNIQUE.** It proves a fix against real production data and real credentials without changing anything, and without consuming the one artifact (here, a live invite) the human still needs in order to verify the user-facing half.
  - **⏳ NOT YET OBSERVED: Felix actually completing the link in a browser.** That needs David. **He does NOT need a new invite** — the old one is unconsumed and unexpired. He must **open the invite link again** rather than only reloading, because the old frontend had already deleted his pending localStorage key before it failed.
  - **⚠️ COGNITO'S EMAIL-OTP SIGN-IN AUTO-CONFIRMS AN UNCONFIRMED USER.** Felix reads `CONFIRMED` in the pool despite never completing a sign-up. **So fix 3 alone would NOT have rescued him** (resend refuses a confirmed account) and fix 2 was genuinely required. It also means the unconfirmed window is short and self-closing, which is why this presented as a mystery rather than as a reproducible state.
  - **Tests 47 → 53.** New: the guard refusing an unregistered caller on **both** doors, `ensure_self` unblocking the invited patient end to end, its idempotence, its refusal of an address owned by another sub, and its refusal of an unauthenticated caller.

- **🪤 A PATCH LEAVES A NEW FUNCTION EXECUTABLE BY `PUBLIC`. A FRESH BUILD DOES NOT. (2026-09-05, caught by the diff and not by review.)** Postgres gives every newly created function an implicit `EXECUTE` grant to `PUBLIC`. `db/schema.sql` strips it with a blanket `revoke execute on all functions in schema public from public` near its foot; **a patch has no such sweep.** So the first draft would have handed `ensure_self` to **every role, `glowpt_weekly` and `glowpt_postconfirm` included**, while a fresh build would not.
  - **It was found ONLY because the rehearsed patch was diffed against a fresh build** — function names, owners AND acl, then the function bodies. The 2026-08-26 entry already said to rehearse a patch against a pre-patch database; **this adds the second half: also diff the RESULT against a fresh build.** Rehearsing proves the patch runs. Diffing proves it produces the schema of record.
  - **⛔ EVERY FUTURE PATCH THAT CREATES A FUNCTION MUST `revoke execute ... from public` BEFORE granting.** The patch now does, and its own guard block asserts PUBLIC cannot execute it, so the mistake cannot return silently.

- **⚠️ THE INFRA TEST SUITE (`infra/test/infra.test.ts`) WAS RED, AND HAD BEEN FOR TWO WEEKS. Fixed 2026-09-05.** Two stale assertions, neither related to the day's work: the public-route list still expected exactly ONE unauthenticated route and missed **`GET /staff-invites/{token}`** (added 2026-09-04), and the RDS Proxy auth count still read **3** when **`glowpt/db/weekly` made it 4 on 2026-08-22**.
  - **Confirmed pre-existing by stashing the day's changes and re-running: it failed identically.** Worth doing before assuming a red test is yours.
  - **THE REAL FINDING IS THAT NOBODY NOTICED FOR TWO WEEKS**, which means the suite is not being run as part of shipping. The 2026-08-31 lesson about the Monday heartbeat applies exactly: **an alarm nobody looks at is not an alarm.** Worth deciding whether `npx jest` joins the pre-deploy routine alongside `cdk diff`.
  - Both assertions were rewritten to say **why** the numbers are what they are, since "which routes are unauthenticated" and "which roles can reach the database" are security facts, not trivia. **Now 3/3.**

- **✅ RESOLVED, THEN MADE MOOT — the RidgePT `open_signup` drift.** It read `true` on 2026-09-05, contradicting the 09-04 entry. David set it back, then decided to **remove walk-in sign-up entirely**, so the switch that drifted no longer exists. **He also confirmed he probably did tap it the day before and forgot**, which is exactly the accident the control invited. See the removal entry above.

- **⚖️ THE PRIVACY NOTICE AND BAA SUMMARY NAME BEDROCK NOW, NOT ANTHROPIC (2026-09-04, David: "We will not be using anthropic because we have no baa with them. So it's moot.").** `PRIVACY_NOTICE_VERSION` v2 → v3 and `BAA_VERSION` summary-v2 → summary-v3, because **who receives PHI is the most material change this notice can carry** and that is exactly what the version mechanism exists to record.
  - **⚠️⚠️ THIS DELIBERATELY BREAKS A COUPLING THIS DOC USED TO STATE ABSOLUTELY.** The Bedrock block says *"the Lambda switch and the v3 privacy copy MUST land together, do not split them."* **They are now split**, on David's instruction, with a reason: **production still calls `api.anthropic.com`, so the copy is ahead of the code until `bedrock-ai-response` merges.** It is safe because **demo-data-only governs and a real patient cannot use GlowPT before Bedrock lands anyway** — Bedrock is one of the two go-live gates — so nobody reads this notice for real until it is true. **⛔ IF BEDROCK IS EVER ABANDONED RATHER THAN MERGED, THIS COPY MUST BE REVERTED WITH IT.** That warning lives in `src/lib/legal.js` above the version constant, not only here.
  - **🪤 WRITING IT FRESH RATHER THAN LIFTING THE BRANCH'S VERSION CAUGHT A REAL REGRESSION.** The v3 text has existed on `bedrock-ai-response` since 2026-08-23 — **four days before the em-dash ban** — and carried **four em dashes in patient-facing strings** ("Your care team at X — your assigned therapist", "we send that one check-in — your first name ... — to Amazon Bedrock", and two more). **Merging that branch would have silently put them back**, undoing the 2026-08-27 pass. This is the same trap the doc already flags for the `ai-response` FALLBACK, firing on a different file.
  - **✅ FIXED AT SOURCE: the branch's `legal.js` is now a copy of main's**, so the two agree and that file is no longer part of what the Bedrock merge touches. **The general habit: when a long-lived branch carries user-facing copy, a rule adopted on main after the branch was cut does NOT reach it. Diff the copy, not just the code, before merging.**
  - **The new wording, for the record:** the reflection goes "to Amazon Bedrock, the AI service that runs inside our own protected Amazon Web Services account", and the words "stay inside that protected environment, are not sent to an outside AI company, and are not used to train anyone's models". Subcontractors is now "Amazon Web Services alone ... all under one executed AWS business associate addendum".
  - **The `legal/` attorney drafts already said AWS alone** and are unchanged. The hazard recorded there is the same one as here and expires the same way, when Bedrock merges.
  - **Verified by opening both modals in the running app**, not by reading the diff: Bedrock named, Anthropic absent, zero em dashes and zero en dashes in either.

- **✅ BOTH INVITE FLOWS PROVEN END TO END IN PRODUCTION BY DAVID, 2026-09-04 (4th session).** PT Pete followed a staff invite link, entered his code, and landed on the therapist view; Timmy followed a patient invite link, ticked the consent box, checked in, and got a real AI reflection. The manager dashboard then showed **2 patients, 2 active, 100% engagement, PT Pete on the care team**, and the roster listed Charlie (assigned) and Timmy (unassigned). **The house bar is met on both: shipped means observed working.**
  - The patient screen showed the consent checkbox and privacy notice link; the staff screen did not. That difference is the whole reason the two doors exist separately, and it is now visible rather than only asserted by tests.
  - **⚠️ MONDAY EMAIL VOLUME IS NOW 12:** Riverside 8 (6 patients + 2 staff) plus RidgePT 4 (Charlie, Timmy, David as manager, PT Pete). All still land in David's own inbox as `besoulful+` aliases.

- **✂️ THE EM-DASH BAN IN THE AI PROMPT WAS ONLY HALF A BAN, FOUND 2026-09-04 by reading two real reflections.** The prompt said *"Do not use em dashes in your response."* and the model simply reached for the nearest thing that was not one: **Charlie's reflection used a spaced hyphen (` - `), Timmy's used an EN dash (` – `).** Both are a dash joining two clauses, which is exactly what the house rule exists to stop a patient reading.
  - **The instruction now names all three and says what to use instead**, and explicitly exempts hyphens inside words so `check-in` survives. **It stays LAST in the prompt** — the doc's existing note that final instructions carry the most weight still holds.
  - **⚠️ THE GENERAL LESSON, worth more than the fix: a prohibition a model can satisfy with a near-synonym is not a prohibition.** The 2026-08-27 verification saw "zero em dashes" and concluded the instruction was holding. It was, literally, and the rule was still being broken. **When checking a generated-copy rule, look for what the model substituted, not only for the banned thing.**
  - The prompt lives in `PatientApp.jsx` (`handleSubmit`), the frontend, so there is only ONE copy. The `bedrock-ai-response` duplication warning applies to the Lambda's FALLBACK string, not to this.

- **🔢 `1 days checked in` AND `1 check-ins` ON THE STAT CARDS — FIXED 2026-09-04, having been deliberately left earlier the same day.** The reasoning then was that a unit label under a big figure is not a sentence, so it need not agree. **Seeing them rendered for a brand-new patient, that was wrong: they read as an error, not as a label.** Fixed the same way as the month line. Recorded because the first judgement was defensible on paper and still wrong on screen, which is the argument for looking.

- **✅ THE THERAPIST CASELOAD VIEW IS CORRECT — checked and closed 2026-09-04.** PT Pete's own view briefly read "No patients assigned to you yet" while the manager dashboard showed him carrying Charlie. **That screenshot simply predated the assignment**, confirmed by David reloading it: his view now lists Charlie with today's check-in, a 1-day streak and a 4.0 average. `profiles_select_caseload` is scoping correctly and **the therapist path is proven end to end for the first time**, not just the sign-up half of it.
  - Worth keeping as a habit rather than a finding: **two screenshots of the same system are not necessarily of the same moment.** The contradiction was real on the page and false about the system, and the only way to tell was to ask for a fresh look rather than reason from the images.

- **🚪 PATIENT INVITES + THE PER-CLINIC SIGN-UP SWITCH — SHIPPED 2026-09-04 (3rd session). This closes the open-join-link hole.**
  - **THE DESIGN QUESTION DAVID ASKED, AND THE ANSWER, because it governs anything built here next.** He asked why staff and patients had two different processes and whether staff should just get a link like patients do. **The answer is both, and the reason is what a role can SEE.** A patient join link is safe to hand around because a patient only ever sees their own check-ins, which is exactly why it can be a QR on a wall. **A therapist reads the health records of every patient assigned to them**, so "who may claim this role" is a different question and cannot be answered by "whoever holds the link". The real inconsistency he had spotted was that a manager had **nothing to send** — so invites now produce a link AND keep the email gate. **Do not "simplify" either half away.**
  - **⚠️ `clinics.open_signup` IS A THIRD FLAG AND MUST STAY ONE.** `activated_at` = may this clinic operate at all (David's switch). `baa_signed_at` = the legal record. `open_signup` = how it enrols (the manager's switch). **They move for entirely different reasons and one column would force a lie**, exactly as the doc already argued for the first two. **T17b asserts the independence directly: an ACTIVE clinic still refuses walk-ins while invite-only.**
  - **FALSE by default.** A new clinic is invite-only from the moment it exists, so nobody is ever exposed by a link they never asked for. **The patch set Riverside true** (sales demo + Monday heartbeat, its walk-in link has to work) **and everything else false.**
  - **The switch is the MANAGER's, on their own dashboard, not the platform admin's** — only they know whether they want a code on their front desk. `set_clinic_open_signup` refuses anyone who is not a manager of that clinic.
  - **⛔ THE CONSENT RULE, WHICH IS THE SUBTLEST PART.** `accept_staff_invite` records no consent. A patient attached through it would have no `consents` row, which is precisely the gap the privacy notice exists to close. So **the staff door now REFUSES patient invites on BOTH paths** (the token path raises; the blind safety-net path filters them out), and `accept_patient_invite` is the only patient door. **It pins the role to 'patient' rather than reading it off the invite row**, so even a tampered row could not mint staff through the patient screen. The mirror holds too: the patient door refuses staff invites. **Neither screen can be used to claim the other kind of account.**
  - **An invite is NOT a way around the activation gate.** `accept_patient_invite` re-checks `clinic_is_active`, asserted by T40.
  - **URLS:** invite links are now **`/invite/<token>`**, handled by `InviteJoin.jsx` for every role. **`/staff/:token` stays mounted as an alias until 2026-09-18**, because the first staff invite links pointed there and they live 14 days. **Delete the alias after that date.** The DB function and API route are still named `get_staff_invite` / `/staff-invites/{token}` on purpose: the deployed API called them by those names while links were in flight, and the table is `staff_invites` anyway, so the internal names are consistent with each other even though they now serve both.
  - **Suite is 47, up from 36.** New: both doors refusing each other's invites, the safety net skipping patient invites, consent actually written, the activation gate holding for an invitee, the two gates proven independent, and a patient unable to invite anyone or open their own clinic.
  - **⚠️ `scripts/aws-seed-demo.mjs` NOW SETS `open_signup: true` FOR RIVERSIDE, and this is the SECOND time this trap has fired.** The script inserts the clinic directly rather than through `provision_clinic`, so it is the one path that can recreate the demo in a default state nobody wants. It already had to be taught `activated_at` for the same reason on 2026-08-26. **Any future flag with a safe-by-default value needs the same treatment there.**
  - **✅ OBSERVED IN PRODUCTION:** patch committed with `GUARDS PASSED.` and the clinic table reading Riverside `open link + QR` / RidgePT `invite only` · deploy `UPDATE_COMPLETE` in 67s, three routes added, nothing replaced · the live slug lookup reports `open_signup` correctly for both clinics · all three new routes 401 without a token and with a forged bearer · the staff invite route PT Pete is using still behaves.
  - **⏳ NOT YET OBSERVED: a patient invite email arriving and a patient completing the link.** That needs David, same as the staff one.
  - **⚠️ A DEPLOY-ORDER NOTE THAT COST A CONFUSING MINUTE:** waiting on CloudFormation's stack status right after launching `cdk deploy` reads the PREVIOUS deploy's `UPDATE_COMPLETE`, because CDK spends the first minute bundling and publishing before it touches the stack. **Wait on the `cdk` process, or on `LastUpdatedTime` changing — not on the status alone.**

- **🔑 STAFF INVITE LINKS — SHIPPED 2026-09-04 (2nd session). This REPAIRS A REGRESSION THE AWS CUTOVER CAUSED, and the way it happened is the lesson.**
  - **⚠️ THE FEATURE WORKED ON SUPABASE. David used it. Do not tell him it was never built.** `invite_staff` wrote a pending row and nothing else, and that was fine, because `/login` called `signInWithOtp` which **created an account for any email** — the therapist signed in, `accept_staff_invite` matched them by email, done.
  - **The Phase 2 "clinic-only accounts" decision (2026-08-07) removed self-creation and gave staff no replacement door.** The post-confirmation Lambda got its `staff` branch, so the BACKEND door was built and has been sitting there unused since August; **no screen ever called it.** From the 2026-08-22 cutover until today, an invited therapist could not get an account at all: they would enter their email at `/login`, see the code screen (existence errors are prevented at the pool), and no code would ever arrive.
  - **⚠️⚠️ THE V2.4 ENTRY WARNED ABOUT THIS IN BOLD AND WAS OVERRIDDEN ANYWAY:** *"do NOT 'fix' this by setting `shouldCreateUser: false` on `/login` — invited therapists have no account yet and MUST create one there."* **The generalisable lesson: when a decision contradicts a standing warning in this doc, the warning has to be answered, not just outvoted.** Phase 2's own design listed "staff-invite accept" as one of the three doors, so the intent was right and only the frontend was missed.
  - **WHAT SHIPPED, and why it is links rather than just an email.** David asked why staff and patients had two different processes at all, and whether staff should simply get a link like patients do. **The answer is both.** A patient join link is safe to hand around because a patient can only ever see their own check-ins, which is why it can be a QR on a wall. **A therapist reads the health records of every patient assigned to them**, so "who may claim this role" is a different question and cannot be answered by "whoever has the link". The real inconsistency he had spotted was that the manager had **nothing to send** — so invites now produce a link AND keep the email gate.
  - **⛔ THE TOKEN IS NOT A CREDENTIAL, AND THIS IS THE WHOLE DESIGN.** It says WHICH invite is being claimed. `accept_staff_invite` additionally requires the signed-in user's **verified** email to equal the invited address, and the role comes off the invite row, never from the caller. **A forwarded or leaked link therefore grants nothing**, and that check is in the database, so it holds whatever any frontend does. Re-inviting mints a fresh token, which is how a link sent to the wrong place gets killed. 14-day expiry, single use.
  - **`/staff/<token>` shows the invited email FIXED, not as an input.** The invite is for that address and the database refuses anything else; an editable field would only invite a confusing rejection.
  - **A failed load is told apart from an invalid invite.** Only a 404 says "expired or used"; anything else says "we couldn't load this". A network blip must not send someone off asking for a replacement link they do not need.
  - **The email sends from SES via `no-reply@glowpt.app`**, through the interface VPC endpoint `WeeklySummary` owns (the API Lambda has no NAT). **The invite row commits BEFORE the send, so a failed send does not throw the invite away** — the response carries `email_sent` honestly and the manager still gets the link on screen. That is the house's no-silent-partial-success rule applied.
  - **Tests: 36, up from 24**, in a new `db/tests/invite_tests.sql`. The twelve new ones cover the wrong-email link holder (and that a failed attempt attaches them to nothing), a forged token, expiry on BOTH the token and the blind safety-net path, replay, supersession, and a patient trying to mint an invite. **T30 failed when first written and the code was right** — the test read the token as a non-manager, and `staff_invites` is RLS-scoped to the manager of its own clinic, so it got null and silently exercised the no-token branch. **A test that hands a function null instead of the value it meant to pass can pass for the wrong reason; assert the input was actually read.**
  - **⚠️ DEPLOY ORDER IS SQL → API → FRONTEND, and it is not optional.** The API selects the new columns, and the frontend expects `invite_url` in the response. The SQL alone is safe: the old zero-arg `accept_staff_invite()` call still resolves against the new defaulted-argument version, and the old handler ignoring `invite_staff`'s new return value is harmless.
  - **The patch was rehearsed against a PRE-patch database** (the 2026-08-26 lesson), re-run to prove it is a no-op, and the result diffed against a fresh build: columns, indexes and function signatures identical. **`staff_invites.token` uniqueness is a unique INDEX rather than an inline constraint precisely so both build paths match** — a patch cannot add a constraint idempotently.
  - **✅ OBSERVED IN PRODUCTION:** patch committed with `GUARDS PASSED: 1 invite(s) now carry a link` (PT Pete's existing invite gained a token in place, so nobody needed re-inviting) · deploy `UPDATE_COMPLETE` in 83s with **no replacements and the bastion absent from the diff**, so the AMI pin is still holding · the live unauthenticated route returns RidgePT / therapist / PT Pete for the real token · a forged token 404s · CORS returns `https://glowpt.app` · `/clinic/roster` still 401s without a token.
  - **⏳ NOT YET OBSERVED: the email actually arriving, and a real person completing the link.** That needs David. Until he confirms, this entry is "deployed and proven at the API", not "proven end to end".

- **📱 THE QR DOWNLOAD DID NOTHING ON AN IPHONE, FIXED 2026-09-04.** It was `<a href={dataUrl} download>`. **iOS Safari ignores the `download` attribute on a `data:` URL and also refuses to navigate to one**, so the tap was inert, silently. It worked on desktop, which is the only place it had ever been tried.
  - Now chosen **by capability, not by sniffing the user agent**: where the browser can share a file (`navigator.canShare({files})`, true on iOS Safari and Android Chrome) it opens the native share sheet, which is how a file actually gets onto a phone. Everywhere else it clicks a `blob:` URL. **Both paths use a blob rather than the data: URL** — some browsers choke on very long ones. A dismissed share sheet is a choice, not an error.
  - Label moved from "Download QR" to **"Save QR"**, because on a phone it no longer downloads.
  - **The mechanism was verified in a real browser** (data URL → blob → File → blob: URL, and `canShare({files})` correctly false on desktop so it takes the download branch). **⏳ The iOS branch needs David's iPhone and is not yet confirmed.** Meanwhile long-pressing the QR image and choosing Add to Photos has always worked.

- **✅ CLOSED SAME DAY — ANYONE WITH A CLINIC'S JOIN LINK COULD ENROL AS ITS PATIENT. Fixed 2026-09-04 (3rd session); see the patient-invite entry above for what shipped.** The original finding, kept because the reasoning still governs the design: Checked in the source, not assumed: `join_clinic` verifies you are signed in, the clinic exists, the clinic is switched on, and you are not already staff. **Nothing checks WHICH person you are.** Slugs are guessable (`/join/ridgept`, `/join/riverside-pt`), so this is not only "someone forwarded the link".
  - **⚠️ DO NOT CONFLATE THIS WITH V2.4.** That fixed people ending up with **no clinic at all** (orphan accounts and check-ins no dashboard could see). It never restricted who may use a clinic's link. David asked whether we had already corrected for this; we had not.
  - **Nor did it come from an incident.** The manager-sent patient invites backlog item was a feature David asked for on 2026-07-15, and the doc recorded the reasoning then: the QR/join link is *"clinic-wide by nature and stays the self-serve walk-in path, not an invite."* A code on a wall only works if anyone can scan it.
  - **What it costs:** a stray person can enrol in a real clinic, write check-ins, sit in the roster and burn AI reflections on David's bill. **What it does NOT do is leak anything** — RLS still scopes them to their own data, and a manager can discharge them. Real, low severity, unaddressed.
  - ~~The fix is the manager-sent patient invite feature.~~ **DONE. David's words: "I don't want stray patients sitting in aws. I wanted this fixed before."**

- **✅ THE ACTIVATION GATE WAS WALKED END TO END IN PRODUCTION BY DAVID, 2026-09-04 — the first time a human has driven the whole flow on the live site.** He built the `RidgePT` sandbox at `/onboard`, confirmed the clinic came up **CLOSED**, opened `/join/ridgept` and got **"This clinic isn't open yet."**, pressed **Switch On** in `/admin`, joined as a test patient (`Charlie`), checked in, got a **real Anthropic reflection** (it named his cat and read back the 4-out-of-5, so not the fallback line), and confirmed the clinic dashboard showed the check-in.
  - **This is the house's own bar met: shipped means observed working.** The gate had 23 passing schema tests and a production smoke test behind it, but nobody had ever watched a patient be refused and then let in. Now someone has.
  - **⚠️ It does NOT prove anything about Bedrock.** Production still calls `api.anthropic.com`; a working reflection has never been evidence the Bedrock quotas cleared, and a session got that wrong once already on 2026-08-28.
  - It also quietly proved the last month of copy and layout work renders correctly on the live site: the stacked price line, the "This is a summary" line under the BAA checkbox, the closed-clinic notice, and the slug auto-derivation.

- **🔠 THE SMALL-LABEL FAMILY IS NOW THREE NAMED TIERS IN `AuthShell.jsx`, NOT A PILE OF LITERALS (2026-09-04, David: the tiny labels "need at least 200% larger, but make it proportionate too").** Every one of them was **11.5px or 12px** — two arbitrary values for a single class of label, and small enough that **a clinic's own name was the smallest text on its own sign-up screen**, below even the fine print.
  - **`LABEL_SIZE = 22`** — a label above **display type**: the clinic name under the logo (over a 34px title), "Today's Reflection" (over the 22px serif), the check-in date (over the 32px question), "Your Week · Tap Any Day" and "Your Month" (over 38px day dots), and all three modal titles.
  - **`SECTION_LABEL_SIZE = 18`** — a section head above **body copy**: the dashboard cards ("Your Patient Invite Link" over a 14px URL, "Patient Sign-Up QR", "Care Team" over 14.5px rows) and the journal's "Your Note" / "Today's Reflection" over 16-19px text.
  - **`CARD_LABEL_SIZE = 15`** — a label above **the figure it names**: the patient stat cards (28px value), the dashboard tiles (32px figure), the check-in question labels (20px question), and the "GlowPT" byline on a journal reflection.
  - **⛔ THE THREE TIERS ARE ONE RULE — a label must not outgrow what it introduces — AND COLLAPSING THEM HAS ALREADY BEEN TRIED AND FAILED.** One size for the whole family was built and rendered first: **at 22 a stat label outgrows its own number, "Today's Feeling" wraps to two lines in a half-width card at phone width, and the card reads label-first**, which inverts what a stat card is for; and a 22px dashboard head dwarfs the 14px URL beneath it. **Do not "finish the job" by unifying them.** The reasoning is also written into the source above the constants.
  - **⛔ NEVER RETYPE A LITERAL AT A CALL SITE.** All 13 labels import their size. Same one-declaration rule as `lib/feelings.js` and `LogoMark` — a rule this codebase has now been bitten by three times.
  - **Deliberately NOT swept:** the dashboard's roster column headers and legend labels (`rosterHead`, `legendLabel`), the Admin screen's `metaLabel`, the flag pills, and every piece of fine print. Those are table furniture and quiet text, not section labels, and they were never the complaint.
  - **Verified by rendering, not by reading:** `/join/ridgept` and the privacy modal driven live at 375px and 1280px. The patient and dashboard screens sit behind the OTP login, so those were checked by rendering their **real style values** into a harness at each candidate size and looking at all of them side by side — faithful, but **not the live screens**, so they are worth a glance next time David is signed in.

- **📐 THE `/join` SUB-LINE IS TWO STACKED SENTENCES (2026-09-04, David).** "Your daily check-in, from {clinic}." and "One good day at a time." are now two sibling `<div>`s inside one `ui.muted` wrapper. As a single string the wrap point was decided by whatever measure the viewport gave, and at phone width it broke **mid-sentence** — "One good day / at a time." **Same fix and same reasoning as the stacked price line**, and the wrapper keeps the single `marginBottom`. Verified at 375px and 1280px.
  - **The closed-clinic screen's sub-line was deliberately left alone** — it also wraps mid-sentence, but David asked only for the sign-up screen. Flag it to him if it ever bothers him.

- **🐞 "1 check-ins in 30 days" — FIXED 2026-09-04, and how it hid for two months is the useful part.** The patient Progress screen hardcoded the plural in `PatientApp.jsx`; it now reads `check-in{count === 1 ? '' : 's'}`. **Only a patient with exactly one check-in could ever see it**, and every demo account is seeded with many (Grace has ~25), so **the bug was unreachable from the demo data and became visible the moment a real fresh account existed.** Worth remembering when reasoning about anything that renders a count: the demo clinic is not a representative sample. The stat-card sublabels ("check-ins", "days checked in") were left alone on purpose — those are unit labels under a figure, not sentences.

- **✖️ EVERY MODAL HAS AN X TOP-RIGHT AND A SMALL CLOSE AT THE FOOT (2026-08-29, David).** The Close button used to be `ui.btn` at full panel width (460 to 520px), which read as a primary action when it is only a dismiss. It is now an **86px centered pill**; an **X** sits in the top right of all three modals.
  - **Both styles live once, in `AuthShell`'s `ui`: `modalCloseX` and `modalCloseBtn`.** A fourth modal inherits them. Landing imports `ui` purely for these two, even though it keeps its own `s` style object.
  - **⚠️ The panel needs `position: 'relative'`** or the absolutely placed X escapes to the viewport. All three panels now set it.
  - **BOTH CONTROLS ARE DELIBERATE, DO NOT REMOVE THE FOOTER ONE AS REDUNDANT.** The X sits INSIDE the panel's own scroll box, so it scrolls away as you read down. That matches the FranklinAI modal. The X dismisses from the top, the footer Close dismisses when you have read to the end, which is exactly the case that matters on the long BAA and privacy modals.
  - **Verified by driving each modal:** X present, page locked while open, closes on click, `overflow` restored, and the consent checkboxes on Onboard and Join untouched through the cycle.
- **📐 TWO LAYOUT PINS ADDED 2026-08-29. Both are load-bearing; do not "simplify" either.**
  1. **The `/onboard` title carries an explicit `<br />`:** `Bring <Brand /> to<br />Your Clinic`. Left to natural wrapping it put "Your" on line 1 at desktop width and broke differently across browsers. David asked for a fixed two-line stack. Verified at 375px and 1280px.
  2. **The landing hero measure moved `maxWidth: '24ch'` to `'28ch'`.** At 24ch the 2026-08-29 copy stranded **"full." alone on a fourth line** at desktop width; 28ch gives three balanced lines. **Mobile is unaffected either way, because the viewport binds there and not `maxWidth`** (five lines, ending "stays full."). If the hero copy changes again, re-check the last line at BOTH widths, since this is the third time the measure has needed retuning.
- **🧮 THE TWO BULLET LISTS ARE NOW IDENTICAL: 8 AND 8, SAME WORDS, SAME ORDER (2026-08-30, David — this REVERSES the old "do not reconcile the counts" rule).** GlowPT ran 5 bullets and FranklinAI ran 8 for several days, deliberately, on the reasoning that the glowpt.app modal was a nudge rather than the full pitch. **David overruled that:** a buyer reads both surfaces in one sitting and must never see the product described two different ways. The three that were missing here are back — the patient journal/streaks bullet, the "one subscription covers the clinic" bullet, and the remote-monitoring billing bullet — and GlowPT was also **reordered to match FranklinAI's order**, since matching counts with a shuffled order is not actually matching.
  - **The two lists live in two repos and nothing enforces the match at build time.** `whatGlowptIs.points` in `src/lib/marketing.js` here, `features` in `franklinai-v2/src/App.jsx` there. **Both carry a comment pointing at the other. Change one, change the other.** Verified identical at the time by parsing both arrays and comparing element by element, not by reading them side by side.
  - **✍️ BULLET 3 REWRITTEN TWICE ON 2026-08-30 (David). It now reads: "One subscription covers the clinic and the patients join for free."** It previously read "One subscription covers the clinic, no matter how many patients you have. Patients join free." **David never liked the "no matter" construction** — it is defensive phrasing doing work the sentence does not need. The rewrite also **shrank the redundancy** that restoring this bullet had created: it used to end with the exact sentence "Patients join free.", which the price line beneath it repeats verbatim. Now only the word "free" echoes, which is tolerable. **`PRICE_LINE` was deliberately NOT touched** — it is contractually load-bearing (Subscription §5.1, "the amount stated at sign-up") and also renders on `/onboard`, so it is not a free edit.
  - **⚠️ THE READING RISK IS NOW CLOSED, and how it closed is the useful part.** The first rewrite read "...covers the clinic and all its patients for free", which can land for a beat as the *subscription* being free, since the $350 line sits just below it. That was flagged and David initially kept it, then came back unprompted with the same objection and fixed it himself. **The mechanism: "for free" floating at the end of a clause attaches to whatever the reader last held in mind, which was the subscription. Giving it an explicit subject, "the patients join for free", pins it.** Applies to any clause ending in a bare "for free".
  - **The price line beneath it still reads "Patients join free.", so the two now echo more closely than before.** That was accepted: clarity in the bullet is worth more than avoiding a repeat, and `PRICE_LINE` is contractually load-bearing (Subscription §5.1) and also renders on `/onboard`.
  - **What WAS fixed 2026-08-29:** FranklinAI's bullet 8 said "A **free** weekly roster summary"; it now matches this repo's "A weekly roster summary". **The shared bullets are now word-identical; only the count and the order differ.**
- **✅ CLOSED 2026-08-29 — THE FRANKLINAI CARD TEASER NAMES THE NICHE AGAIN.** David put "physical therapy patients" back into the FranklinAI copy only, accepting that one surface reads differently rather than lengthening all four. **The advice that produced this: "PT" in the product name carries the niche for the actual buyer, so glowpt.app does not need the words, but on the FranklinAI card the brand name is scanned rather than parsed and the words earn their place.** The original item, kept for the reasoning: **THE FRANKLINAI CARD TEASER NO LONGER NAMES THE NICHE (raised 2026-08-28).** When the GlowPT pitch collapsed to one sentence, the FranklinAI Available card lost the words "physical therapy" from its teaser. David noticed after the fact and wants to come back to it.
  - **What is true right now:** the FranklinAI **headline** still names the practice, so the site has not stopped being specific. But the card stack itself no longer does, and that was a documented rule (FranklinAI `CLAUDE.md`, *Where the niche is named and where it is not*) which V50 had to retire to make the change.
  - **The tension to resolve:** the shared sentence is deliberately identical on all four surfaces, so naming the niche on the FranklinAI card again means **either** accepting one surface that reads differently, **or** putting "physical therapy" into the shared sentence and changing all four. There is no version that keeps both perfect consistency and the niche on the card.
  - **This item belongs to the FranklinAI repo's copy**, and is recorded here only because the decision is a cross-site one. Anything actually changed there needs a `CLAUDE.md` regeneration on that side.
- **✍️ THE SHARED GLOWPT PITCH, AND WHERE THE TWO SITES DELIBERATELY DIFFER (rewritten 2026-08-29, David). This SUPERSEDES the header entry below.** The pitch lives on FOUR public surfaces, two here and two on the FranklinAI site. It is **two sentences**, and the two sites carry **deliberately different first sentences**:
  > **Here (hero + More Info modal):** The GlowPT app engages patients between visits. More completed plans of care and the clinic stays full.
  >
  > **FranklinAI (card teaser + GlowPT modal):** The GlowPT app keeps physical therapy patients engaged between visits. More completed plans of care and the clinic stays full.
  - **⚠️ THE DIFFERENCE IS THE POINT AND IS NOT DRIFT.** glowpt.app omits "physical therapy" because the page is already GlowPT-branded and saying it there is redundant. The FranklinAI card sits in a product stack where "GlowPT" is scanned as a name, so the words do real work. **Second sentence is identical on all four. Keep it that way.**
  - **History, so nobody re-derives it:** on 2026-08-28 all four were one identical sentence ("...keeps patients engaged between visits, so more plans of care get completed and the clinic stays full."). David split it into two sentences and reintroduced the niche on the FranklinAI side only, on 2026-08-29. On **2026-08-30** he rewrote the second sentence on all four surfaces from "More plans of care get completed and the clinic stays full." to **"More completed plans of care and the clinic stays full."** — deliberately lifting the noun phrase straight out of the shared bullet "More completed plans of care, because engaged patients finish their care.", so the lead sentence and the bullet now use the same words. **The echo is intentional; do not "vary" it.**
  - **📐 The hero re-checked at both widths after that rewrite (the measure has needed retuning three times, so this is not optional).** `maxWidth: 28ch` still correct: **three balanced lines at 1280px, four lines at 375px**, no orphaned word at either, both buttons and the More Info link above the fold on a phone. The shorter sentence dropped mobile from five lines to four.
  - **Here:** the landing hero in `Landing.jsx`, and `whatGlowptIs.lead` in `src/lib/marketing.js`. **There:** the GlowPT card teaser and the GlowPT modal description, both in `franklinai-v2/src/App.jsx`.
  - **The modal lead LOST its problem-setup sentence** ("Patients walk out doing great, then drift off before their plan of care is done."). It is gone from both repos. Do not reinstate it on one side only.
  - **⚠️ THE REPETITION IS THE POINT, NOT AN OVERSIGHT.** The hero and the More Info modal now say nearly the same thing back to back. That was raised with David before it shipped and he chose it deliberately: what he likes, consistency, and the teaching value of repetition. **Do not "fix" the duplication.**
  - **It drops two things the old copy carried.** "Zero work for your therapists" now survives only as a modal bullet on both sites. The FranklinAI card **no longer names the niche**, which retires that half of FranklinAI's "where the niche is named" rule. Its headline still names the practice.
  - **⚠️ CROSS-REPO DEPENDENCY:** FranklinAI's GlowPT modal carries a ghost button labelled **"Bring GlowPT to Your Clinic"** pointing at `glowpt.app/onboard`, deliberately matching this app's own primary button so the click never lands on a page showing an identical button. **Change the landing button label here and that match breaks** — tell the FranklinAI side.
- **🌄 RIVERSIDE RE-PRISTINED AGAIN 2026-08-31**, run by Claude over the bastion tunnel. Verified after by querying rather than trusting the script's own output: **6 patients, 23 check-ins in the last 7 days, last check-in TODAY, clinic Open, 0 orphans, and the platform-admin row survived** (the preservation fix did its job — the script logged `preserving platform admin` then `Platform admin restored (1 row)`). **Riverside is now the ONLY clinic**, and therefore also the Monday heartbeat. ⚠️ **David must sign in again** — every demo Cognito account was destroyed and recreated with a fresh sub, so his stored tokens are dead. Not a bug.
- **📐 THREE FIXES TO THE PUBLIC PAGES, 2026-08-30 (David, all three raised by eye on the live site).**
  1. **The More Info modal's footer is now CENTERED.** `s.price` and `s.contact` in `Landing.jsx` gained `textAlign: 'center'`. **The modal panel itself stays `textAlign: 'left'` and must** — the bullets need to read as a list. But the footer is a closing statement sitting under a rule with a **centered Close button beneath it**, and left-aligned text above a centered button reads as a mistake. David's words: "it's been making me crazy."
  2. **The BAA summary line MOVED from below the submit button to directly under the BAA checkbox.** "This is a summary. You'll review and sign the full agreement before any real patient information enters GlowPT." **David was right that it belongs there:** it qualifies the checkbox's link, which opens a *summary* rather than the executable agreement. Below the button it read as generic footer fine print, and a manager could tick the box without ever meeting it. **It is gated on the same `!BAA_IS_EXECUTED` flag as the checkbox's own "I've reviewed" wording, so the two are a pair** — when the BAA is executed the checkbox becomes "I agree" and this line disappears. **Move them together or not at all.**
  3. **That line no longer uses `ui.fine`.** `ui.fine` is **italic Fraunces**, and every other line on `/onboard` is upright DM Sans, so it read as a different typeface sitting in the middle of a form. It is now an inline DM Sans style matching the closed-clinic note below the button.
  - **✅ AND THEN `ui.fine` ITSELF WENT UPRIGHT, APP-WIDE (David, same session).** It was left alone at first, on the reasoning that restyling a shared token off one complaint about one screen would sweep four others. **David asked for the sweep, so `ui.fine` in `AuthShell.jsx` dropped BOTH `fontStyle: 'italic'` AND `fontFamily: "'Fraunces', serif"`** and now inherits DM Sans from the shell. **Removing only the italic would have left upright *Fraunces*, still a serif among sans — the family had to go too.**
  - **Fine print is now distinguished by size and opacity alone (12px at 0.35), which is the whole job.** A serif italic dropped into otherwise upright sans screens read as a different typeface rather than as quieter text.
  - **Affects five screens:** `Login.jsx`, `Join.jsx`, `NoClinic.jsx`, `CodeVerify.jsx`, and the shared token itself. **Verified by rendering `/login` and `/join/riverside-pt`.** ⚠️ **`NoClinic` and `CodeVerify` were NOT seen** — both sit behind a flow Claude cannot drive. They use the same token with no local overrides, so they follow mechanically, but that is inference rather than observation.
  - **Both `/onboard` notes moved `maxWidth` 38ch to 44ch.** At 38ch each stranded a single word on its last line ("GlowPT." and "sign up."). At 44ch both sit in two balanced lines on desktop. **Mobile is unaffected: the 430px shell binds there, not `maxWidth`.** Verified by rendering at 375px and at desktop.
- **📧 THE WEEKLY EMAIL CARRIES THE REAL LOGO (2026-08-31).** There was **no logo in the email at all** before this — just the text wordmark and a stock 🌅 emoji standing in for a mark. It now leads with `${APP_URL}/apple-touch-icon.png`, i.e. **the same PNG the app and the browser tab use, rendered from `public/favicon.svg`, so the email physically cannot drift from the product.** Email clients will not render SVG, which is why that PNG is the right asset and `favicon.svg` is not.
  - **⛔ THE TEXT WORDMARK UNDER THE LOGO IS NOT REDUNDANT — DO NOT DELETE IT.** Most clients block remote images until the reader allows them, so with images off the email must still read as GlowPT rather than as a broken box. The wordmark is the fallback.
  - **If the logo art ever changes, re-render `public/apple-touch-icon.png`** (headless Chrome at `--force-device-scale-factor=3 --window-size=180,180`, then `sips -z 180 180`) — the email, the tab and the home-screen icon all follow that one file. This is the same derived-copy rule already recorded under the brand entries.
  - **Verified by rendering all four variants** (patient with and without check-ins, clinic with and without attention needed) and looking at them, then by a real send David confirmed in his inbox.
- **🚫 NO EMOJI IN THE PRODUCT, WITH TWO DELIBERATE EXCEPTIONS (David's call, 2026-08-31).** He judged the stock 🌅 in the email off-brand, and the same reasoning swept the app. Gone: the sunrise from the email body **and subject**, and `🌟` `🌤` `💛` `🌱` from the four streak/trend messages in `PatientApp.jsx` (which now end in a period, as the house copy rule already wanted — their siblings always did).
  - **✅ EXCEPTION 1 — the five mood faces in `src/lib/feelings.js` (😔😕🙂😊😄) were never touched and must not be.** They are not decoration, they are **the interface**: the patient taps them to check in and the dashboard renders the same five as the shared patient/clinic language. Removing them breaks the product.
  - **✅ EXCEPTION 2 — the streak 🔥 was swept and then PUT BACK at David's request** (`3033b05` removed it, `5b3aa6e` restored it), in both the patient streak hero and the dashboard roster cell. **The distinction is worth keeping:** the four that stayed gone were mood decoration tacked onto the end of sentences; **the flame is a unit marker on a number**, doing structural work like a progress indicator, and it is near-universal shorthand for a streak. Removing it took away meaning, not clutter.
  - **⚠️ The bare `—` in the dashboard streak cell survived both passes and must keep surviving.** It is one of the six protected empty-value placeholders, a UI glyph meaning "nothing here", not prose.
- **📌 THE BASTION AMI IS PINNED, AND THIS PREVENTED A RECURRING TRAP (2026-08-31).** `cdk diff` while shipping the email change showed, alongside the two expected Lambdas, **`AWS::EC2::Instance Bastion/Host/Resource ... replace`** — nothing to do with the change being deployed. `BastionHostLinux` resolves "latest Amazon Linux" through an SSM parameter **at synth time**, so once AWS publishes a new image **every** deploy wants to rebuild the host regardless of its subject.
  - **Why that is worse than cosmetic:** a replacement returns a **new instance id**, and `i-04b7a6d483e8b682a` is written into the DB runbook in this file and into every start-the-bastion-and-open-the-tunnel instruction. It would have broken quietly, on some future deploy about something else entirely.
  - Pinned in `infra/lib/bastion.ts` to the AMI the host already runs (`ami-068e33c5263812a9b`). Diff is now exactly the Lambdas; **confirmed across two deploys that `BastionInstanceId` is unchanged.** Frozen is right for a jump host that opens no inbound ports and sits stopped between sessions. **Bump it deliberately, in its own change, never as a side effect.**
  - **This is why the house rule is `cdk diff` first and STOP on any unexpected replacement.** It worked exactly as intended.
- **Backlog / future:** ~~"Invite therapist" should send a real invite email~~ **✅ DONE 2026-09-04, and it was worse than a missing email: the therapist could not create an account at all. See the staff-invite entry above.** Stripe subscriptions + billing; **beyond-PT** expansion (chiropractic, mental/behavioral health, chronic care, coaching, wellness — note "PT" can also read as *Physical Transformation*).

